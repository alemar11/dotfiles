#compdef swift

__swift_complete() {
    local -ar non_empty_completions=("${@:#(|:*)}")
    local -ar empty_completions=("${(M)@:#(|:*)}")
    _describe -V '' non_empty_completions -- empty_completions -P $'\'\''
}

__swift_custom_complete() {
    local -a completions
    completions=("${(@f)"$("${command_name}" "${@}" "${command_line[@]}")"}")
    if [[ "${#completions[@]}" -gt 1 ]]; then
        __swift_complete "${completions[@]:0:-1}"
    fi
}

__swift_cursor_index_in_current_word() {
    if [[ -z "${QIPREFIX}${IPREFIX}${PREFIX}" ]]; then
        printf 0
    else
        printf %s "${#${(z)LBUFFER}[-1]}"
    fi
}

_swift() {
    emulate -RL zsh -G
    setopt extendedglob nullglob numericglobsort
    unsetopt aliases banghist

    local -xr SAP_SHELL=zsh
    local -x SAP_SHELL_VERSION
    SAP_SHELL_VERSION="$(builtin emulate zsh -c 'printf %s "${ZSH_VERSION}"')"
    local -r SAP_SHELL_VERSION

    local context state state_descr line
    local -A opt_args

    local -r command_name="${words[1]}"
    local -ar command_line=("${words[@]}")
    local -ir current_word_index="$((CURRENT - 1))"

    local -i ret=1
    local -ar arg_specs=(
        '(-h --help)'{-h,--help}'[Show help information.]'
        '(-): :->command'
        '(-)*:: :->arg'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0
    case "${state}" in
    command)
        local -ar subcommands=(
            'run:Build and run an executable product.'
            'build:Build sources into binary products.'
            'test:Build and run tests.'
            'package:Perform operations on Swift packages.'
            'help:Show subcommand help information.'
        )
        _describe -V subcommand subcommands
        ;;
    arg)
        case "${words[1]}" in
        run|build|test|package|help)
            "_swift_${words[1]}"
            ;;
        esac
        ;;
    esac

    return "${ret}"
}

_swift_run() {
    local -i ret=1
    local -ar ___configuration=('debug' 'release')
    local -ar ___sanitize=('address' 'thread' 'undefined' 'scudo' 'fuzzer')
    local -ar ___explicit_target_dependency_import_check=('none' 'warn' 'error')
    local -ar ___build_system=('native' 'swiftbuild' 'xcode')
    local -ar __debug_info_format=('dwarf' 'codeview' 'none')
    local -ar arg_specs=(
        '--package-path[Specify the package path to operate on (default current directory). This changes the working directory before any other operation.]:package-path:_files -/'
        '--cache-path[Specify the shared cache directory path.]:cache-path:_files -/'
        '--config-path[Specify the shared configuration directory path.]:config-path:_files -/'
        '--security-path[Specify the shared security directory path.]:security-path:_files -/'
        '--scratch-path[Specify a custom scratch directory path. (default .build)]:scratch-path:_files -/'
        '--swift-sdks-path[Path to the directory containing installed Swift SDKs.]:swift-sdks-path:_files -/'
        '*--toolset[Specify a toolset JSON file to use when building for the target platform. Use the option multiple times to specify more than one toolset. Toolsets will be merged in the order they'\''re specified into a single final toolset for the current build.]:toolset:_files -g '\''*..json'\'''
        '*--pkg-config-path[Specify alternative path to search for pkg-config `.pc` files. Use the option multiple times to
        specify more than one path.]:pkg-config-path:_files -/'
        '--enable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--disable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--enable-build-manifest-caching'
        '--disable-build-manifest-caching'
        '--manifest-cache[Caching mode of Package.swift manifests. Valid values are\: (shared\: shared cache, local\: package'\''s build directory, none\: disabled)]:manifest-cache:'
        '--enable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '--disable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '(--verbose -v)'{--verbose,-v}'[Increase verbosity to include informational output.]'
        '(--very-verbose --vv)'{--very-verbose,--vv}'[Increase verbosity to include debug output.]'
        '(--quiet -q)'{--quiet,-q}'[Decrease verbosity to only include error output.]'
        '--color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--no-color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--disable-sandbox[Disable using the sandbox when executing subprocesses.]'
        '--netrc[Use netrc file even in cases where other credential stores are preferred.]'
        '--enable-netrc[Load credentials from a netrc file.]'
        '--disable-netrc[Load credentials from a netrc file.]'
        '--netrc-file[Specify the netrc file path.]:netrc-file:_files'
        '--enable-keychain[Search credentials in macOS keychain.]'
        '--disable-keychain[Search credentials in macOS keychain.]'
        '--resolver-fingerprint-checking:resolver-fingerprint-checking:'
        '--resolver-signing-entity-checking:resolver-signing-entity-checking:'
        '--enable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--disable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--enable-prefetching'
        '--disable-prefetching'
        '(--force-resolved-versions --disable-automatic-resolution --only-use-versions-from-resolved-file)'{--force-resolved-versions,--disable-automatic-resolution,--only-use-versions-from-resolved-file}'[Only use versions from the Package.resolved file and fail resolution if it is out-of-date.]'
        '--skip-update[Skip updating dependencies from their remote during a resolution.]'
        '--disable-scm-to-registry-transformation[Disable source control to registry transformation.]'
        '--use-registry-identity-for-scm[Look up source control dependencies in the registry and use their registry identity when possible to help deduplicate across the two origins.]'
        '--replace-scm-with-registry[Look up source control dependencies in the registry and use the registry to retrieve them instead of source control when possible.]'
        '--default-registry-url[Default registry URL to use, instead of the registries.json configuration file.]:default-registry-url:'
        '(--configuration -c)'{--configuration,-c}'[Build with configuration]:configuration:{__swift_complete "${___configuration[@]}"}'
        '*-Xcc[Pass flag through to all C compiler invocations.]:Xcc:'
        '*-Xswiftc[Pass flag through to all Swift compiler invocations.]:Xswiftc:'
        '*-Xlinker[Pass flag through to all linker invocations.]:Xlinker:'
        '*-Xcxx[Pass flag through to all C++ compiler invocations.]:Xcxx:'
        '--triple:triple:'
        '--sdk:sdk:_files -/'
        '--toolchain:toolchain:_files -/'
        '--swift-sdk[Filter for selecting a specific Swift SDK to build with.]:swift-sdk:'
        '*--sanitize[Turn on runtime checks for erroneous behavior.]:sanitize:{__swift_complete "${___sanitize[@]}"}'
        '--auto-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-index-store[Enable or disable indexing-while-building feature.]'
        '--disable-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-parseable-module-interfaces'
        '(--jobs -j)'{--jobs,-j}'[The number of jobs to spawn in parallel during the build process.]:jobs:'
        '--use-integrated-swift-driver'
        '--explicit-target-dependency-import-check[A flag that indicates this build should check whether targets only import their explicitly-declared dependencies.]:explicit-target-dependency-import-check:{__swift_complete "${___explicit_target_dependency_import_check[@]}"}'
        '--build-system[Specify the build system to use.]:build-system:{__swift_complete "${___build_system[@]}"}'
        '-debug-info-format[The Debug Information Format to use.]:debug-info-format:{__swift_complete "${__debug_info_format[@]}"}'
        '--enable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-local-rpath[Disable adding $ORIGIN/@loader_path to the rpath by default.]'
        '--traits[Enables the passed traits of the package. Multiple traits can be specified by providing a comma separated list e.g. `--traits Trait1,Trait2`. When enabling specific traits the defaults traits need to explictily enabled as well by passing `defaults` to this command.]:traits:'
        '--enable-all-traits[Enables all traits of the package.]'
        '--disable-default-traits[Disables all default traits of the package.]'
        '--repl[Launch Swift REPL for the package.]'
        '--debugger[Launch the executable in a debugger session.]'
        '--run[Launch the executable with the provided arguments.]'
        '--skip-build[Skip building the executable product.]'
        '--build-tests[Build both source and test targets.]'
        ':executable:{local -a list;list=(${(f)"$(swift package completion-tool list-executables)"});_describe -V "" list}'
        '*:arguments:'
        '--version[Show the version.]'
        '(-help -h --help)'{-help,-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_swift_build() {
    local -i ret=1
    local -ar ___configuration=('debug' 'release')
    local -ar ___sanitize=('address' 'thread' 'undefined' 'scudo' 'fuzzer')
    local -ar ___explicit_target_dependency_import_check=('none' 'warn' 'error')
    local -ar ___build_system=('native' 'swiftbuild' 'xcode')
    local -ar __debug_info_format=('dwarf' 'codeview' 'none')
    local -ar arg_specs=(
        '--package-path[Specify the package path to operate on (default current directory). This changes the working directory before any other operation.]:package-path:_files -/'
        '--cache-path[Specify the shared cache directory path.]:cache-path:_files -/'
        '--config-path[Specify the shared configuration directory path.]:config-path:_files -/'
        '--security-path[Specify the shared security directory path.]:security-path:_files -/'
        '--scratch-path[Specify a custom scratch directory path. (default .build)]:scratch-path:_files -/'
        '--swift-sdks-path[Path to the directory containing installed Swift SDKs.]:swift-sdks-path:_files -/'
        '*--toolset[Specify a toolset JSON file to use when building for the target platform. Use the option multiple times to specify more than one toolset. Toolsets will be merged in the order they'\''re specified into a single final toolset for the current build.]:toolset:_files -g '\''*..json'\'''
        '*--pkg-config-path[Specify alternative path to search for pkg-config `.pc` files. Use the option multiple times to
        specify more than one path.]:pkg-config-path:_files -/'
        '--enable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--disable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--enable-build-manifest-caching'
        '--disable-build-manifest-caching'
        '--manifest-cache[Caching mode of Package.swift manifests. Valid values are\: (shared\: shared cache, local\: package'\''s build directory, none\: disabled)]:manifest-cache:'
        '--enable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '--disable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '(--verbose -v)'{--verbose,-v}'[Increase verbosity to include informational output.]'
        '(--very-verbose --vv)'{--very-verbose,--vv}'[Increase verbosity to include debug output.]'
        '(--quiet -q)'{--quiet,-q}'[Decrease verbosity to only include error output.]'
        '--color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--no-color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--disable-sandbox[Disable using the sandbox when executing subprocesses.]'
        '--netrc[Use netrc file even in cases where other credential stores are preferred.]'
        '--enable-netrc[Load credentials from a netrc file.]'
        '--disable-netrc[Load credentials from a netrc file.]'
        '--netrc-file[Specify the netrc file path.]:netrc-file:_files'
        '--enable-keychain[Search credentials in macOS keychain.]'
        '--disable-keychain[Search credentials in macOS keychain.]'
        '--resolver-fingerprint-checking:resolver-fingerprint-checking:'
        '--resolver-signing-entity-checking:resolver-signing-entity-checking:'
        '--enable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--disable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--enable-prefetching'
        '--disable-prefetching'
        '(--force-resolved-versions --disable-automatic-resolution --only-use-versions-from-resolved-file)'{--force-resolved-versions,--disable-automatic-resolution,--only-use-versions-from-resolved-file}'[Only use versions from the Package.resolved file and fail resolution if it is out-of-date.]'
        '--skip-update[Skip updating dependencies from their remote during a resolution.]'
        '--disable-scm-to-registry-transformation[Disable source control to registry transformation.]'
        '--use-registry-identity-for-scm[Look up source control dependencies in the registry and use their registry identity when possible to help deduplicate across the two origins.]'
        '--replace-scm-with-registry[Look up source control dependencies in the registry and use the registry to retrieve them instead of source control when possible.]'
        '--default-registry-url[Default registry URL to use, instead of the registries.json configuration file.]:default-registry-url:'
        '(--configuration -c)'{--configuration,-c}'[Build with configuration]:configuration:{__swift_complete "${___configuration[@]}"}'
        '*-Xcc[Pass flag through to all C compiler invocations.]:Xcc:'
        '*-Xswiftc[Pass flag through to all Swift compiler invocations.]:Xswiftc:'
        '*-Xlinker[Pass flag through to all linker invocations.]:Xlinker:'
        '*-Xcxx[Pass flag through to all C++ compiler invocations.]:Xcxx:'
        '--triple:triple:'
        '--sdk:sdk:_files -/'
        '--toolchain:toolchain:_files -/'
        '--swift-sdk[Filter for selecting a specific Swift SDK to build with.]:swift-sdk:'
        '*--sanitize[Turn on runtime checks for erroneous behavior.]:sanitize:{__swift_complete "${___sanitize[@]}"}'
        '--auto-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-index-store[Enable or disable indexing-while-building feature.]'
        '--disable-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-parseable-module-interfaces'
        '(--jobs -j)'{--jobs,-j}'[The number of jobs to spawn in parallel during the build process.]:jobs:'
        '--use-integrated-swift-driver'
        '--explicit-target-dependency-import-check[A flag that indicates this build should check whether targets only import their explicitly-declared dependencies.]:explicit-target-dependency-import-check:{__swift_complete "${___explicit_target_dependency_import_check[@]}"}'
        '--build-system[Specify the build system to use.]:build-system:{__swift_complete "${___build_system[@]}"}'
        '-debug-info-format[The Debug Information Format to use.]:debug-info-format:{__swift_complete "${__debug_info_format[@]}"}'
        '--enable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-local-rpath[Disable adding $ORIGIN/@loader_path to the rpath by default.]'
        '--traits[Enables the passed traits of the package. Multiple traits can be specified by providing a comma separated list e.g. `--traits Trait1,Trait2`. When enabling specific traits the defaults traits need to explictily enabled as well by passing `defaults` to this command.]:traits:'
        '--enable-all-traits[Enables all traits of the package.]'
        '--disable-default-traits[Disables all default traits of the package.]'
        '--build-tests[Build both source and test targets.]'
        '--enable-code-coverage[Enable code coverage.]'
        '--disable-code-coverage[Enable code coverage.]'
        '--show-bin-path[Print the binary output path.]'
        '--print-manifest-job-graph[Write the command graph for the build manifest as a Graphviz file.]'
        '--print-pif-manifest-graph[Write the PIF JSON sent to Swift Build as a Graphviz file.]'
        '--target[Build the specified target.]:target:'
        '--product[Build the specified product.]:product:'
        '--enable-xctest[Enable support for XCTest.]'
        '--disable-xctest[Enable support for XCTest.]'
        '--enable-swift-testing[Enable support for Swift Testing.]'
        '--disable-swift-testing[Enable support for Swift Testing.]'
        '--static-swift-stdlib[Link Swift stdlib statically.]'
        '--no-static-swift-stdlib[Link Swift stdlib statically.]'
        '--version[Show the version.]'
        '(-help -h --help)'{-help,-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_swift_test() {
    local -i ret=1
    local -ar ___configuration=('debug' 'release')
    local -ar ___sanitize=('address' 'thread' 'undefined' 'scudo' 'fuzzer')
    local -ar ___explicit_target_dependency_import_check=('none' 'warn' 'error')
    local -ar ___build_system=('native' 'swiftbuild' 'xcode')
    local -ar __debug_info_format=('dwarf' 'codeview' 'none')
    local -ar arg_specs=(
        '--package-path[Specify the package path to operate on (default current directory). This changes the working directory before any other operation.]:package-path:_files -/'
        '--cache-path[Specify the shared cache directory path.]:cache-path:_files -/'
        '--config-path[Specify the shared configuration directory path.]:config-path:_files -/'
        '--security-path[Specify the shared security directory path.]:security-path:_files -/'
        '--scratch-path[Specify a custom scratch directory path. (default .build)]:scratch-path:_files -/'
        '--swift-sdks-path[Path to the directory containing installed Swift SDKs.]:swift-sdks-path:_files -/'
        '*--toolset[Specify a toolset JSON file to use when building for the target platform. Use the option multiple times to specify more than one toolset. Toolsets will be merged in the order they'\''re specified into a single final toolset for the current build.]:toolset:_files -g '\''*..json'\'''
        '*--pkg-config-path[Specify alternative path to search for pkg-config `.pc` files. Use the option multiple times to
        specify more than one path.]:pkg-config-path:_files -/'
        '--enable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--disable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--enable-build-manifest-caching'
        '--disable-build-manifest-caching'
        '--manifest-cache[Caching mode of Package.swift manifests. Valid values are\: (shared\: shared cache, local\: package'\''s build directory, none\: disabled)]:manifest-cache:'
        '--enable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '--disable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '(--verbose -v)'{--verbose,-v}'[Increase verbosity to include informational output.]'
        '(--very-verbose --vv)'{--very-verbose,--vv}'[Increase verbosity to include debug output.]'
        '(--quiet -q)'{--quiet,-q}'[Decrease verbosity to only include error output.]'
        '--color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--no-color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--disable-sandbox[Disable using the sandbox when executing subprocesses.]'
        '--netrc[Use netrc file even in cases where other credential stores are preferred.]'
        '--enable-netrc[Load credentials from a netrc file.]'
        '--disable-netrc[Load credentials from a netrc file.]'
        '--netrc-file[Specify the netrc file path.]:netrc-file:_files'
        '--enable-keychain[Search credentials in macOS keychain.]'
        '--disable-keychain[Search credentials in macOS keychain.]'
        '--resolver-fingerprint-checking:resolver-fingerprint-checking:'
        '--resolver-signing-entity-checking:resolver-signing-entity-checking:'
        '--enable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--disable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--enable-prefetching'
        '--disable-prefetching'
        '(--force-resolved-versions --disable-automatic-resolution --only-use-versions-from-resolved-file)'{--force-resolved-versions,--disable-automatic-resolution,--only-use-versions-from-resolved-file}'[Only use versions from the Package.resolved file and fail resolution if it is out-of-date.]'
        '--skip-update[Skip updating dependencies from their remote during a resolution.]'
        '--disable-scm-to-registry-transformation[Disable source control to registry transformation.]'
        '--use-registry-identity-for-scm[Look up source control dependencies in the registry and use their registry identity when possible to help deduplicate across the two origins.]'
        '--replace-scm-with-registry[Look up source control dependencies in the registry and use the registry to retrieve them instead of source control when possible.]'
        '--default-registry-url[Default registry URL to use, instead of the registries.json configuration file.]:default-registry-url:'
        '(--configuration -c)'{--configuration,-c}'[Build with configuration]:configuration:{__swift_complete "${___configuration[@]}"}'
        '*-Xcc[Pass flag through to all C compiler invocations.]:Xcc:'
        '*-Xswiftc[Pass flag through to all Swift compiler invocations.]:Xswiftc:'
        '*-Xlinker[Pass flag through to all linker invocations.]:Xlinker:'
        '*-Xcxx[Pass flag through to all C++ compiler invocations.]:Xcxx:'
        '--triple:triple:'
        '--sdk:sdk:_files -/'
        '--toolchain:toolchain:_files -/'
        '--swift-sdk[Filter for selecting a specific Swift SDK to build with.]:swift-sdk:'
        '*--sanitize[Turn on runtime checks for erroneous behavior.]:sanitize:{__swift_complete "${___sanitize[@]}"}'
        '--auto-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-index-store[Enable or disable indexing-while-building feature.]'
        '--disable-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-parseable-module-interfaces'
        '(--jobs -j)'{--jobs,-j}'[The number of jobs to spawn in parallel during the build process.]:jobs:'
        '--use-integrated-swift-driver'
        '--explicit-target-dependency-import-check[A flag that indicates this build should check whether targets only import their explicitly-declared dependencies.]:explicit-target-dependency-import-check:{__swift_complete "${___explicit_target_dependency_import_check[@]}"}'
        '--build-system[Specify the build system to use.]:build-system:{__swift_complete "${___build_system[@]}"}'
        '-debug-info-format[The Debug Information Format to use.]:debug-info-format:{__swift_complete "${__debug_info_format[@]}"}'
        '--enable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-local-rpath[Disable adding $ORIGIN/@loader_path to the rpath by default.]'
        '--traits[Enables the passed traits of the package. Multiple traits can be specified by providing a comma separated list e.g. `--traits Trait1,Trait2`. When enabling specific traits the defaults traits need to explictily enabled as well by passing `defaults` to this command.]:traits:'
        '--enable-all-traits[Enables all traits of the package.]'
        '--disable-default-traits[Disables all default traits of the package.]'
        '--skip-build[Skip building the test target.]'
        '--enable-xctest[Enable support for XCTest.]'
        '--disable-xctest[Enable support for XCTest.]'
        '--enable-swift-testing[Enable support for Swift Testing.]'
        '--disable-swift-testing[Enable support for Swift Testing.]'
        '--attachments-path[Path where attachments should be written (Swift Testing only). This path must be an existing directory the current user can write to. If not specified, any attachments created during testing are discarded.]:attachments-path:_files -/'
        '--parallel[Run the tests in parallel.]'
        '--no-parallel[Run the tests in parallel.]'
        '--num-workers[Number of tests to execute in parallel.]:num-workers:'
        '(--list-tests -l)'{--list-tests,-l}'[Lists test methods in specifier format.]'
        '(--show-codecov-path --show-code-coverage-path --show-coverage-path)'{--show-codecov-path,--show-code-coverage-path,--show-coverage-path}'[Print the path of the exported code coverage JSON file.]'
        '(-s --specifier)'{-s,--specifier}':specifier:'
        '*--filter[Run test cases that match a regular expression, Format\: '\''<test-target>.<test-case>'\'' or '\''<test-target>.<test-case>/<test>'\''.]:filter:'
        '*--skip[Skip test cases that match a regular expression, Example\: '\''--skip PerformanceTests'\''.]:skip:'
        '--xunit-output[Path where the xUnit xml file should be generated.]:xunit-output:_files -/'
        '--enable-testable-imports[Enable or disable testable imports. Enabled by default.]'
        '--disable-testable-imports[Enable or disable testable imports. Enabled by default.]'
        '--enable-code-coverage[Enable code coverage.]'
        '--disable-code-coverage[Enable code coverage.]'
        '--version[Show the version.]'
        '(-help -h --help)'{-help,-h,--help}'[Show help information.]'
        '(-): :->command'
        '(-)*:: :->arg'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0
    case "${state}" in
    command)
        local -ar subcommands=(
            'list:Lists test methods in specifier format'
            'last:'
        )
        _describe -V subcommand subcommands
        ;;
    arg)
        case "${words[1]}" in
        list|last)
            "_swift_test_${words[1]}"
            ;;
        esac
        ;;
    esac

    return "${ret}"
}

_swift_test_list() {
    local -i ret=1
    local -ar ___configuration=('debug' 'release')
    local -ar ___sanitize=('address' 'thread' 'undefined' 'scudo' 'fuzzer')
    local -ar ___explicit_target_dependency_import_check=('none' 'warn' 'error')
    local -ar ___build_system=('native' 'swiftbuild' 'xcode')
    local -ar __debug_info_format=('dwarf' 'codeview' 'none')
    local -ar arg_specs=(
        '--package-path[Specify the package path to operate on (default current directory). This changes the working directory before any other operation.]:package-path:_files -/'
        '--cache-path[Specify the shared cache directory path.]:cache-path:_files -/'
        '--config-path[Specify the shared configuration directory path.]:config-path:_files -/'
        '--security-path[Specify the shared security directory path.]:security-path:_files -/'
        '--scratch-path[Specify a custom scratch directory path. (default .build)]:scratch-path:_files -/'
        '--swift-sdks-path[Path to the directory containing installed Swift SDKs.]:swift-sdks-path:_files -/'
        '*--toolset[Specify a toolset JSON file to use when building for the target platform. Use the option multiple times to specify more than one toolset. Toolsets will be merged in the order they'\''re specified into a single final toolset for the current build.]:toolset:_files -g '\''*..json'\'''
        '*--pkg-config-path[Specify alternative path to search for pkg-config `.pc` files. Use the option multiple times to
        specify more than one path.]:pkg-config-path:_files -/'
        '--enable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--disable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--enable-build-manifest-caching'
        '--disable-build-manifest-caching'
        '--manifest-cache[Caching mode of Package.swift manifests. Valid values are\: (shared\: shared cache, local\: package'\''s build directory, none\: disabled)]:manifest-cache:'
        '--enable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '--disable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '(--verbose -v)'{--verbose,-v}'[Increase verbosity to include informational output.]'
        '(--very-verbose --vv)'{--very-verbose,--vv}'[Increase verbosity to include debug output.]'
        '(--quiet -q)'{--quiet,-q}'[Decrease verbosity to only include error output.]'
        '--color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--no-color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--disable-sandbox[Disable using the sandbox when executing subprocesses.]'
        '--netrc[Use netrc file even in cases where other credential stores are preferred.]'
        '--enable-netrc[Load credentials from a netrc file.]'
        '--disable-netrc[Load credentials from a netrc file.]'
        '--netrc-file[Specify the netrc file path.]:netrc-file:_files'
        '--enable-keychain[Search credentials in macOS keychain.]'
        '--disable-keychain[Search credentials in macOS keychain.]'
        '--resolver-fingerprint-checking:resolver-fingerprint-checking:'
        '--resolver-signing-entity-checking:resolver-signing-entity-checking:'
        '--enable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--disable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--enable-prefetching'
        '--disable-prefetching'
        '(--force-resolved-versions --disable-automatic-resolution --only-use-versions-from-resolved-file)'{--force-resolved-versions,--disable-automatic-resolution,--only-use-versions-from-resolved-file}'[Only use versions from the Package.resolved file and fail resolution if it is out-of-date.]'
        '--skip-update[Skip updating dependencies from their remote during a resolution.]'
        '--disable-scm-to-registry-transformation[Disable source control to registry transformation.]'
        '--use-registry-identity-for-scm[Look up source control dependencies in the registry and use their registry identity when possible to help deduplicate across the two origins.]'
        '--replace-scm-with-registry[Look up source control dependencies in the registry and use the registry to retrieve them instead of source control when possible.]'
        '--default-registry-url[Default registry URL to use, instead of the registries.json configuration file.]:default-registry-url:'
        '(--configuration -c)'{--configuration,-c}'[Build with configuration]:configuration:{__swift_complete "${___configuration[@]}"}'
        '*-Xcc[Pass flag through to all C compiler invocations.]:Xcc:'
        '*-Xswiftc[Pass flag through to all Swift compiler invocations.]:Xswiftc:'
        '*-Xlinker[Pass flag through to all linker invocations.]:Xlinker:'
        '*-Xcxx[Pass flag through to all C++ compiler invocations.]:Xcxx:'
        '--triple:triple:'
        '--sdk:sdk:_files -/'
        '--toolchain:toolchain:_files -/'
        '--swift-sdk[Filter for selecting a specific Swift SDK to build with.]:swift-sdk:'
        '*--sanitize[Turn on runtime checks for erroneous behavior.]:sanitize:{__swift_complete "${___sanitize[@]}"}'
        '--auto-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-index-store[Enable or disable indexing-while-building feature.]'
        '--disable-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-parseable-module-interfaces'
        '(--jobs -j)'{--jobs,-j}'[The number of jobs to spawn in parallel during the build process.]:jobs:'
        '--use-integrated-swift-driver'
        '--explicit-target-dependency-import-check[A flag that indicates this build should check whether targets only import their explicitly-declared dependencies.]:explicit-target-dependency-import-check:{__swift_complete "${___explicit_target_dependency_import_check[@]}"}'
        '--build-system[Specify the build system to use.]:build-system:{__swift_complete "${___build_system[@]}"}'
        '-debug-info-format[The Debug Information Format to use.]:debug-info-format:{__swift_complete "${__debug_info_format[@]}"}'
        '--enable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-local-rpath[Disable adding $ORIGIN/@loader_path to the rpath by default.]'
        '--traits[Enables the passed traits of the package. Multiple traits can be specified by providing a comma separated list e.g. `--traits Trait1,Trait2`. When enabling specific traits the defaults traits need to explictily enabled as well by passing `defaults` to this command.]:traits:'
        '--enable-all-traits[Enables all traits of the package.]'
        '--disable-default-traits[Disables all default traits of the package.]'
        '--skip-build[Skip building the test target.]'
        '--enable-xctest[Enable support for XCTest.]'
        '--disable-xctest[Enable support for XCTest.]'
        '--enable-swift-testing[Enable support for Swift Testing.]'
        '--disable-swift-testing[Enable support for Swift Testing.]'
        '--attachments-path[Path where attachments should be written (Swift Testing only). This path must be an existing directory the current user can write to. If not specified, any attachments created during testing are discarded.]:attachments-path:_files -/'
        '--version[Show the version.]'
        '(-help -h --help)'{-help,-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_swift_test_last() {
    local -i ret=1
    local -ar ___configuration=('debug' 'release')
    local -ar ___sanitize=('address' 'thread' 'undefined' 'scudo' 'fuzzer')
    local -ar ___explicit_target_dependency_import_check=('none' 'warn' 'error')
    local -ar ___build_system=('native' 'swiftbuild' 'xcode')
    local -ar __debug_info_format=('dwarf' 'codeview' 'none')
    local -ar arg_specs=(
        '--package-path[Specify the package path to operate on (default current directory). This changes the working directory before any other operation.]:package-path:_files -/'
        '--cache-path[Specify the shared cache directory path.]:cache-path:_files -/'
        '--config-path[Specify the shared configuration directory path.]:config-path:_files -/'
        '--security-path[Specify the shared security directory path.]:security-path:_files -/'
        '--scratch-path[Specify a custom scratch directory path. (default .build)]:scratch-path:_files -/'
        '--swift-sdks-path[Path to the directory containing installed Swift SDKs.]:swift-sdks-path:_files -/'
        '*--toolset[Specify a toolset JSON file to use when building for the target platform. Use the option multiple times to specify more than one toolset. Toolsets will be merged in the order they'\''re specified into a single final toolset for the current build.]:toolset:_files -g '\''*..json'\'''
        '*--pkg-config-path[Specify alternative path to search for pkg-config `.pc` files. Use the option multiple times to
        specify more than one path.]:pkg-config-path:_files -/'
        '--enable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--disable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--enable-build-manifest-caching'
        '--disable-build-manifest-caching'
        '--manifest-cache[Caching mode of Package.swift manifests. Valid values are\: (shared\: shared cache, local\: package'\''s build directory, none\: disabled)]:manifest-cache:'
        '--enable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '--disable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '(--verbose -v)'{--verbose,-v}'[Increase verbosity to include informational output.]'
        '(--very-verbose --vv)'{--very-verbose,--vv}'[Increase verbosity to include debug output.]'
        '(--quiet -q)'{--quiet,-q}'[Decrease verbosity to only include error output.]'
        '--color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--no-color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--disable-sandbox[Disable using the sandbox when executing subprocesses.]'
        '--netrc[Use netrc file even in cases where other credential stores are preferred.]'
        '--enable-netrc[Load credentials from a netrc file.]'
        '--disable-netrc[Load credentials from a netrc file.]'
        '--netrc-file[Specify the netrc file path.]:netrc-file:_files'
        '--enable-keychain[Search credentials in macOS keychain.]'
        '--disable-keychain[Search credentials in macOS keychain.]'
        '--resolver-fingerprint-checking:resolver-fingerprint-checking:'
        '--resolver-signing-entity-checking:resolver-signing-entity-checking:'
        '--enable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--disable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--enable-prefetching'
        '--disable-prefetching'
        '(--force-resolved-versions --disable-automatic-resolution --only-use-versions-from-resolved-file)'{--force-resolved-versions,--disable-automatic-resolution,--only-use-versions-from-resolved-file}'[Only use versions from the Package.resolved file and fail resolution if it is out-of-date.]'
        '--skip-update[Skip updating dependencies from their remote during a resolution.]'
        '--disable-scm-to-registry-transformation[Disable source control to registry transformation.]'
        '--use-registry-identity-for-scm[Look up source control dependencies in the registry and use their registry identity when possible to help deduplicate across the two origins.]'
        '--replace-scm-with-registry[Look up source control dependencies in the registry and use the registry to retrieve them instead of source control when possible.]'
        '--default-registry-url[Default registry URL to use, instead of the registries.json configuration file.]:default-registry-url:'
        '(--configuration -c)'{--configuration,-c}'[Build with configuration]:configuration:{__swift_complete "${___configuration[@]}"}'
        '*-Xcc[Pass flag through to all C compiler invocations.]:Xcc:'
        '*-Xswiftc[Pass flag through to all Swift compiler invocations.]:Xswiftc:'
        '*-Xlinker[Pass flag through to all linker invocations.]:Xlinker:'
        '*-Xcxx[Pass flag through to all C++ compiler invocations.]:Xcxx:'
        '--triple:triple:'
        '--sdk:sdk:_files -/'
        '--toolchain:toolchain:_files -/'
        '--swift-sdk[Filter for selecting a specific Swift SDK to build with.]:swift-sdk:'
        '*--sanitize[Turn on runtime checks for erroneous behavior.]:sanitize:{__swift_complete "${___sanitize[@]}"}'
        '--auto-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-index-store[Enable or disable indexing-while-building feature.]'
        '--disable-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-parseable-module-interfaces'
        '(--jobs -j)'{--jobs,-j}'[The number of jobs to spawn in parallel during the build process.]:jobs:'
        '--use-integrated-swift-driver'
        '--explicit-target-dependency-import-check[A flag that indicates this build should check whether targets only import their explicitly-declared dependencies.]:explicit-target-dependency-import-check:{__swift_complete "${___explicit_target_dependency_import_check[@]}"}'
        '--build-system[Specify the build system to use.]:build-system:{__swift_complete "${___build_system[@]}"}'
        '-debug-info-format[The Debug Information Format to use.]:debug-info-format:{__swift_complete "${__debug_info_format[@]}"}'
        '--enable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-local-rpath[Disable adding $ORIGIN/@loader_path to the rpath by default.]'
        '--traits[Enables the passed traits of the package. Multiple traits can be specified by providing a comma separated list e.g. `--traits Trait1,Trait2`. When enabling specific traits the defaults traits need to explictily enabled as well by passing `defaults` to this command.]:traits:'
        '--enable-all-traits[Enables all traits of the package.]'
        '--disable-default-traits[Disables all default traits of the package.]'
        '--version[Show the version.]'
        '(-help -h --help)'{-help,-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_swift_package() {
    local -i ret=1
    local -ar ___configuration=('debug' 'release')
    local -ar ___sanitize=('address' 'thread' 'undefined' 'scudo' 'fuzzer')
    local -ar ___explicit_target_dependency_import_check=('none' 'warn' 'error')
    local -ar ___build_system=('native' 'swiftbuild' 'xcode')
    local -ar __debug_info_format=('dwarf' 'codeview' 'none')
    local -ar arg_specs=(
        '--package-path[Specify the package path to operate on (default current directory). This changes the working directory before any other operation.]:package-path:_files -/'
        '--cache-path[Specify the shared cache directory path.]:cache-path:_files -/'
        '--config-path[Specify the shared configuration directory path.]:config-path:_files -/'
        '--security-path[Specify the shared security directory path.]:security-path:_files -/'
        '--scratch-path[Specify a custom scratch directory path. (default .build)]:scratch-path:_files -/'
        '--swift-sdks-path[Path to the directory containing installed Swift SDKs.]:swift-sdks-path:_files -/'
        '*--toolset[Specify a toolset JSON file to use when building for the target platform. Use the option multiple times to specify more than one toolset. Toolsets will be merged in the order they'\''re specified into a single final toolset for the current build.]:toolset:_files -g '\''*..json'\'''
        '*--pkg-config-path[Specify alternative path to search for pkg-config `.pc` files. Use the option multiple times to
        specify more than one path.]:pkg-config-path:_files -/'
        '--enable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--disable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--enable-build-manifest-caching'
        '--disable-build-manifest-caching'
        '--manifest-cache[Caching mode of Package.swift manifests. Valid values are\: (shared\: shared cache, local\: package'\''s build directory, none\: disabled)]:manifest-cache:'
        '--enable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '--disable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '(--verbose -v)'{--verbose,-v}'[Increase verbosity to include informational output.]'
        '(--very-verbose --vv)'{--very-verbose,--vv}'[Increase verbosity to include debug output.]'
        '(--quiet -q)'{--quiet,-q}'[Decrease verbosity to only include error output.]'
        '--color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--no-color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--disable-sandbox[Disable using the sandbox when executing subprocesses.]'
        '--netrc[Use netrc file even in cases where other credential stores are preferred.]'
        '--enable-netrc[Load credentials from a netrc file.]'
        '--disable-netrc[Load credentials from a netrc file.]'
        '--netrc-file[Specify the netrc file path.]:netrc-file:_files'
        '--enable-keychain[Search credentials in macOS keychain.]'
        '--disable-keychain[Search credentials in macOS keychain.]'
        '--resolver-fingerprint-checking:resolver-fingerprint-checking:'
        '--resolver-signing-entity-checking:resolver-signing-entity-checking:'
        '--enable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--disable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--enable-prefetching'
        '--disable-prefetching'
        '(--force-resolved-versions --disable-automatic-resolution --only-use-versions-from-resolved-file)'{--force-resolved-versions,--disable-automatic-resolution,--only-use-versions-from-resolved-file}'[Only use versions from the Package.resolved file and fail resolution if it is out-of-date.]'
        '--skip-update[Skip updating dependencies from their remote during a resolution.]'
        '--disable-scm-to-registry-transformation[Disable source control to registry transformation.]'
        '--use-registry-identity-for-scm[Look up source control dependencies in the registry and use their registry identity when possible to help deduplicate across the two origins.]'
        '--replace-scm-with-registry[Look up source control dependencies in the registry and use the registry to retrieve them instead of source control when possible.]'
        '--default-registry-url[Default registry URL to use, instead of the registries.json configuration file.]:default-registry-url:'
        '(--configuration -c)'{--configuration,-c}'[Build with configuration]:configuration:{__swift_complete "${___configuration[@]}"}'
        '*-Xcc[Pass flag through to all C compiler invocations.]:Xcc:'
        '*-Xswiftc[Pass flag through to all Swift compiler invocations.]:Xswiftc:'
        '*-Xlinker[Pass flag through to all linker invocations.]:Xlinker:'
        '*-Xcxx[Pass flag through to all C++ compiler invocations.]:Xcxx:'
        '--triple:triple:'
        '--sdk:sdk:_files -/'
        '--toolchain:toolchain:_files -/'
        '--swift-sdk[Filter for selecting a specific Swift SDK to build with.]:swift-sdk:'
        '*--sanitize[Turn on runtime checks for erroneous behavior.]:sanitize:{__swift_complete "${___sanitize[@]}"}'
        '--auto-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-index-store[Enable or disable indexing-while-building feature.]'
        '--disable-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-parseable-module-interfaces'
        '(--jobs -j)'{--jobs,-j}'[The number of jobs to spawn in parallel during the build process.]:jobs:'
        '--use-integrated-swift-driver'
        '--explicit-target-dependency-import-check[A flag that indicates this build should check whether targets only import their explicitly-declared dependencies.]:explicit-target-dependency-import-check:{__swift_complete "${___explicit_target_dependency_import_check[@]}"}'
        '--build-system[Specify the build system to use.]:build-system:{__swift_complete "${___build_system[@]}"}'
        '-debug-info-format[The Debug Information Format to use.]:debug-info-format:{__swift_complete "${__debug_info_format[@]}"}'
        '--enable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-local-rpath[Disable adding $ORIGIN/@loader_path to the rpath by default.]'
        '--traits[Enables the passed traits of the package. Multiple traits can be specified by providing a comma separated list e.g. `--traits Trait1,Trait2`. When enabling specific traits the defaults traits need to explictily enabled as well by passing `defaults` to this command.]:traits:'
        '--enable-all-traits[Enables all traits of the package.]'
        '--disable-default-traits[Disables all default traits of the package.]'
        '--version[Show the version.]'
        '(-): :->command'
        '(-)*:: :->arg'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0
    case "${state}" in
    command)
        local -ar subcommands=(
            'add-dependency:Add a package dependency to the manifest.'
            'add-product:Add a new product to the manifest.'
            'add-target:Add a new target to the manifest.'
            'add-target-dependency:Add a new target dependency to the manifest.'
            'add-setting:Add a new setting to the manifest.'
            'experimental-audit-binary-artifact:Audit a static library binary artifact for undefined symbols.'
            'clean:Delete build artifacts.'
            'purge-cache:Purge the global repository cache.'
            'reset:Reset the complete cache/build directory.'
            'update:Update package dependencies.'
            'describe:Describe the current package.'
            'init:Initialize a new package.'
            'migrate:Migrate a package or its individual targets to use the given set of features.'
            'experimental-install:Offers the ability to install executable products of the current package.'
            'experimental-uninstall:Offers the ability to uninstall executable products previously installed by `swift package experimental-install`.'
            'diagnose-api-breaking-changes:Diagnose API-breaking changes to Swift modules in a package.'
            'dump-symbol-graph:Dump symbol graphs.'
            'dump-package:Print parsed Package.swift as JSON.'
            'edit:Put a package in editable mode.'
            'unedit:Remove a package from editable mode.'
            'config:Manipulate configuration of the package'
            'resolve:Resolve package dependencies.'
            'show-dependencies:Print the resolved dependency graph.'
            'show-executables:List the available executables from this package.'
            'show-traits:List the available traits for a package.'
            'tools-version:Manipulate tools version of the current package.'
            'compute-checksum:Compute the checksum for a binary artifact.'
            'archive-source:Create a source archive for the package.'
            'completion-tool:Command to generate shell completions.'
            'plugin:Invoke a command plugin or perform other actions on command plugins.'
        )
        _describe -V subcommand subcommands
        ;;
    arg)
        case "${words[1]}" in
        add-dependency|add-product|add-target|add-target-dependency|add-setting|experimental-audit-binary-artifact|clean|purge-cache|reset|update|describe|init|migrate|experimental-install|experimental-uninstall|diagnose-api-breaking-changes|dump-symbol-graph|dump-package|edit|unedit|config|resolve|show-dependencies|show-executables|show-traits|tools-version|compute-checksum|archive-source|completion-tool|plugin)
            "_swift_package_${words[1]}"
            ;;
        esac
        ;;
    esac

    return "${ret}"
}

_swift_package_add-dependency() {
    local -i ret=1
    local -ar ___configuration=('debug' 'release')
    local -ar ___sanitize=('address' 'thread' 'undefined' 'scudo' 'fuzzer')
    local -ar ___explicit_target_dependency_import_check=('none' 'warn' 'error')
    local -ar ___build_system=('native' 'swiftbuild' 'xcode')
    local -ar __debug_info_format=('dwarf' 'codeview' 'none')
    local -ar ___type=('url' 'path' 'registry')
    local -ar arg_specs=(
        ':dependency:'
        '--package-path[Specify the package path to operate on (default current directory). This changes the working directory before any other operation.]:package-path:_files -/'
        '--cache-path[Specify the shared cache directory path.]:cache-path:_files -/'
        '--config-path[Specify the shared configuration directory path.]:config-path:_files -/'
        '--security-path[Specify the shared security directory path.]:security-path:_files -/'
        '--scratch-path[Specify a custom scratch directory path. (default .build)]:scratch-path:_files -/'
        '--swift-sdks-path[Path to the directory containing installed Swift SDKs.]:swift-sdks-path:_files -/'
        '*--toolset[Specify a toolset JSON file to use when building for the target platform. Use the option multiple times to specify more than one toolset. Toolsets will be merged in the order they'\''re specified into a single final toolset for the current build.]:toolset:_files -g '\''*..json'\'''
        '*--pkg-config-path[Specify alternative path to search for pkg-config `.pc` files. Use the option multiple times to
        specify more than one path.]:pkg-config-path:_files -/'
        '--enable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--disable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--enable-build-manifest-caching'
        '--disable-build-manifest-caching'
        '--manifest-cache[Caching mode of Package.swift manifests. Valid values are\: (shared\: shared cache, local\: package'\''s build directory, none\: disabled)]:manifest-cache:'
        '--enable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '--disable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '(--verbose -v)'{--verbose,-v}'[Increase verbosity to include informational output.]'
        '(--very-verbose --vv)'{--very-verbose,--vv}'[Increase verbosity to include debug output.]'
        '(--quiet -q)'{--quiet,-q}'[Decrease verbosity to only include error output.]'
        '--color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--no-color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--disable-sandbox[Disable using the sandbox when executing subprocesses.]'
        '--netrc[Use netrc file even in cases where other credential stores are preferred.]'
        '--enable-netrc[Load credentials from a netrc file.]'
        '--disable-netrc[Load credentials from a netrc file.]'
        '--netrc-file[Specify the netrc file path.]:netrc-file:_files'
        '--enable-keychain[Search credentials in macOS keychain.]'
        '--disable-keychain[Search credentials in macOS keychain.]'
        '--resolver-fingerprint-checking:resolver-fingerprint-checking:'
        '--resolver-signing-entity-checking:resolver-signing-entity-checking:'
        '--enable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--disable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--enable-prefetching'
        '--disable-prefetching'
        '(--force-resolved-versions --disable-automatic-resolution --only-use-versions-from-resolved-file)'{--force-resolved-versions,--disable-automatic-resolution,--only-use-versions-from-resolved-file}'[Only use versions from the Package.resolved file and fail resolution if it is out-of-date.]'
        '--skip-update[Skip updating dependencies from their remote during a resolution.]'
        '--disable-scm-to-registry-transformation[Disable source control to registry transformation.]'
        '--use-registry-identity-for-scm[Look up source control dependencies in the registry and use their registry identity when possible to help deduplicate across the two origins.]'
        '--replace-scm-with-registry[Look up source control dependencies in the registry and use the registry to retrieve them instead of source control when possible.]'
        '--default-registry-url[Default registry URL to use, instead of the registries.json configuration file.]:default-registry-url:'
        '(--configuration -c)'{--configuration,-c}'[Build with configuration]:configuration:{__swift_complete "${___configuration[@]}"}'
        '*-Xcc[Pass flag through to all C compiler invocations.]:Xcc:'
        '*-Xswiftc[Pass flag through to all Swift compiler invocations.]:Xswiftc:'
        '*-Xlinker[Pass flag through to all linker invocations.]:Xlinker:'
        '*-Xcxx[Pass flag through to all C++ compiler invocations.]:Xcxx:'
        '--triple:triple:'
        '--sdk:sdk:_files -/'
        '--toolchain:toolchain:_files -/'
        '--swift-sdk[Filter for selecting a specific Swift SDK to build with.]:swift-sdk:'
        '*--sanitize[Turn on runtime checks for erroneous behavior.]:sanitize:{__swift_complete "${___sanitize[@]}"}'
        '--auto-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-index-store[Enable or disable indexing-while-building feature.]'
        '--disable-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-parseable-module-interfaces'
        '(--jobs -j)'{--jobs,-j}'[The number of jobs to spawn in parallel during the build process.]:jobs:'
        '--use-integrated-swift-driver'
        '--explicit-target-dependency-import-check[A flag that indicates this build should check whether targets only import their explicitly-declared dependencies.]:explicit-target-dependency-import-check:{__swift_complete "${___explicit_target_dependency_import_check[@]}"}'
        '--build-system[Specify the build system to use.]:build-system:{__swift_complete "${___build_system[@]}"}'
        '-debug-info-format[The Debug Information Format to use.]:debug-info-format:{__swift_complete "${__debug_info_format[@]}"}'
        '--enable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-local-rpath[Disable adding $ORIGIN/@loader_path to the rpath by default.]'
        '--traits[Enables the passed traits of the package. Multiple traits can be specified by providing a comma separated list e.g. `--traits Trait1,Trait2`. When enabling specific traits the defaults traits need to explictily enabled as well by passing `defaults` to this command.]:traits:'
        '--enable-all-traits[Enables all traits of the package.]'
        '--disable-default-traits[Disables all default traits of the package.]'
        '--exact[The exact package version to depend on.]:exact:'
        '--revision[The specific package revision to depend on.]:revision:'
        '--branch[The branch of the package to depend on.]:branch:'
        '--from[The package version to depend on (up to the next major version).]:from:'
        '--up-to-next-minor-from[The package version to depend on (up to the next minor version).]:up-to-next-minor-from:'
        '--to[Specify upper bound on the package version range (exclusive).]:to:'
        '--type[Specify dependency type.]:type:{__swift_complete "${___type[@]}"}'
        '--version[Show the version.]'
        '(-help -h --help)'{-help,-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_swift_package_add-product() {
    local -i ret=1
    local -ar ___configuration=('debug' 'release')
    local -ar ___sanitize=('address' 'thread' 'undefined' 'scudo' 'fuzzer')
    local -ar ___explicit_target_dependency_import_check=('none' 'warn' 'error')
    local -ar ___build_system=('native' 'swiftbuild' 'xcode')
    local -ar __debug_info_format=('dwarf' 'codeview' 'none')
    local -ar ___type=('executable' 'library' 'static-library' 'dynamic-library' 'plugin')
    local -ar arg_specs=(
        '--package-path[Specify the package path to operate on (default current directory). This changes the working directory before any other operation.]:package-path:_files -/'
        '--cache-path[Specify the shared cache directory path.]:cache-path:_files -/'
        '--config-path[Specify the shared configuration directory path.]:config-path:_files -/'
        '--security-path[Specify the shared security directory path.]:security-path:_files -/'
        '--scratch-path[Specify a custom scratch directory path. (default .build)]:scratch-path:_files -/'
        '--swift-sdks-path[Path to the directory containing installed Swift SDKs.]:swift-sdks-path:_files -/'
        '*--toolset[Specify a toolset JSON file to use when building for the target platform. Use the option multiple times to specify more than one toolset. Toolsets will be merged in the order they'\''re specified into a single final toolset for the current build.]:toolset:_files -g '\''*..json'\'''
        '*--pkg-config-path[Specify alternative path to search for pkg-config `.pc` files. Use the option multiple times to
        specify more than one path.]:pkg-config-path:_files -/'
        '--enable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--disable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--enable-build-manifest-caching'
        '--disable-build-manifest-caching'
        '--manifest-cache[Caching mode of Package.swift manifests. Valid values are\: (shared\: shared cache, local\: package'\''s build directory, none\: disabled)]:manifest-cache:'
        '--enable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '--disable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '(--verbose -v)'{--verbose,-v}'[Increase verbosity to include informational output.]'
        '(--very-verbose --vv)'{--very-verbose,--vv}'[Increase verbosity to include debug output.]'
        '(--quiet -q)'{--quiet,-q}'[Decrease verbosity to only include error output.]'
        '--color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--no-color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--disable-sandbox[Disable using the sandbox when executing subprocesses.]'
        '--netrc[Use netrc file even in cases where other credential stores are preferred.]'
        '--enable-netrc[Load credentials from a netrc file.]'
        '--disable-netrc[Load credentials from a netrc file.]'
        '--netrc-file[Specify the netrc file path.]:netrc-file:_files'
        '--enable-keychain[Search credentials in macOS keychain.]'
        '--disable-keychain[Search credentials in macOS keychain.]'
        '--resolver-fingerprint-checking:resolver-fingerprint-checking:'
        '--resolver-signing-entity-checking:resolver-signing-entity-checking:'
        '--enable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--disable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--enable-prefetching'
        '--disable-prefetching'
        '(--force-resolved-versions --disable-automatic-resolution --only-use-versions-from-resolved-file)'{--force-resolved-versions,--disable-automatic-resolution,--only-use-versions-from-resolved-file}'[Only use versions from the Package.resolved file and fail resolution if it is out-of-date.]'
        '--skip-update[Skip updating dependencies from their remote during a resolution.]'
        '--disable-scm-to-registry-transformation[Disable source control to registry transformation.]'
        '--use-registry-identity-for-scm[Look up source control dependencies in the registry and use their registry identity when possible to help deduplicate across the two origins.]'
        '--replace-scm-with-registry[Look up source control dependencies in the registry and use the registry to retrieve them instead of source control when possible.]'
        '--default-registry-url[Default registry URL to use, instead of the registries.json configuration file.]:default-registry-url:'
        '(--configuration -c)'{--configuration,-c}'[Build with configuration]:configuration:{__swift_complete "${___configuration[@]}"}'
        '*-Xcc[Pass flag through to all C compiler invocations.]:Xcc:'
        '*-Xswiftc[Pass flag through to all Swift compiler invocations.]:Xswiftc:'
        '*-Xlinker[Pass flag through to all linker invocations.]:Xlinker:'
        '*-Xcxx[Pass flag through to all C++ compiler invocations.]:Xcxx:'
        '--triple:triple:'
        '--sdk:sdk:_files -/'
        '--toolchain:toolchain:_files -/'
        '--swift-sdk[Filter for selecting a specific Swift SDK to build with.]:swift-sdk:'
        '*--sanitize[Turn on runtime checks for erroneous behavior.]:sanitize:{__swift_complete "${___sanitize[@]}"}'
        '--auto-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-index-store[Enable or disable indexing-while-building feature.]'
        '--disable-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-parseable-module-interfaces'
        '(--jobs -j)'{--jobs,-j}'[The number of jobs to spawn in parallel during the build process.]:jobs:'
        '--use-integrated-swift-driver'
        '--explicit-target-dependency-import-check[A flag that indicates this build should check whether targets only import their explicitly-declared dependencies.]:explicit-target-dependency-import-check:{__swift_complete "${___explicit_target_dependency_import_check[@]}"}'
        '--build-system[Specify the build system to use.]:build-system:{__swift_complete "${___build_system[@]}"}'
        '-debug-info-format[The Debug Information Format to use.]:debug-info-format:{__swift_complete "${__debug_info_format[@]}"}'
        '--enable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-local-rpath[Disable adding $ORIGIN/@loader_path to the rpath by default.]'
        '--traits[Enables the passed traits of the package. Multiple traits can be specified by providing a comma separated list e.g. `--traits Trait1,Trait2`. When enabling specific traits the defaults traits need to explictily enabled as well by passing `defaults` to this command.]:traits:'
        '--enable-all-traits[Enables all traits of the package.]'
        '--disable-default-traits[Disables all default traits of the package.]'
        ':name:'
        '--type[The type of target to add.]:type:{__swift_complete "${___type[@]}"}'
        '--targets[A list of targets that are part of this product.]:targets:'
        '--version[Show the version.]'
        '(-help -h --help)'{-help,-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_swift_package_add-target() {
    local -i ret=1
    local -ar ___configuration=('debug' 'release')
    local -ar ___sanitize=('address' 'thread' 'undefined' 'scudo' 'fuzzer')
    local -ar ___explicit_target_dependency_import_check=('none' 'warn' 'error')
    local -ar ___build_system=('native' 'swiftbuild' 'xcode')
    local -ar __debug_info_format=('dwarf' 'codeview' 'none')
    local -ar ___type=('library' 'executable' 'test' 'macro')
    local -ar arg_specs=(
        '--package-path[Specify the package path to operate on (default current directory). This changes the working directory before any other operation.]:package-path:_files -/'
        '--cache-path[Specify the shared cache directory path.]:cache-path:_files -/'
        '--config-path[Specify the shared configuration directory path.]:config-path:_files -/'
        '--security-path[Specify the shared security directory path.]:security-path:_files -/'
        '--scratch-path[Specify a custom scratch directory path. (default .build)]:scratch-path:_files -/'
        '--swift-sdks-path[Path to the directory containing installed Swift SDKs.]:swift-sdks-path:_files -/'
        '*--toolset[Specify a toolset JSON file to use when building for the target platform. Use the option multiple times to specify more than one toolset. Toolsets will be merged in the order they'\''re specified into a single final toolset for the current build.]:toolset:_files -g '\''*..json'\'''
        '*--pkg-config-path[Specify alternative path to search for pkg-config `.pc` files. Use the option multiple times to
        specify more than one path.]:pkg-config-path:_files -/'
        '--enable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--disable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--enable-build-manifest-caching'
        '--disable-build-manifest-caching'
        '--manifest-cache[Caching mode of Package.swift manifests. Valid values are\: (shared\: shared cache, local\: package'\''s build directory, none\: disabled)]:manifest-cache:'
        '--enable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '--disable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '(--verbose -v)'{--verbose,-v}'[Increase verbosity to include informational output.]'
        '(--very-verbose --vv)'{--very-verbose,--vv}'[Increase verbosity to include debug output.]'
        '(--quiet -q)'{--quiet,-q}'[Decrease verbosity to only include error output.]'
        '--color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--no-color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--disable-sandbox[Disable using the sandbox when executing subprocesses.]'
        '--netrc[Use netrc file even in cases where other credential stores are preferred.]'
        '--enable-netrc[Load credentials from a netrc file.]'
        '--disable-netrc[Load credentials from a netrc file.]'
        '--netrc-file[Specify the netrc file path.]:netrc-file:_files'
        '--enable-keychain[Search credentials in macOS keychain.]'
        '--disable-keychain[Search credentials in macOS keychain.]'
        '--resolver-fingerprint-checking:resolver-fingerprint-checking:'
        '--resolver-signing-entity-checking:resolver-signing-entity-checking:'
        '--enable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--disable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--enable-prefetching'
        '--disable-prefetching'
        '(--force-resolved-versions --disable-automatic-resolution --only-use-versions-from-resolved-file)'{--force-resolved-versions,--disable-automatic-resolution,--only-use-versions-from-resolved-file}'[Only use versions from the Package.resolved file and fail resolution if it is out-of-date.]'
        '--skip-update[Skip updating dependencies from their remote during a resolution.]'
        '--disable-scm-to-registry-transformation[Disable source control to registry transformation.]'
        '--use-registry-identity-for-scm[Look up source control dependencies in the registry and use their registry identity when possible to help deduplicate across the two origins.]'
        '--replace-scm-with-registry[Look up source control dependencies in the registry and use the registry to retrieve them instead of source control when possible.]'
        '--default-registry-url[Default registry URL to use, instead of the registries.json configuration file.]:default-registry-url:'
        '(--configuration -c)'{--configuration,-c}'[Build with configuration]:configuration:{__swift_complete "${___configuration[@]}"}'
        '*-Xcc[Pass flag through to all C compiler invocations.]:Xcc:'
        '*-Xswiftc[Pass flag through to all Swift compiler invocations.]:Xswiftc:'
        '*-Xlinker[Pass flag through to all linker invocations.]:Xlinker:'
        '*-Xcxx[Pass flag through to all C++ compiler invocations.]:Xcxx:'
        '--triple:triple:'
        '--sdk:sdk:_files -/'
        '--toolchain:toolchain:_files -/'
        '--swift-sdk[Filter for selecting a specific Swift SDK to build with.]:swift-sdk:'
        '*--sanitize[Turn on runtime checks for erroneous behavior.]:sanitize:{__swift_complete "${___sanitize[@]}"}'
        '--auto-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-index-store[Enable or disable indexing-while-building feature.]'
        '--disable-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-parseable-module-interfaces'
        '(--jobs -j)'{--jobs,-j}'[The number of jobs to spawn in parallel during the build process.]:jobs:'
        '--use-integrated-swift-driver'
        '--explicit-target-dependency-import-check[A flag that indicates this build should check whether targets only import their explicitly-declared dependencies.]:explicit-target-dependency-import-check:{__swift_complete "${___explicit_target_dependency_import_check[@]}"}'
        '--build-system[Specify the build system to use.]:build-system:{__swift_complete "${___build_system[@]}"}'
        '-debug-info-format[The Debug Information Format to use.]:debug-info-format:{__swift_complete "${__debug_info_format[@]}"}'
        '--enable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-local-rpath[Disable adding $ORIGIN/@loader_path to the rpath by default.]'
        '--traits[Enables the passed traits of the package. Multiple traits can be specified by providing a comma separated list e.g. `--traits Trait1,Trait2`. When enabling specific traits the defaults traits need to explictily enabled as well by passing `defaults` to this command.]:traits:'
        '--enable-all-traits[Enables all traits of the package.]'
        '--disable-default-traits[Disables all default traits of the package.]'
        ':name:'
        '--type[The type of target to add.]:type:{__swift_complete "${___type[@]}"}'
        '--dependencies[A list of target dependency names.]:dependencies:'
        '--url[The URL for a remote binary target.]:url:'
        '--path[The path to a local binary target.]:path:'
        '--checksum[The checksum for a remote binary target.]:checksum:'
        '--testing-library[The testing library to use when generating test targets, which can be one of '\''xctest'\'', '\''swift-testing'\'', or '\''none'\''.]:testing-library:'
        '--version[Show the version.]'
        '(-help -h --help)'{-help,-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_swift_package_add-target-dependency() {
    local -i ret=1
    local -ar ___configuration=('debug' 'release')
    local -ar ___sanitize=('address' 'thread' 'undefined' 'scudo' 'fuzzer')
    local -ar ___explicit_target_dependency_import_check=('none' 'warn' 'error')
    local -ar ___build_system=('native' 'swiftbuild' 'xcode')
    local -ar __debug_info_format=('dwarf' 'codeview' 'none')
    local -ar arg_specs=(
        '--package-path[Specify the package path to operate on (default current directory). This changes the working directory before any other operation.]:package-path:_files -/'
        '--cache-path[Specify the shared cache directory path.]:cache-path:_files -/'
        '--config-path[Specify the shared configuration directory path.]:config-path:_files -/'
        '--security-path[Specify the shared security directory path.]:security-path:_files -/'
        '--scratch-path[Specify a custom scratch directory path. (default .build)]:scratch-path:_files -/'
        '--swift-sdks-path[Path to the directory containing installed Swift SDKs.]:swift-sdks-path:_files -/'
        '*--toolset[Specify a toolset JSON file to use when building for the target platform. Use the option multiple times to specify more than one toolset. Toolsets will be merged in the order they'\''re specified into a single final toolset for the current build.]:toolset:_files -g '\''*..json'\'''
        '*--pkg-config-path[Specify alternative path to search for pkg-config `.pc` files. Use the option multiple times to
        specify more than one path.]:pkg-config-path:_files -/'
        '--enable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--disable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--enable-build-manifest-caching'
        '--disable-build-manifest-caching'
        '--manifest-cache[Caching mode of Package.swift manifests. Valid values are\: (shared\: shared cache, local\: package'\''s build directory, none\: disabled)]:manifest-cache:'
        '--enable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '--disable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '(--verbose -v)'{--verbose,-v}'[Increase verbosity to include informational output.]'
        '(--very-verbose --vv)'{--very-verbose,--vv}'[Increase verbosity to include debug output.]'
        '(--quiet -q)'{--quiet,-q}'[Decrease verbosity to only include error output.]'
        '--color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--no-color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--disable-sandbox[Disable using the sandbox when executing subprocesses.]'
        '--netrc[Use netrc file even in cases where other credential stores are preferred.]'
        '--enable-netrc[Load credentials from a netrc file.]'
        '--disable-netrc[Load credentials from a netrc file.]'
        '--netrc-file[Specify the netrc file path.]:netrc-file:_files'
        '--enable-keychain[Search credentials in macOS keychain.]'
        '--disable-keychain[Search credentials in macOS keychain.]'
        '--resolver-fingerprint-checking:resolver-fingerprint-checking:'
        '--resolver-signing-entity-checking:resolver-signing-entity-checking:'
        '--enable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--disable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--enable-prefetching'
        '--disable-prefetching'
        '(--force-resolved-versions --disable-automatic-resolution --only-use-versions-from-resolved-file)'{--force-resolved-versions,--disable-automatic-resolution,--only-use-versions-from-resolved-file}'[Only use versions from the Package.resolved file and fail resolution if it is out-of-date.]'
        '--skip-update[Skip updating dependencies from their remote during a resolution.]'
        '--disable-scm-to-registry-transformation[Disable source control to registry transformation.]'
        '--use-registry-identity-for-scm[Look up source control dependencies in the registry and use their registry identity when possible to help deduplicate across the two origins.]'
        '--replace-scm-with-registry[Look up source control dependencies in the registry and use the registry to retrieve them instead of source control when possible.]'
        '--default-registry-url[Default registry URL to use, instead of the registries.json configuration file.]:default-registry-url:'
        '(--configuration -c)'{--configuration,-c}'[Build with configuration]:configuration:{__swift_complete "${___configuration[@]}"}'
        '*-Xcc[Pass flag through to all C compiler invocations.]:Xcc:'
        '*-Xswiftc[Pass flag through to all Swift compiler invocations.]:Xswiftc:'
        '*-Xlinker[Pass flag through to all linker invocations.]:Xlinker:'
        '*-Xcxx[Pass flag through to all C++ compiler invocations.]:Xcxx:'
        '--triple:triple:'
        '--sdk:sdk:_files -/'
        '--toolchain:toolchain:_files -/'
        '--swift-sdk[Filter for selecting a specific Swift SDK to build with.]:swift-sdk:'
        '*--sanitize[Turn on runtime checks for erroneous behavior.]:sanitize:{__swift_complete "${___sanitize[@]}"}'
        '--auto-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-index-store[Enable or disable indexing-while-building feature.]'
        '--disable-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-parseable-module-interfaces'
        '(--jobs -j)'{--jobs,-j}'[The number of jobs to spawn in parallel during the build process.]:jobs:'
        '--use-integrated-swift-driver'
        '--explicit-target-dependency-import-check[A flag that indicates this build should check whether targets only import their explicitly-declared dependencies.]:explicit-target-dependency-import-check:{__swift_complete "${___explicit_target_dependency_import_check[@]}"}'
        '--build-system[Specify the build system to use.]:build-system:{__swift_complete "${___build_system[@]}"}'
        '-debug-info-format[The Debug Information Format to use.]:debug-info-format:{__swift_complete "${__debug_info_format[@]}"}'
        '--enable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-local-rpath[Disable adding $ORIGIN/@loader_path to the rpath by default.]'
        '--traits[Enables the passed traits of the package. Multiple traits can be specified by providing a comma separated list e.g. `--traits Trait1,Trait2`. When enabling specific traits the defaults traits need to explictily enabled as well by passing `defaults` to this command.]:traits:'
        '--enable-all-traits[Enables all traits of the package.]'
        '--disable-default-traits[Disables all default traits of the package.]'
        ':dependency-name:'
        ':target-name:'
        '--package[The package in which the dependency resides.]:package:'
        '--version[Show the version.]'
        '(-help -h --help)'{-help,-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_swift_package_add-setting() {
    local -i ret=1
    local -ar ___configuration=('debug' 'release')
    local -ar ___sanitize=('address' 'thread' 'undefined' 'scudo' 'fuzzer')
    local -ar ___explicit_target_dependency_import_check=('none' 'warn' 'error')
    local -ar ___build_system=('native' 'swiftbuild' 'xcode')
    local -ar __debug_info_format=('dwarf' 'codeview' 'none')
    local -ar arg_specs=(
        '--package-path[Specify the package path to operate on (default current directory). This changes the working directory before any other operation.]:package-path:_files -/'
        '--cache-path[Specify the shared cache directory path.]:cache-path:_files -/'
        '--config-path[Specify the shared configuration directory path.]:config-path:_files -/'
        '--security-path[Specify the shared security directory path.]:security-path:_files -/'
        '--scratch-path[Specify a custom scratch directory path. (default .build)]:scratch-path:_files -/'
        '--swift-sdks-path[Path to the directory containing installed Swift SDKs.]:swift-sdks-path:_files -/'
        '*--toolset[Specify a toolset JSON file to use when building for the target platform. Use the option multiple times to specify more than one toolset. Toolsets will be merged in the order they'\''re specified into a single final toolset for the current build.]:toolset:_files -g '\''*..json'\'''
        '*--pkg-config-path[Specify alternative path to search for pkg-config `.pc` files. Use the option multiple times to
        specify more than one path.]:pkg-config-path:_files -/'
        '--enable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--disable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--enable-build-manifest-caching'
        '--disable-build-manifest-caching'
        '--manifest-cache[Caching mode of Package.swift manifests. Valid values are\: (shared\: shared cache, local\: package'\''s build directory, none\: disabled)]:manifest-cache:'
        '--enable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '--disable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '(--verbose -v)'{--verbose,-v}'[Increase verbosity to include informational output.]'
        '(--very-verbose --vv)'{--very-verbose,--vv}'[Increase verbosity to include debug output.]'
        '(--quiet -q)'{--quiet,-q}'[Decrease verbosity to only include error output.]'
        '--color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--no-color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--disable-sandbox[Disable using the sandbox when executing subprocesses.]'
        '--netrc[Use netrc file even in cases where other credential stores are preferred.]'
        '--enable-netrc[Load credentials from a netrc file.]'
        '--disable-netrc[Load credentials from a netrc file.]'
        '--netrc-file[Specify the netrc file path.]:netrc-file:_files'
        '--enable-keychain[Search credentials in macOS keychain.]'
        '--disable-keychain[Search credentials in macOS keychain.]'
        '--resolver-fingerprint-checking:resolver-fingerprint-checking:'
        '--resolver-signing-entity-checking:resolver-signing-entity-checking:'
        '--enable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--disable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--enable-prefetching'
        '--disable-prefetching'
        '(--force-resolved-versions --disable-automatic-resolution --only-use-versions-from-resolved-file)'{--force-resolved-versions,--disable-automatic-resolution,--only-use-versions-from-resolved-file}'[Only use versions from the Package.resolved file and fail resolution if it is out-of-date.]'
        '--skip-update[Skip updating dependencies from their remote during a resolution.]'
        '--disable-scm-to-registry-transformation[Disable source control to registry transformation.]'
        '--use-registry-identity-for-scm[Look up source control dependencies in the registry and use their registry identity when possible to help deduplicate across the two origins.]'
        '--replace-scm-with-registry[Look up source control dependencies in the registry and use the registry to retrieve them instead of source control when possible.]'
        '--default-registry-url[Default registry URL to use, instead of the registries.json configuration file.]:default-registry-url:'
        '(--configuration -c)'{--configuration,-c}'[Build with configuration]:configuration:{__swift_complete "${___configuration[@]}"}'
        '*-Xcc[Pass flag through to all C compiler invocations.]:Xcc:'
        '*-Xswiftc[Pass flag through to all Swift compiler invocations.]:Xswiftc:'
        '*-Xlinker[Pass flag through to all linker invocations.]:Xlinker:'
        '*-Xcxx[Pass flag through to all C++ compiler invocations.]:Xcxx:'
        '--triple:triple:'
        '--sdk:sdk:_files -/'
        '--toolchain:toolchain:_files -/'
        '--swift-sdk[Filter for selecting a specific Swift SDK to build with.]:swift-sdk:'
        '*--sanitize[Turn on runtime checks for erroneous behavior.]:sanitize:{__swift_complete "${___sanitize[@]}"}'
        '--auto-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-index-store[Enable or disable indexing-while-building feature.]'
        '--disable-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-parseable-module-interfaces'
        '(--jobs -j)'{--jobs,-j}'[The number of jobs to spawn in parallel during the build process.]:jobs:'
        '--use-integrated-swift-driver'
        '--explicit-target-dependency-import-check[A flag that indicates this build should check whether targets only import their explicitly-declared dependencies.]:explicit-target-dependency-import-check:{__swift_complete "${___explicit_target_dependency_import_check[@]}"}'
        '--build-system[Specify the build system to use.]:build-system:{__swift_complete "${___build_system[@]}"}'
        '-debug-info-format[The Debug Information Format to use.]:debug-info-format:{__swift_complete "${__debug_info_format[@]}"}'
        '--enable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-local-rpath[Disable adding $ORIGIN/@loader_path to the rpath by default.]'
        '--traits[Enables the passed traits of the package. Multiple traits can be specified by providing a comma separated list e.g. `--traits Trait1,Trait2`. When enabling specific traits the defaults traits need to explictily enabled as well by passing `defaults` to this command.]:traits:'
        '--enable-all-traits[Enables all traits of the package.]'
        '--disable-default-traits[Disables all default traits of the package.]'
        '--target[The target to add the setting to.]:target:'
        '--swift[The Swift language setting(s) to add. Supported settings\: experimentalFeature, upcomingFeature, languageMode, StrictMemorySafety.]:swift:'
        '--version[Show the version.]'
        '(-help -h --help)'{-help,-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_swift_package_experimental-audit-binary-artifact() {
    local -i ret=1
    local -ar ___configuration=('debug' 'release')
    local -ar ___sanitize=('address' 'thread' 'undefined' 'scudo' 'fuzzer')
    local -ar ___explicit_target_dependency_import_check=('none' 'warn' 'error')
    local -ar ___build_system=('native' 'swiftbuild' 'xcode')
    local -ar __debug_info_format=('dwarf' 'codeview' 'none')
    local -ar arg_specs=(
        '--package-path[Specify the package path to operate on (default current directory). This changes the working directory before any other operation.]:package-path:_files -/'
        '--cache-path[Specify the shared cache directory path.]:cache-path:_files -/'
        '--config-path[Specify the shared configuration directory path.]:config-path:_files -/'
        '--security-path[Specify the shared security directory path.]:security-path:_files -/'
        '--scratch-path[Specify a custom scratch directory path. (default .build)]:scratch-path:_files -/'
        '--swift-sdks-path[Path to the directory containing installed Swift SDKs.]:swift-sdks-path:_files -/'
        '*--toolset[Specify a toolset JSON file to use when building for the target platform. Use the option multiple times to specify more than one toolset. Toolsets will be merged in the order they'\''re specified into a single final toolset for the current build.]:toolset:_files -g '\''*..json'\'''
        '*--pkg-config-path[Specify alternative path to search for pkg-config `.pc` files. Use the option multiple times to
        specify more than one path.]:pkg-config-path:_files -/'
        '--enable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--disable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--enable-build-manifest-caching'
        '--disable-build-manifest-caching'
        '--manifest-cache[Caching mode of Package.swift manifests. Valid values are\: (shared\: shared cache, local\: package'\''s build directory, none\: disabled)]:manifest-cache:'
        '--enable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '--disable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '(--verbose -v)'{--verbose,-v}'[Increase verbosity to include informational output.]'
        '(--very-verbose --vv)'{--very-verbose,--vv}'[Increase verbosity to include debug output.]'
        '(--quiet -q)'{--quiet,-q}'[Decrease verbosity to only include error output.]'
        '--color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--no-color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--disable-sandbox[Disable using the sandbox when executing subprocesses.]'
        '--netrc[Use netrc file even in cases where other credential stores are preferred.]'
        '--enable-netrc[Load credentials from a netrc file.]'
        '--disable-netrc[Load credentials from a netrc file.]'
        '--netrc-file[Specify the netrc file path.]:netrc-file:_files'
        '--enable-keychain[Search credentials in macOS keychain.]'
        '--disable-keychain[Search credentials in macOS keychain.]'
        '--resolver-fingerprint-checking:resolver-fingerprint-checking:'
        '--resolver-signing-entity-checking:resolver-signing-entity-checking:'
        '--enable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--disable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--enable-prefetching'
        '--disable-prefetching'
        '(--force-resolved-versions --disable-automatic-resolution --only-use-versions-from-resolved-file)'{--force-resolved-versions,--disable-automatic-resolution,--only-use-versions-from-resolved-file}'[Only use versions from the Package.resolved file and fail resolution if it is out-of-date.]'
        '--skip-update[Skip updating dependencies from their remote during a resolution.]'
        '--disable-scm-to-registry-transformation[Disable source control to registry transformation.]'
        '--use-registry-identity-for-scm[Look up source control dependencies in the registry and use their registry identity when possible to help deduplicate across the two origins.]'
        '--replace-scm-with-registry[Look up source control dependencies in the registry and use the registry to retrieve them instead of source control when possible.]'
        '--default-registry-url[Default registry URL to use, instead of the registries.json configuration file.]:default-registry-url:'
        '(--configuration -c)'{--configuration,-c}'[Build with configuration]:configuration:{__swift_complete "${___configuration[@]}"}'
        '*-Xcc[Pass flag through to all C compiler invocations.]:Xcc:'
        '*-Xswiftc[Pass flag through to all Swift compiler invocations.]:Xswiftc:'
        '*-Xlinker[Pass flag through to all linker invocations.]:Xlinker:'
        '*-Xcxx[Pass flag through to all C++ compiler invocations.]:Xcxx:'
        '--triple:triple:'
        '--sdk:sdk:_files -/'
        '--toolchain:toolchain:_files -/'
        '--swift-sdk[Filter for selecting a specific Swift SDK to build with.]:swift-sdk:'
        '*--sanitize[Turn on runtime checks for erroneous behavior.]:sanitize:{__swift_complete "${___sanitize[@]}"}'
        '--auto-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-index-store[Enable or disable indexing-while-building feature.]'
        '--disable-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-parseable-module-interfaces'
        '(--jobs -j)'{--jobs,-j}'[The number of jobs to spawn in parallel during the build process.]:jobs:'
        '--use-integrated-swift-driver'
        '--explicit-target-dependency-import-check[A flag that indicates this build should check whether targets only import their explicitly-declared dependencies.]:explicit-target-dependency-import-check:{__swift_complete "${___explicit_target_dependency_import_check[@]}"}'
        '--build-system[Specify the build system to use.]:build-system:{__swift_complete "${___build_system[@]}"}'
        '-debug-info-format[The Debug Information Format to use.]:debug-info-format:{__swift_complete "${__debug_info_format[@]}"}'
        '--enable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-local-rpath[Disable adding $ORIGIN/@loader_path to the rpath by default.]'
        '--traits[Enables the passed traits of the package. Multiple traits can be specified by providing a comma separated list e.g. `--traits Trait1,Trait2`. When enabling specific traits the defaults traits need to explictily enabled as well by passing `defaults` to this command.]:traits:'
        '--enable-all-traits[Enables all traits of the package.]'
        '--disable-default-traits[Disables all default traits of the package.]'
        ':path:_files -/'
        '--version[Show the version.]'
        '(-help -h --help)'{-help,-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_swift_package_clean() {
    local -i ret=1
    local -ar ___configuration=('debug' 'release')
    local -ar ___sanitize=('address' 'thread' 'undefined' 'scudo' 'fuzzer')
    local -ar ___explicit_target_dependency_import_check=('none' 'warn' 'error')
    local -ar ___build_system=('native' 'swiftbuild' 'xcode')
    local -ar __debug_info_format=('dwarf' 'codeview' 'none')
    local -ar arg_specs=(
        '--package-path[Specify the package path to operate on (default current directory). This changes the working directory before any other operation.]:package-path:_files -/'
        '--cache-path[Specify the shared cache directory path.]:cache-path:_files -/'
        '--config-path[Specify the shared configuration directory path.]:config-path:_files -/'
        '--security-path[Specify the shared security directory path.]:security-path:_files -/'
        '--scratch-path[Specify a custom scratch directory path. (default .build)]:scratch-path:_files -/'
        '--swift-sdks-path[Path to the directory containing installed Swift SDKs.]:swift-sdks-path:_files -/'
        '*--toolset[Specify a toolset JSON file to use when building for the target platform. Use the option multiple times to specify more than one toolset. Toolsets will be merged in the order they'\''re specified into a single final toolset for the current build.]:toolset:_files -g '\''*..json'\'''
        '*--pkg-config-path[Specify alternative path to search for pkg-config `.pc` files. Use the option multiple times to
        specify more than one path.]:pkg-config-path:_files -/'
        '--enable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--disable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--enable-build-manifest-caching'
        '--disable-build-manifest-caching'
        '--manifest-cache[Caching mode of Package.swift manifests. Valid values are\: (shared\: shared cache, local\: package'\''s build directory, none\: disabled)]:manifest-cache:'
        '--enable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '--disable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '(--verbose -v)'{--verbose,-v}'[Increase verbosity to include informational output.]'
        '(--very-verbose --vv)'{--very-verbose,--vv}'[Increase verbosity to include debug output.]'
        '(--quiet -q)'{--quiet,-q}'[Decrease verbosity to only include error output.]'
        '--color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--no-color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--disable-sandbox[Disable using the sandbox when executing subprocesses.]'
        '--netrc[Use netrc file even in cases where other credential stores are preferred.]'
        '--enable-netrc[Load credentials from a netrc file.]'
        '--disable-netrc[Load credentials from a netrc file.]'
        '--netrc-file[Specify the netrc file path.]:netrc-file:_files'
        '--enable-keychain[Search credentials in macOS keychain.]'
        '--disable-keychain[Search credentials in macOS keychain.]'
        '--resolver-fingerprint-checking:resolver-fingerprint-checking:'
        '--resolver-signing-entity-checking:resolver-signing-entity-checking:'
        '--enable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--disable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--enable-prefetching'
        '--disable-prefetching'
        '(--force-resolved-versions --disable-automatic-resolution --only-use-versions-from-resolved-file)'{--force-resolved-versions,--disable-automatic-resolution,--only-use-versions-from-resolved-file}'[Only use versions from the Package.resolved file and fail resolution if it is out-of-date.]'
        '--skip-update[Skip updating dependencies from their remote during a resolution.]'
        '--disable-scm-to-registry-transformation[Disable source control to registry transformation.]'
        '--use-registry-identity-for-scm[Look up source control dependencies in the registry and use their registry identity when possible to help deduplicate across the two origins.]'
        '--replace-scm-with-registry[Look up source control dependencies in the registry and use the registry to retrieve them instead of source control when possible.]'
        '--default-registry-url[Default registry URL to use, instead of the registries.json configuration file.]:default-registry-url:'
        '(--configuration -c)'{--configuration,-c}'[Build with configuration]:configuration:{__swift_complete "${___configuration[@]}"}'
        '*-Xcc[Pass flag through to all C compiler invocations.]:Xcc:'
        '*-Xswiftc[Pass flag through to all Swift compiler invocations.]:Xswiftc:'
        '*-Xlinker[Pass flag through to all linker invocations.]:Xlinker:'
        '*-Xcxx[Pass flag through to all C++ compiler invocations.]:Xcxx:'
        '--triple:triple:'
        '--sdk:sdk:_files -/'
        '--toolchain:toolchain:_files -/'
        '--swift-sdk[Filter for selecting a specific Swift SDK to build with.]:swift-sdk:'
        '*--sanitize[Turn on runtime checks for erroneous behavior.]:sanitize:{__swift_complete "${___sanitize[@]}"}'
        '--auto-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-index-store[Enable or disable indexing-while-building feature.]'
        '--disable-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-parseable-module-interfaces'
        '(--jobs -j)'{--jobs,-j}'[The number of jobs to spawn in parallel during the build process.]:jobs:'
        '--use-integrated-swift-driver'
        '--explicit-target-dependency-import-check[A flag that indicates this build should check whether targets only import their explicitly-declared dependencies.]:explicit-target-dependency-import-check:{__swift_complete "${___explicit_target_dependency_import_check[@]}"}'
        '--build-system[Specify the build system to use.]:build-system:{__swift_complete "${___build_system[@]}"}'
        '-debug-info-format[The Debug Information Format to use.]:debug-info-format:{__swift_complete "${__debug_info_format[@]}"}'
        '--enable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-local-rpath[Disable adding $ORIGIN/@loader_path to the rpath by default.]'
        '--traits[Enables the passed traits of the package. Multiple traits can be specified by providing a comma separated list e.g. `--traits Trait1,Trait2`. When enabling specific traits the defaults traits need to explictily enabled as well by passing `defaults` to this command.]:traits:'
        '--enable-all-traits[Enables all traits of the package.]'
        '--disable-default-traits[Disables all default traits of the package.]'
        '--version[Show the version.]'
        '(-help -h --help)'{-help,-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_swift_package_purge-cache() {
    local -i ret=1
    local -ar ___configuration=('debug' 'release')
    local -ar ___sanitize=('address' 'thread' 'undefined' 'scudo' 'fuzzer')
    local -ar ___explicit_target_dependency_import_check=('none' 'warn' 'error')
    local -ar ___build_system=('native' 'swiftbuild' 'xcode')
    local -ar __debug_info_format=('dwarf' 'codeview' 'none')
    local -ar arg_specs=(
        '--package-path[Specify the package path to operate on (default current directory). This changes the working directory before any other operation.]:package-path:_files -/'
        '--cache-path[Specify the shared cache directory path.]:cache-path:_files -/'
        '--config-path[Specify the shared configuration directory path.]:config-path:_files -/'
        '--security-path[Specify the shared security directory path.]:security-path:_files -/'
        '--scratch-path[Specify a custom scratch directory path. (default .build)]:scratch-path:_files -/'
        '--swift-sdks-path[Path to the directory containing installed Swift SDKs.]:swift-sdks-path:_files -/'
        '*--toolset[Specify a toolset JSON file to use when building for the target platform. Use the option multiple times to specify more than one toolset. Toolsets will be merged in the order they'\''re specified into a single final toolset for the current build.]:toolset:_files -g '\''*..json'\'''
        '*--pkg-config-path[Specify alternative path to search for pkg-config `.pc` files. Use the option multiple times to
        specify more than one path.]:pkg-config-path:_files -/'
        '--enable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--disable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--enable-build-manifest-caching'
        '--disable-build-manifest-caching'
        '--manifest-cache[Caching mode of Package.swift manifests. Valid values are\: (shared\: shared cache, local\: package'\''s build directory, none\: disabled)]:manifest-cache:'
        '--enable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '--disable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '(--verbose -v)'{--verbose,-v}'[Increase verbosity to include informational output.]'
        '(--very-verbose --vv)'{--very-verbose,--vv}'[Increase verbosity to include debug output.]'
        '(--quiet -q)'{--quiet,-q}'[Decrease verbosity to only include error output.]'
        '--color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--no-color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--disable-sandbox[Disable using the sandbox when executing subprocesses.]'
        '--netrc[Use netrc file even in cases where other credential stores are preferred.]'
        '--enable-netrc[Load credentials from a netrc file.]'
        '--disable-netrc[Load credentials from a netrc file.]'
        '--netrc-file[Specify the netrc file path.]:netrc-file:_files'
        '--enable-keychain[Search credentials in macOS keychain.]'
        '--disable-keychain[Search credentials in macOS keychain.]'
        '--resolver-fingerprint-checking:resolver-fingerprint-checking:'
        '--resolver-signing-entity-checking:resolver-signing-entity-checking:'
        '--enable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--disable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--enable-prefetching'
        '--disable-prefetching'
        '(--force-resolved-versions --disable-automatic-resolution --only-use-versions-from-resolved-file)'{--force-resolved-versions,--disable-automatic-resolution,--only-use-versions-from-resolved-file}'[Only use versions from the Package.resolved file and fail resolution if it is out-of-date.]'
        '--skip-update[Skip updating dependencies from their remote during a resolution.]'
        '--disable-scm-to-registry-transformation[Disable source control to registry transformation.]'
        '--use-registry-identity-for-scm[Look up source control dependencies in the registry and use their registry identity when possible to help deduplicate across the two origins.]'
        '--replace-scm-with-registry[Look up source control dependencies in the registry and use the registry to retrieve them instead of source control when possible.]'
        '--default-registry-url[Default registry URL to use, instead of the registries.json configuration file.]:default-registry-url:'
        '(--configuration -c)'{--configuration,-c}'[Build with configuration]:configuration:{__swift_complete "${___configuration[@]}"}'
        '*-Xcc[Pass flag through to all C compiler invocations.]:Xcc:'
        '*-Xswiftc[Pass flag through to all Swift compiler invocations.]:Xswiftc:'
        '*-Xlinker[Pass flag through to all linker invocations.]:Xlinker:'
        '*-Xcxx[Pass flag through to all C++ compiler invocations.]:Xcxx:'
        '--triple:triple:'
        '--sdk:sdk:_files -/'
        '--toolchain:toolchain:_files -/'
        '--swift-sdk[Filter for selecting a specific Swift SDK to build with.]:swift-sdk:'
        '*--sanitize[Turn on runtime checks for erroneous behavior.]:sanitize:{__swift_complete "${___sanitize[@]}"}'
        '--auto-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-index-store[Enable or disable indexing-while-building feature.]'
        '--disable-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-parseable-module-interfaces'
        '(--jobs -j)'{--jobs,-j}'[The number of jobs to spawn in parallel during the build process.]:jobs:'
        '--use-integrated-swift-driver'
        '--explicit-target-dependency-import-check[A flag that indicates this build should check whether targets only import their explicitly-declared dependencies.]:explicit-target-dependency-import-check:{__swift_complete "${___explicit_target_dependency_import_check[@]}"}'
        '--build-system[Specify the build system to use.]:build-system:{__swift_complete "${___build_system[@]}"}'
        '-debug-info-format[The Debug Information Format to use.]:debug-info-format:{__swift_complete "${__debug_info_format[@]}"}'
        '--enable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-local-rpath[Disable adding $ORIGIN/@loader_path to the rpath by default.]'
        '--traits[Enables the passed traits of the package. Multiple traits can be specified by providing a comma separated list e.g. `--traits Trait1,Trait2`. When enabling specific traits the defaults traits need to explictily enabled as well by passing `defaults` to this command.]:traits:'
        '--enable-all-traits[Enables all traits of the package.]'
        '--disable-default-traits[Disables all default traits of the package.]'
        '--version[Show the version.]'
        '(-help -h --help)'{-help,-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_swift_package_reset() {
    local -i ret=1
    local -ar ___configuration=('debug' 'release')
    local -ar ___sanitize=('address' 'thread' 'undefined' 'scudo' 'fuzzer')
    local -ar ___explicit_target_dependency_import_check=('none' 'warn' 'error')
    local -ar ___build_system=('native' 'swiftbuild' 'xcode')
    local -ar __debug_info_format=('dwarf' 'codeview' 'none')
    local -ar arg_specs=(
        '--package-path[Specify the package path to operate on (default current directory). This changes the working directory before any other operation.]:package-path:_files -/'
        '--cache-path[Specify the shared cache directory path.]:cache-path:_files -/'
        '--config-path[Specify the shared configuration directory path.]:config-path:_files -/'
        '--security-path[Specify the shared security directory path.]:security-path:_files -/'
        '--scratch-path[Specify a custom scratch directory path. (default .build)]:scratch-path:_files -/'
        '--swift-sdks-path[Path to the directory containing installed Swift SDKs.]:swift-sdks-path:_files -/'
        '*--toolset[Specify a toolset JSON file to use when building for the target platform. Use the option multiple times to specify more than one toolset. Toolsets will be merged in the order they'\''re specified into a single final toolset for the current build.]:toolset:_files -g '\''*..json'\'''
        '*--pkg-config-path[Specify alternative path to search for pkg-config `.pc` files. Use the option multiple times to
        specify more than one path.]:pkg-config-path:_files -/'
        '--enable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--disable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--enable-build-manifest-caching'
        '--disable-build-manifest-caching'
        '--manifest-cache[Caching mode of Package.swift manifests. Valid values are\: (shared\: shared cache, local\: package'\''s build directory, none\: disabled)]:manifest-cache:'
        '--enable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '--disable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '(--verbose -v)'{--verbose,-v}'[Increase verbosity to include informational output.]'
        '(--very-verbose --vv)'{--very-verbose,--vv}'[Increase verbosity to include debug output.]'
        '(--quiet -q)'{--quiet,-q}'[Decrease verbosity to only include error output.]'
        '--color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--no-color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--disable-sandbox[Disable using the sandbox when executing subprocesses.]'
        '--netrc[Use netrc file even in cases where other credential stores are preferred.]'
        '--enable-netrc[Load credentials from a netrc file.]'
        '--disable-netrc[Load credentials from a netrc file.]'
        '--netrc-file[Specify the netrc file path.]:netrc-file:_files'
        '--enable-keychain[Search credentials in macOS keychain.]'
        '--disable-keychain[Search credentials in macOS keychain.]'
        '--resolver-fingerprint-checking:resolver-fingerprint-checking:'
        '--resolver-signing-entity-checking:resolver-signing-entity-checking:'
        '--enable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--disable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--enable-prefetching'
        '--disable-prefetching'
        '(--force-resolved-versions --disable-automatic-resolution --only-use-versions-from-resolved-file)'{--force-resolved-versions,--disable-automatic-resolution,--only-use-versions-from-resolved-file}'[Only use versions from the Package.resolved file and fail resolution if it is out-of-date.]'
        '--skip-update[Skip updating dependencies from their remote during a resolution.]'
        '--disable-scm-to-registry-transformation[Disable source control to registry transformation.]'
        '--use-registry-identity-for-scm[Look up source control dependencies in the registry and use their registry identity when possible to help deduplicate across the two origins.]'
        '--replace-scm-with-registry[Look up source control dependencies in the registry and use the registry to retrieve them instead of source control when possible.]'
        '--default-registry-url[Default registry URL to use, instead of the registries.json configuration file.]:default-registry-url:'
        '(--configuration -c)'{--configuration,-c}'[Build with configuration]:configuration:{__swift_complete "${___configuration[@]}"}'
        '*-Xcc[Pass flag through to all C compiler invocations.]:Xcc:'
        '*-Xswiftc[Pass flag through to all Swift compiler invocations.]:Xswiftc:'
        '*-Xlinker[Pass flag through to all linker invocations.]:Xlinker:'
        '*-Xcxx[Pass flag through to all C++ compiler invocations.]:Xcxx:'
        '--triple:triple:'
        '--sdk:sdk:_files -/'
        '--toolchain:toolchain:_files -/'
        '--swift-sdk[Filter for selecting a specific Swift SDK to build with.]:swift-sdk:'
        '*--sanitize[Turn on runtime checks for erroneous behavior.]:sanitize:{__swift_complete "${___sanitize[@]}"}'
        '--auto-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-index-store[Enable or disable indexing-while-building feature.]'
        '--disable-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-parseable-module-interfaces'
        '(--jobs -j)'{--jobs,-j}'[The number of jobs to spawn in parallel during the build process.]:jobs:'
        '--use-integrated-swift-driver'
        '--explicit-target-dependency-import-check[A flag that indicates this build should check whether targets only import their explicitly-declared dependencies.]:explicit-target-dependency-import-check:{__swift_complete "${___explicit_target_dependency_import_check[@]}"}'
        '--build-system[Specify the build system to use.]:build-system:{__swift_complete "${___build_system[@]}"}'
        '-debug-info-format[The Debug Information Format to use.]:debug-info-format:{__swift_complete "${__debug_info_format[@]}"}'
        '--enable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-local-rpath[Disable adding $ORIGIN/@loader_path to the rpath by default.]'
        '--traits[Enables the passed traits of the package. Multiple traits can be specified by providing a comma separated list e.g. `--traits Trait1,Trait2`. When enabling specific traits the defaults traits need to explictily enabled as well by passing `defaults` to this command.]:traits:'
        '--enable-all-traits[Enables all traits of the package.]'
        '--disable-default-traits[Disables all default traits of the package.]'
        '--version[Show the version.]'
        '(-help -h --help)'{-help,-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_swift_package_update() {
    local -i ret=1
    local -ar ___configuration=('debug' 'release')
    local -ar ___sanitize=('address' 'thread' 'undefined' 'scudo' 'fuzzer')
    local -ar ___explicit_target_dependency_import_check=('none' 'warn' 'error')
    local -ar ___build_system=('native' 'swiftbuild' 'xcode')
    local -ar __debug_info_format=('dwarf' 'codeview' 'none')
    local -ar arg_specs=(
        '--package-path[Specify the package path to operate on (default current directory). This changes the working directory before any other operation.]:package-path:_files -/'
        '--cache-path[Specify the shared cache directory path.]:cache-path:_files -/'
        '--config-path[Specify the shared configuration directory path.]:config-path:_files -/'
        '--security-path[Specify the shared security directory path.]:security-path:_files -/'
        '--scratch-path[Specify a custom scratch directory path. (default .build)]:scratch-path:_files -/'
        '--swift-sdks-path[Path to the directory containing installed Swift SDKs.]:swift-sdks-path:_files -/'
        '*--toolset[Specify a toolset JSON file to use when building for the target platform. Use the option multiple times to specify more than one toolset. Toolsets will be merged in the order they'\''re specified into a single final toolset for the current build.]:toolset:_files -g '\''*..json'\'''
        '*--pkg-config-path[Specify alternative path to search for pkg-config `.pc` files. Use the option multiple times to
        specify more than one path.]:pkg-config-path:_files -/'
        '--enable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--disable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--enable-build-manifest-caching'
        '--disable-build-manifest-caching'
        '--manifest-cache[Caching mode of Package.swift manifests. Valid values are\: (shared\: shared cache, local\: package'\''s build directory, none\: disabled)]:manifest-cache:'
        '--enable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '--disable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '(--verbose -v)'{--verbose,-v}'[Increase verbosity to include informational output.]'
        '(--very-verbose --vv)'{--very-verbose,--vv}'[Increase verbosity to include debug output.]'
        '(--quiet -q)'{--quiet,-q}'[Decrease verbosity to only include error output.]'
        '--color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--no-color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--disable-sandbox[Disable using the sandbox when executing subprocesses.]'
        '--netrc[Use netrc file even in cases where other credential stores are preferred.]'
        '--enable-netrc[Load credentials from a netrc file.]'
        '--disable-netrc[Load credentials from a netrc file.]'
        '--netrc-file[Specify the netrc file path.]:netrc-file:_files'
        '--enable-keychain[Search credentials in macOS keychain.]'
        '--disable-keychain[Search credentials in macOS keychain.]'
        '--resolver-fingerprint-checking:resolver-fingerprint-checking:'
        '--resolver-signing-entity-checking:resolver-signing-entity-checking:'
        '--enable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--disable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--enable-prefetching'
        '--disable-prefetching'
        '(--force-resolved-versions --disable-automatic-resolution --only-use-versions-from-resolved-file)'{--force-resolved-versions,--disable-automatic-resolution,--only-use-versions-from-resolved-file}'[Only use versions from the Package.resolved file and fail resolution if it is out-of-date.]'
        '--skip-update[Skip updating dependencies from their remote during a resolution.]'
        '--disable-scm-to-registry-transformation[Disable source control to registry transformation.]'
        '--use-registry-identity-for-scm[Look up source control dependencies in the registry and use their registry identity when possible to help deduplicate across the two origins.]'
        '--replace-scm-with-registry[Look up source control dependencies in the registry and use the registry to retrieve them instead of source control when possible.]'
        '--default-registry-url[Default registry URL to use, instead of the registries.json configuration file.]:default-registry-url:'
        '(--configuration -c)'{--configuration,-c}'[Build with configuration]:configuration:{__swift_complete "${___configuration[@]}"}'
        '*-Xcc[Pass flag through to all C compiler invocations.]:Xcc:'
        '*-Xswiftc[Pass flag through to all Swift compiler invocations.]:Xswiftc:'
        '*-Xlinker[Pass flag through to all linker invocations.]:Xlinker:'
        '*-Xcxx[Pass flag through to all C++ compiler invocations.]:Xcxx:'
        '--triple:triple:'
        '--sdk:sdk:_files -/'
        '--toolchain:toolchain:_files -/'
        '--swift-sdk[Filter for selecting a specific Swift SDK to build with.]:swift-sdk:'
        '*--sanitize[Turn on runtime checks for erroneous behavior.]:sanitize:{__swift_complete "${___sanitize[@]}"}'
        '--auto-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-index-store[Enable or disable indexing-while-building feature.]'
        '--disable-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-parseable-module-interfaces'
        '(--jobs -j)'{--jobs,-j}'[The number of jobs to spawn in parallel during the build process.]:jobs:'
        '--use-integrated-swift-driver'
        '--explicit-target-dependency-import-check[A flag that indicates this build should check whether targets only import their explicitly-declared dependencies.]:explicit-target-dependency-import-check:{__swift_complete "${___explicit_target_dependency_import_check[@]}"}'
        '--build-system[Specify the build system to use.]:build-system:{__swift_complete "${___build_system[@]}"}'
        '-debug-info-format[The Debug Information Format to use.]:debug-info-format:{__swift_complete "${__debug_info_format[@]}"}'
        '--enable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-local-rpath[Disable adding $ORIGIN/@loader_path to the rpath by default.]'
        '--traits[Enables the passed traits of the package. Multiple traits can be specified by providing a comma separated list e.g. `--traits Trait1,Trait2`. When enabling specific traits the defaults traits need to explictily enabled as well by passing `defaults` to this command.]:traits:'
        '--enable-all-traits[Enables all traits of the package.]'
        '--disable-default-traits[Disables all default traits of the package.]'
        '(--dry-run -n)'{--dry-run,-n}'[Display the list of dependencies that can be updated.]'
        '*:packages:'
        '--version[Show the version.]'
        '(-help -h --help)'{-help,-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_swift_package_describe() {
    local -i ret=1
    local -ar ___configuration=('debug' 'release')
    local -ar ___sanitize=('address' 'thread' 'undefined' 'scudo' 'fuzzer')
    local -ar ___explicit_target_dependency_import_check=('none' 'warn' 'error')
    local -ar ___build_system=('native' 'swiftbuild' 'xcode')
    local -ar __debug_info_format=('dwarf' 'codeview' 'none')
    local -ar ___type=('json' 'text' 'mermaid')
    local -ar arg_specs=(
        '--package-path[Specify the package path to operate on (default current directory). This changes the working directory before any other operation.]:package-path:_files -/'
        '--cache-path[Specify the shared cache directory path.]:cache-path:_files -/'
        '--config-path[Specify the shared configuration directory path.]:config-path:_files -/'
        '--security-path[Specify the shared security directory path.]:security-path:_files -/'
        '--scratch-path[Specify a custom scratch directory path. (default .build)]:scratch-path:_files -/'
        '--swift-sdks-path[Path to the directory containing installed Swift SDKs.]:swift-sdks-path:_files -/'
        '*--toolset[Specify a toolset JSON file to use when building for the target platform. Use the option multiple times to specify more than one toolset. Toolsets will be merged in the order they'\''re specified into a single final toolset for the current build.]:toolset:_files -g '\''*..json'\'''
        '*--pkg-config-path[Specify alternative path to search for pkg-config `.pc` files. Use the option multiple times to
        specify more than one path.]:pkg-config-path:_files -/'
        '--enable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--disable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--enable-build-manifest-caching'
        '--disable-build-manifest-caching'
        '--manifest-cache[Caching mode of Package.swift manifests. Valid values are\: (shared\: shared cache, local\: package'\''s build directory, none\: disabled)]:manifest-cache:'
        '--enable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '--disable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '(--verbose -v)'{--verbose,-v}'[Increase verbosity to include informational output.]'
        '(--very-verbose --vv)'{--very-verbose,--vv}'[Increase verbosity to include debug output.]'
        '(--quiet -q)'{--quiet,-q}'[Decrease verbosity to only include error output.]'
        '--color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--no-color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--disable-sandbox[Disable using the sandbox when executing subprocesses.]'
        '--netrc[Use netrc file even in cases where other credential stores are preferred.]'
        '--enable-netrc[Load credentials from a netrc file.]'
        '--disable-netrc[Load credentials from a netrc file.]'
        '--netrc-file[Specify the netrc file path.]:netrc-file:_files'
        '--enable-keychain[Search credentials in macOS keychain.]'
        '--disable-keychain[Search credentials in macOS keychain.]'
        '--resolver-fingerprint-checking:resolver-fingerprint-checking:'
        '--resolver-signing-entity-checking:resolver-signing-entity-checking:'
        '--enable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--disable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--enable-prefetching'
        '--disable-prefetching'
        '(--force-resolved-versions --disable-automatic-resolution --only-use-versions-from-resolved-file)'{--force-resolved-versions,--disable-automatic-resolution,--only-use-versions-from-resolved-file}'[Only use versions from the Package.resolved file and fail resolution if it is out-of-date.]'
        '--skip-update[Skip updating dependencies from their remote during a resolution.]'
        '--disable-scm-to-registry-transformation[Disable source control to registry transformation.]'
        '--use-registry-identity-for-scm[Look up source control dependencies in the registry and use their registry identity when possible to help deduplicate across the two origins.]'
        '--replace-scm-with-registry[Look up source control dependencies in the registry and use the registry to retrieve them instead of source control when possible.]'
        '--default-registry-url[Default registry URL to use, instead of the registries.json configuration file.]:default-registry-url:'
        '(--configuration -c)'{--configuration,-c}'[Build with configuration]:configuration:{__swift_complete "${___configuration[@]}"}'
        '*-Xcc[Pass flag through to all C compiler invocations.]:Xcc:'
        '*-Xswiftc[Pass flag through to all Swift compiler invocations.]:Xswiftc:'
        '*-Xlinker[Pass flag through to all linker invocations.]:Xlinker:'
        '*-Xcxx[Pass flag through to all C++ compiler invocations.]:Xcxx:'
        '--triple:triple:'
        '--sdk:sdk:_files -/'
        '--toolchain:toolchain:_files -/'
        '--swift-sdk[Filter for selecting a specific Swift SDK to build with.]:swift-sdk:'
        '*--sanitize[Turn on runtime checks for erroneous behavior.]:sanitize:{__swift_complete "${___sanitize[@]}"}'
        '--auto-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-index-store[Enable or disable indexing-while-building feature.]'
        '--disable-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-parseable-module-interfaces'
        '(--jobs -j)'{--jobs,-j}'[The number of jobs to spawn in parallel during the build process.]:jobs:'
        '--use-integrated-swift-driver'
        '--explicit-target-dependency-import-check[A flag that indicates this build should check whether targets only import their explicitly-declared dependencies.]:explicit-target-dependency-import-check:{__swift_complete "${___explicit_target_dependency_import_check[@]}"}'
        '--build-system[Specify the build system to use.]:build-system:{__swift_complete "${___build_system[@]}"}'
        '-debug-info-format[The Debug Information Format to use.]:debug-info-format:{__swift_complete "${__debug_info_format[@]}"}'
        '--enable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-local-rpath[Disable adding $ORIGIN/@loader_path to the rpath by default.]'
        '--traits[Enables the passed traits of the package. Multiple traits can be specified by providing a comma separated list e.g. `--traits Trait1,Trait2`. When enabling specific traits the defaults traits need to explictily enabled as well by passing `defaults` to this command.]:traits:'
        '--enable-all-traits[Enables all traits of the package.]'
        '--disable-default-traits[Disables all default traits of the package.]'
        '--type[Set the output format.]:type:{__swift_complete "${___type[@]}"}'
        '--version[Show the version.]'
        '(-help -h --help)'{-help,-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_swift_package_init() {
    local -i ret=1
    local -ar ___configuration=('debug' 'release')
    local -ar ___sanitize=('address' 'thread' 'undefined' 'scudo' 'fuzzer')
    local -ar ___explicit_target_dependency_import_check=('none' 'warn' 'error')
    local -ar ___build_system=('native' 'swiftbuild' 'xcode')
    local -ar __debug_info_format=('dwarf' 'codeview' 'none')
    local -ar arg_specs=(
        '--package-path[Specify the package path to operate on (default current directory). This changes the working directory before any other operation.]:package-path:_files -/'
        '--cache-path[Specify the shared cache directory path.]:cache-path:_files -/'
        '--config-path[Specify the shared configuration directory path.]:config-path:_files -/'
        '--security-path[Specify the shared security directory path.]:security-path:_files -/'
        '--scratch-path[Specify a custom scratch directory path. (default .build)]:scratch-path:_files -/'
        '--swift-sdks-path[Path to the directory containing installed Swift SDKs.]:swift-sdks-path:_files -/'
        '*--toolset[Specify a toolset JSON file to use when building for the target platform. Use the option multiple times to specify more than one toolset. Toolsets will be merged in the order they'\''re specified into a single final toolset for the current build.]:toolset:_files -g '\''*..json'\'''
        '*--pkg-config-path[Specify alternative path to search for pkg-config `.pc` files. Use the option multiple times to
        specify more than one path.]:pkg-config-path:_files -/'
        '--enable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--disable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--enable-build-manifest-caching'
        '--disable-build-manifest-caching'
        '--manifest-cache[Caching mode of Package.swift manifests. Valid values are\: (shared\: shared cache, local\: package'\''s build directory, none\: disabled)]:manifest-cache:'
        '--enable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '--disable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '(--verbose -v)'{--verbose,-v}'[Increase verbosity to include informational output.]'
        '(--very-verbose --vv)'{--very-verbose,--vv}'[Increase verbosity to include debug output.]'
        '(--quiet -q)'{--quiet,-q}'[Decrease verbosity to only include error output.]'
        '--color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--no-color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--disable-sandbox[Disable using the sandbox when executing subprocesses.]'
        '--netrc[Use netrc file even in cases where other credential stores are preferred.]'
        '--enable-netrc[Load credentials from a netrc file.]'
        '--disable-netrc[Load credentials from a netrc file.]'
        '--netrc-file[Specify the netrc file path.]:netrc-file:_files'
        '--enable-keychain[Search credentials in macOS keychain.]'
        '--disable-keychain[Search credentials in macOS keychain.]'
        '--resolver-fingerprint-checking:resolver-fingerprint-checking:'
        '--resolver-signing-entity-checking:resolver-signing-entity-checking:'
        '--enable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--disable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--enable-prefetching'
        '--disable-prefetching'
        '(--force-resolved-versions --disable-automatic-resolution --only-use-versions-from-resolved-file)'{--force-resolved-versions,--disable-automatic-resolution,--only-use-versions-from-resolved-file}'[Only use versions from the Package.resolved file and fail resolution if it is out-of-date.]'
        '--skip-update[Skip updating dependencies from their remote during a resolution.]'
        '--disable-scm-to-registry-transformation[Disable source control to registry transformation.]'
        '--use-registry-identity-for-scm[Look up source control dependencies in the registry and use their registry identity when possible to help deduplicate across the two origins.]'
        '--replace-scm-with-registry[Look up source control dependencies in the registry and use the registry to retrieve them instead of source control when possible.]'
        '--default-registry-url[Default registry URL to use, instead of the registries.json configuration file.]:default-registry-url:'
        '(--configuration -c)'{--configuration,-c}'[Build with configuration]:configuration:{__swift_complete "${___configuration[@]}"}'
        '*-Xcc[Pass flag through to all C compiler invocations.]:Xcc:'
        '*-Xswiftc[Pass flag through to all Swift compiler invocations.]:Xswiftc:'
        '*-Xlinker[Pass flag through to all linker invocations.]:Xlinker:'
        '*-Xcxx[Pass flag through to all C++ compiler invocations.]:Xcxx:'
        '--triple:triple:'
        '--sdk:sdk:_files -/'
        '--toolchain:toolchain:_files -/'
        '--swift-sdk[Filter for selecting a specific Swift SDK to build with.]:swift-sdk:'
        '*--sanitize[Turn on runtime checks for erroneous behavior.]:sanitize:{__swift_complete "${___sanitize[@]}"}'
        '--auto-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-index-store[Enable or disable indexing-while-building feature.]'
        '--disable-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-parseable-module-interfaces'
        '(--jobs -j)'{--jobs,-j}'[The number of jobs to spawn in parallel during the build process.]:jobs:'
        '--use-integrated-swift-driver'
        '--explicit-target-dependency-import-check[A flag that indicates this build should check whether targets only import their explicitly-declared dependencies.]:explicit-target-dependency-import-check:{__swift_complete "${___explicit_target_dependency_import_check[@]}"}'
        '--build-system[Specify the build system to use.]:build-system:{__swift_complete "${___build_system[@]}"}'
        '-debug-info-format[The Debug Information Format to use.]:debug-info-format:{__swift_complete "${__debug_info_format[@]}"}'
        '--enable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-local-rpath[Disable adding $ORIGIN/@loader_path to the rpath by default.]'
        '--traits[Enables the passed traits of the package. Multiple traits can be specified by providing a comma separated list e.g. `--traits Trait1,Trait2`. When enabling specific traits the defaults traits need to explictily enabled as well by passing `defaults` to this command.]:traits:'
        '--enable-all-traits[Enables all traits of the package.]'
        '--disable-default-traits[Disables all default traits of the package.]'
        '--type[Package type\:]:type:'
        '--enable-xctest[Enable support for XCTest.]'
        '--disable-xctest[Enable support for XCTest.]'
        '--enable-swift-testing[Enable support for Swift Testing.]'
        '--disable-swift-testing[Enable support for Swift Testing.]'
        '--name[Provide custom package name.]:name:'
        '--version[Show the version.]'
        '(-help -h --help)'{-help,-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_swift_package_migrate() {
    local -i ret=1
    local -ar ___configuration=('debug' 'release')
    local -ar ___sanitize=('address' 'thread' 'undefined' 'scudo' 'fuzzer')
    local -ar ___explicit_target_dependency_import_check=('none' 'warn' 'error')
    local -ar ___build_system=('native' 'swiftbuild' 'xcode')
    local -ar __debug_info_format=('dwarf' 'codeview' 'none')
    local -ar arg_specs=(
        '--package-path[Specify the package path to operate on (default current directory). This changes the working directory before any other operation.]:package-path:_files -/'
        '--cache-path[Specify the shared cache directory path.]:cache-path:_files -/'
        '--config-path[Specify the shared configuration directory path.]:config-path:_files -/'
        '--security-path[Specify the shared security directory path.]:security-path:_files -/'
        '--scratch-path[Specify a custom scratch directory path. (default .build)]:scratch-path:_files -/'
        '--swift-sdks-path[Path to the directory containing installed Swift SDKs.]:swift-sdks-path:_files -/'
        '*--toolset[Specify a toolset JSON file to use when building for the target platform. Use the option multiple times to specify more than one toolset. Toolsets will be merged in the order they'\''re specified into a single final toolset for the current build.]:toolset:_files -g '\''*..json'\'''
        '*--pkg-config-path[Specify alternative path to search for pkg-config `.pc` files. Use the option multiple times to
        specify more than one path.]:pkg-config-path:_files -/'
        '--enable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--disable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--enable-build-manifest-caching'
        '--disable-build-manifest-caching'
        '--manifest-cache[Caching mode of Package.swift manifests. Valid values are\: (shared\: shared cache, local\: package'\''s build directory, none\: disabled)]:manifest-cache:'
        '--enable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '--disable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '(--verbose -v)'{--verbose,-v}'[Increase verbosity to include informational output.]'
        '(--very-verbose --vv)'{--very-verbose,--vv}'[Increase verbosity to include debug output.]'
        '(--quiet -q)'{--quiet,-q}'[Decrease verbosity to only include error output.]'
        '--color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--no-color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--disable-sandbox[Disable using the sandbox when executing subprocesses.]'
        '--netrc[Use netrc file even in cases where other credential stores are preferred.]'
        '--enable-netrc[Load credentials from a netrc file.]'
        '--disable-netrc[Load credentials from a netrc file.]'
        '--netrc-file[Specify the netrc file path.]:netrc-file:_files'
        '--enable-keychain[Search credentials in macOS keychain.]'
        '--disable-keychain[Search credentials in macOS keychain.]'
        '--resolver-fingerprint-checking:resolver-fingerprint-checking:'
        '--resolver-signing-entity-checking:resolver-signing-entity-checking:'
        '--enable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--disable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--enable-prefetching'
        '--disable-prefetching'
        '(--force-resolved-versions --disable-automatic-resolution --only-use-versions-from-resolved-file)'{--force-resolved-versions,--disable-automatic-resolution,--only-use-versions-from-resolved-file}'[Only use versions from the Package.resolved file and fail resolution if it is out-of-date.]'
        '--skip-update[Skip updating dependencies from their remote during a resolution.]'
        '--disable-scm-to-registry-transformation[Disable source control to registry transformation.]'
        '--use-registry-identity-for-scm[Look up source control dependencies in the registry and use their registry identity when possible to help deduplicate across the two origins.]'
        '--replace-scm-with-registry[Look up source control dependencies in the registry and use the registry to retrieve them instead of source control when possible.]'
        '--default-registry-url[Default registry URL to use, instead of the registries.json configuration file.]:default-registry-url:'
        '(--configuration -c)'{--configuration,-c}'[Build with configuration]:configuration:{__swift_complete "${___configuration[@]}"}'
        '*-Xcc[Pass flag through to all C compiler invocations.]:Xcc:'
        '*-Xswiftc[Pass flag through to all Swift compiler invocations.]:Xswiftc:'
        '*-Xlinker[Pass flag through to all linker invocations.]:Xlinker:'
        '*-Xcxx[Pass flag through to all C++ compiler invocations.]:Xcxx:'
        '--triple:triple:'
        '--sdk:sdk:_files -/'
        '--toolchain:toolchain:_files -/'
        '--swift-sdk[Filter for selecting a specific Swift SDK to build with.]:swift-sdk:'
        '*--sanitize[Turn on runtime checks for erroneous behavior.]:sanitize:{__swift_complete "${___sanitize[@]}"}'
        '--auto-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-index-store[Enable or disable indexing-while-building feature.]'
        '--disable-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-parseable-module-interfaces'
        '(--jobs -j)'{--jobs,-j}'[The number of jobs to spawn in parallel during the build process.]:jobs:'
        '--use-integrated-swift-driver'
        '--explicit-target-dependency-import-check[A flag that indicates this build should check whether targets only import their explicitly-declared dependencies.]:explicit-target-dependency-import-check:{__swift_complete "${___explicit_target_dependency_import_check[@]}"}'
        '--build-system[Specify the build system to use.]:build-system:{__swift_complete "${___build_system[@]}"}'
        '-debug-info-format[The Debug Information Format to use.]:debug-info-format:{__swift_complete "${__debug_info_format[@]}"}'
        '--enable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-local-rpath[Disable adding $ORIGIN/@loader_path to the rpath by default.]'
        '--traits[Enables the passed traits of the package. Multiple traits can be specified by providing a comma separated list e.g. `--traits Trait1,Trait2`. When enabling specific traits the defaults traits need to explictily enabled as well by passing `defaults` to this command.]:traits:'
        '--enable-all-traits[Enables all traits of the package.]'
        '--disable-default-traits[Disables all default traits of the package.]'
        '--target[A comma-separated list of targets to migrate. (default\: all Swift targets)]:target:'
        '--to-feature[A comma-separated list of Swift language features to migrate to.]:to-feature:'
        '--version[Show the version.]'
        '(-help -h --help)'{-help,-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_swift_package_experimental-install() {
    local -i ret=1
    local -ar ___configuration=('debug' 'release')
    local -ar ___sanitize=('address' 'thread' 'undefined' 'scudo' 'fuzzer')
    local -ar ___explicit_target_dependency_import_check=('none' 'warn' 'error')
    local -ar ___build_system=('native' 'swiftbuild' 'xcode')
    local -ar __debug_info_format=('dwarf' 'codeview' 'none')
    local -ar arg_specs=(
        '--package-path[Specify the package path to operate on (default current directory). This changes the working directory before any other operation.]:package-path:_files -/'
        '--cache-path[Specify the shared cache directory path.]:cache-path:_files -/'
        '--config-path[Specify the shared configuration directory path.]:config-path:_files -/'
        '--security-path[Specify the shared security directory path.]:security-path:_files -/'
        '--scratch-path[Specify a custom scratch directory path. (default .build)]:scratch-path:_files -/'
        '--swift-sdks-path[Path to the directory containing installed Swift SDKs.]:swift-sdks-path:_files -/'
        '*--toolset[Specify a toolset JSON file to use when building for the target platform. Use the option multiple times to specify more than one toolset. Toolsets will be merged in the order they'\''re specified into a single final toolset for the current build.]:toolset:_files -g '\''*..json'\'''
        '*--pkg-config-path[Specify alternative path to search for pkg-config `.pc` files. Use the option multiple times to
        specify more than one path.]:pkg-config-path:_files -/'
        '--enable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--disable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--enable-build-manifest-caching'
        '--disable-build-manifest-caching'
        '--manifest-cache[Caching mode of Package.swift manifests. Valid values are\: (shared\: shared cache, local\: package'\''s build directory, none\: disabled)]:manifest-cache:'
        '--enable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '--disable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '(--verbose -v)'{--verbose,-v}'[Increase verbosity to include informational output.]'
        '(--very-verbose --vv)'{--very-verbose,--vv}'[Increase verbosity to include debug output.]'
        '(--quiet -q)'{--quiet,-q}'[Decrease verbosity to only include error output.]'
        '--color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--no-color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--disable-sandbox[Disable using the sandbox when executing subprocesses.]'
        '--netrc[Use netrc file even in cases where other credential stores are preferred.]'
        '--enable-netrc[Load credentials from a netrc file.]'
        '--disable-netrc[Load credentials from a netrc file.]'
        '--netrc-file[Specify the netrc file path.]:netrc-file:_files'
        '--enable-keychain[Search credentials in macOS keychain.]'
        '--disable-keychain[Search credentials in macOS keychain.]'
        '--resolver-fingerprint-checking:resolver-fingerprint-checking:'
        '--resolver-signing-entity-checking:resolver-signing-entity-checking:'
        '--enable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--disable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--enable-prefetching'
        '--disable-prefetching'
        '(--force-resolved-versions --disable-automatic-resolution --only-use-versions-from-resolved-file)'{--force-resolved-versions,--disable-automatic-resolution,--only-use-versions-from-resolved-file}'[Only use versions from the Package.resolved file and fail resolution if it is out-of-date.]'
        '--skip-update[Skip updating dependencies from their remote during a resolution.]'
        '--disable-scm-to-registry-transformation[Disable source control to registry transformation.]'
        '--use-registry-identity-for-scm[Look up source control dependencies in the registry and use their registry identity when possible to help deduplicate across the two origins.]'
        '--replace-scm-with-registry[Look up source control dependencies in the registry and use the registry to retrieve them instead of source control when possible.]'
        '--default-registry-url[Default registry URL to use, instead of the registries.json configuration file.]:default-registry-url:'
        '(--configuration -c)'{--configuration,-c}'[Build with configuration]:configuration:{__swift_complete "${___configuration[@]}"}'
        '*-Xcc[Pass flag through to all C compiler invocations.]:Xcc:'
        '*-Xswiftc[Pass flag through to all Swift compiler invocations.]:Xswiftc:'
        '*-Xlinker[Pass flag through to all linker invocations.]:Xlinker:'
        '*-Xcxx[Pass flag through to all C++ compiler invocations.]:Xcxx:'
        '--triple:triple:'
        '--sdk:sdk:_files -/'
        '--toolchain:toolchain:_files -/'
        '--swift-sdk[Filter for selecting a specific Swift SDK to build with.]:swift-sdk:'
        '*--sanitize[Turn on runtime checks for erroneous behavior.]:sanitize:{__swift_complete "${___sanitize[@]}"}'
        '--auto-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-index-store[Enable or disable indexing-while-building feature.]'
        '--disable-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-parseable-module-interfaces'
        '(--jobs -j)'{--jobs,-j}'[The number of jobs to spawn in parallel during the build process.]:jobs:'
        '--use-integrated-swift-driver'
        '--explicit-target-dependency-import-check[A flag that indicates this build should check whether targets only import their explicitly-declared dependencies.]:explicit-target-dependency-import-check:{__swift_complete "${___explicit_target_dependency_import_check[@]}"}'
        '--build-system[Specify the build system to use.]:build-system:{__swift_complete "${___build_system[@]}"}'
        '-debug-info-format[The Debug Information Format to use.]:debug-info-format:{__swift_complete "${__debug_info_format[@]}"}'
        '--enable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-local-rpath[Disable adding $ORIGIN/@loader_path to the rpath by default.]'
        '--traits[Enables the passed traits of the package. Multiple traits can be specified by providing a comma separated list e.g. `--traits Trait1,Trait2`. When enabling specific traits the defaults traits need to explictily enabled as well by passing `defaults` to this command.]:traits:'
        '--enable-all-traits[Enables all traits of the package.]'
        '--disable-default-traits[Disables all default traits of the package.]'
        '--product[The name of the executable product to install.]:product:'
        '--version[Show the version.]'
        '(-help -h --help)'{-help,-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_swift_package_experimental-uninstall() {
    local -i ret=1
    local -ar ___configuration=('debug' 'release')
    local -ar ___sanitize=('address' 'thread' 'undefined' 'scudo' 'fuzzer')
    local -ar ___explicit_target_dependency_import_check=('none' 'warn' 'error')
    local -ar ___build_system=('native' 'swiftbuild' 'xcode')
    local -ar __debug_info_format=('dwarf' 'codeview' 'none')
    local -ar arg_specs=(
        '--package-path[Specify the package path to operate on (default current directory). This changes the working directory before any other operation.]:package-path:_files -/'
        '--cache-path[Specify the shared cache directory path.]:cache-path:_files -/'
        '--config-path[Specify the shared configuration directory path.]:config-path:_files -/'
        '--security-path[Specify the shared security directory path.]:security-path:_files -/'
        '--scratch-path[Specify a custom scratch directory path. (default .build)]:scratch-path:_files -/'
        '--swift-sdks-path[Path to the directory containing installed Swift SDKs.]:swift-sdks-path:_files -/'
        '*--toolset[Specify a toolset JSON file to use when building for the target platform. Use the option multiple times to specify more than one toolset. Toolsets will be merged in the order they'\''re specified into a single final toolset for the current build.]:toolset:_files -g '\''*..json'\'''
        '*--pkg-config-path[Specify alternative path to search for pkg-config `.pc` files. Use the option multiple times to
        specify more than one path.]:pkg-config-path:_files -/'
        '--enable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--disable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--enable-build-manifest-caching'
        '--disable-build-manifest-caching'
        '--manifest-cache[Caching mode of Package.swift manifests. Valid values are\: (shared\: shared cache, local\: package'\''s build directory, none\: disabled)]:manifest-cache:'
        '--enable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '--disable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '(--verbose -v)'{--verbose,-v}'[Increase verbosity to include informational output.]'
        '(--very-verbose --vv)'{--very-verbose,--vv}'[Increase verbosity to include debug output.]'
        '(--quiet -q)'{--quiet,-q}'[Decrease verbosity to only include error output.]'
        '--color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--no-color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--disable-sandbox[Disable using the sandbox when executing subprocesses.]'
        '--netrc[Use netrc file even in cases where other credential stores are preferred.]'
        '--enable-netrc[Load credentials from a netrc file.]'
        '--disable-netrc[Load credentials from a netrc file.]'
        '--netrc-file[Specify the netrc file path.]:netrc-file:_files'
        '--enable-keychain[Search credentials in macOS keychain.]'
        '--disable-keychain[Search credentials in macOS keychain.]'
        '--resolver-fingerprint-checking:resolver-fingerprint-checking:'
        '--resolver-signing-entity-checking:resolver-signing-entity-checking:'
        '--enable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--disable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--enable-prefetching'
        '--disable-prefetching'
        '(--force-resolved-versions --disable-automatic-resolution --only-use-versions-from-resolved-file)'{--force-resolved-versions,--disable-automatic-resolution,--only-use-versions-from-resolved-file}'[Only use versions from the Package.resolved file and fail resolution if it is out-of-date.]'
        '--skip-update[Skip updating dependencies from their remote during a resolution.]'
        '--disable-scm-to-registry-transformation[Disable source control to registry transformation.]'
        '--use-registry-identity-for-scm[Look up source control dependencies in the registry and use their registry identity when possible to help deduplicate across the two origins.]'
        '--replace-scm-with-registry[Look up source control dependencies in the registry and use the registry to retrieve them instead of source control when possible.]'
        '--default-registry-url[Default registry URL to use, instead of the registries.json configuration file.]:default-registry-url:'
        '(--configuration -c)'{--configuration,-c}'[Build with configuration]:configuration:{__swift_complete "${___configuration[@]}"}'
        '*-Xcc[Pass flag through to all C compiler invocations.]:Xcc:'
        '*-Xswiftc[Pass flag through to all Swift compiler invocations.]:Xswiftc:'
        '*-Xlinker[Pass flag through to all linker invocations.]:Xlinker:'
        '*-Xcxx[Pass flag through to all C++ compiler invocations.]:Xcxx:'
        '--triple:triple:'
        '--sdk:sdk:_files -/'
        '--toolchain:toolchain:_files -/'
        '--swift-sdk[Filter for selecting a specific Swift SDK to build with.]:swift-sdk:'
        '*--sanitize[Turn on runtime checks for erroneous behavior.]:sanitize:{__swift_complete "${___sanitize[@]}"}'
        '--auto-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-index-store[Enable or disable indexing-while-building feature.]'
        '--disable-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-parseable-module-interfaces'
        '(--jobs -j)'{--jobs,-j}'[The number of jobs to spawn in parallel during the build process.]:jobs:'
        '--use-integrated-swift-driver'
        '--explicit-target-dependency-import-check[A flag that indicates this build should check whether targets only import their explicitly-declared dependencies.]:explicit-target-dependency-import-check:{__swift_complete "${___explicit_target_dependency_import_check[@]}"}'
        '--build-system[Specify the build system to use.]:build-system:{__swift_complete "${___build_system[@]}"}'
        '-debug-info-format[The Debug Information Format to use.]:debug-info-format:{__swift_complete "${__debug_info_format[@]}"}'
        '--enable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-local-rpath[Disable adding $ORIGIN/@loader_path to the rpath by default.]'
        '--traits[Enables the passed traits of the package. Multiple traits can be specified by providing a comma separated list e.g. `--traits Trait1,Trait2`. When enabling specific traits the defaults traits need to explictily enabled as well by passing `defaults` to this command.]:traits:'
        '--enable-all-traits[Enables all traits of the package.]'
        '--disable-default-traits[Disables all default traits of the package.]'
        ':name:'
        '--version[Show the version.]'
        '(-help -h --help)'{-help,-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_swift_package_diagnose-api-breaking-changes() {
    local -i ret=1
    local -ar ___configuration=('debug' 'release')
    local -ar ___sanitize=('address' 'thread' 'undefined' 'scudo' 'fuzzer')
    local -ar ___explicit_target_dependency_import_check=('none' 'warn' 'error')
    local -ar ___build_system=('native' 'swiftbuild' 'xcode')
    local -ar __debug_info_format=('dwarf' 'codeview' 'none')
    local -ar arg_specs=(
        '--package-path[Specify the package path to operate on (default current directory). This changes the working directory before any other operation.]:package-path:_files -/'
        '--cache-path[Specify the shared cache directory path.]:cache-path:_files -/'
        '--config-path[Specify the shared configuration directory path.]:config-path:_files -/'
        '--security-path[Specify the shared security directory path.]:security-path:_files -/'
        '--scratch-path[Specify a custom scratch directory path. (default .build)]:scratch-path:_files -/'
        '--swift-sdks-path[Path to the directory containing installed Swift SDKs.]:swift-sdks-path:_files -/'
        '*--toolset[Specify a toolset JSON file to use when building for the target platform. Use the option multiple times to specify more than one toolset. Toolsets will be merged in the order they'\''re specified into a single final toolset for the current build.]:toolset:_files -g '\''*..json'\'''
        '*--pkg-config-path[Specify alternative path to search for pkg-config `.pc` files. Use the option multiple times to
        specify more than one path.]:pkg-config-path:_files -/'
        '--enable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--disable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--enable-build-manifest-caching'
        '--disable-build-manifest-caching'
        '--manifest-cache[Caching mode of Package.swift manifests. Valid values are\: (shared\: shared cache, local\: package'\''s build directory, none\: disabled)]:manifest-cache:'
        '--enable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '--disable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '(--verbose -v)'{--verbose,-v}'[Increase verbosity to include informational output.]'
        '(--very-verbose --vv)'{--very-verbose,--vv}'[Increase verbosity to include debug output.]'
        '(--quiet -q)'{--quiet,-q}'[Decrease verbosity to only include error output.]'
        '--color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--no-color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--disable-sandbox[Disable using the sandbox when executing subprocesses.]'
        '--netrc[Use netrc file even in cases where other credential stores are preferred.]'
        '--enable-netrc[Load credentials from a netrc file.]'
        '--disable-netrc[Load credentials from a netrc file.]'
        '--netrc-file[Specify the netrc file path.]:netrc-file:_files'
        '--enable-keychain[Search credentials in macOS keychain.]'
        '--disable-keychain[Search credentials in macOS keychain.]'
        '--resolver-fingerprint-checking:resolver-fingerprint-checking:'
        '--resolver-signing-entity-checking:resolver-signing-entity-checking:'
        '--enable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--disable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--enable-prefetching'
        '--disable-prefetching'
        '(--force-resolved-versions --disable-automatic-resolution --only-use-versions-from-resolved-file)'{--force-resolved-versions,--disable-automatic-resolution,--only-use-versions-from-resolved-file}'[Only use versions from the Package.resolved file and fail resolution if it is out-of-date.]'
        '--skip-update[Skip updating dependencies from their remote during a resolution.]'
        '--disable-scm-to-registry-transformation[Disable source control to registry transformation.]'
        '--use-registry-identity-for-scm[Look up source control dependencies in the registry and use their registry identity when possible to help deduplicate across the two origins.]'
        '--replace-scm-with-registry[Look up source control dependencies in the registry and use the registry to retrieve them instead of source control when possible.]'
        '--default-registry-url[Default registry URL to use, instead of the registries.json configuration file.]:default-registry-url:'
        '(--configuration -c)'{--configuration,-c}'[Build with configuration]:configuration:{__swift_complete "${___configuration[@]}"}'
        '*-Xcc[Pass flag through to all C compiler invocations.]:Xcc:'
        '*-Xswiftc[Pass flag through to all Swift compiler invocations.]:Xswiftc:'
        '*-Xlinker[Pass flag through to all linker invocations.]:Xlinker:'
        '*-Xcxx[Pass flag through to all C++ compiler invocations.]:Xcxx:'
        '--triple:triple:'
        '--sdk:sdk:_files -/'
        '--toolchain:toolchain:_files -/'
        '--swift-sdk[Filter for selecting a specific Swift SDK to build with.]:swift-sdk:'
        '*--sanitize[Turn on runtime checks for erroneous behavior.]:sanitize:{__swift_complete "${___sanitize[@]}"}'
        '--auto-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-index-store[Enable or disable indexing-while-building feature.]'
        '--disable-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-parseable-module-interfaces'
        '(--jobs -j)'{--jobs,-j}'[The number of jobs to spawn in parallel during the build process.]:jobs:'
        '--use-integrated-swift-driver'
        '--explicit-target-dependency-import-check[A flag that indicates this build should check whether targets only import their explicitly-declared dependencies.]:explicit-target-dependency-import-check:{__swift_complete "${___explicit_target_dependency_import_check[@]}"}'
        '--build-system[Specify the build system to use.]:build-system:{__swift_complete "${___build_system[@]}"}'
        '-debug-info-format[The Debug Information Format to use.]:debug-info-format:{__swift_complete "${__debug_info_format[@]}"}'
        '--enable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-local-rpath[Disable adding $ORIGIN/@loader_path to the rpath by default.]'
        '--traits[Enables the passed traits of the package. Multiple traits can be specified by providing a comma separated list e.g. `--traits Trait1,Trait2`. When enabling specific traits the defaults traits need to explictily enabled as well by passing `defaults` to this command.]:traits:'
        '--enable-all-traits[Enables all traits of the package.]'
        '--disable-default-traits[Disables all default traits of the package.]'
        '--breakage-allowlist-path[The path to a text file containing breaking changes which should be ignored by the API comparison. Each ignored breaking change in the file should appear on its own line and contain the exact message to be ignored (e.g. '\''API breakage\: func foo() has been removed'\'').]:breakage-allowlist-path:_files -/'
        ':treeish:'
        '--products[One or more products to include in the API comparison. If present, only the specified products (and any targets specified using `--targets`) will be compared.]:products:'
        '--targets[One or more targets to include in the API comparison. If present, only the specified targets (and any products specified using `--products`) will be compared.]:targets:'
        '--baseline-dir[The path to a directory used to store API baseline files. If unspecified, a temporary directory will be used.]:baseline-dir:_files -/'
        '--regenerate-baseline[Regenerate the API baseline, even if an existing one is available.]'
        '--version[Show the version.]'
        '(-help -h --help)'{-help,-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_swift_package_dump-symbol-graph() {
    local -i ret=1
    local -ar ___configuration=('debug' 'release')
    local -ar ___sanitize=('address' 'thread' 'undefined' 'scudo' 'fuzzer')
    local -ar ___explicit_target_dependency_import_check=('none' 'warn' 'error')
    local -ar ___build_system=('native' 'swiftbuild' 'xcode')
    local -ar __debug_info_format=('dwarf' 'codeview' 'none')
    local -ar ___minimum_access_level=('private' 'fileprivate' 'internal' 'package' 'public' 'open')
    local -ar arg_specs=(
        '--package-path[Specify the package path to operate on (default current directory). This changes the working directory before any other operation.]:package-path:_files -/'
        '--cache-path[Specify the shared cache directory path.]:cache-path:_files -/'
        '--config-path[Specify the shared configuration directory path.]:config-path:_files -/'
        '--security-path[Specify the shared security directory path.]:security-path:_files -/'
        '--scratch-path[Specify a custom scratch directory path. (default .build)]:scratch-path:_files -/'
        '--swift-sdks-path[Path to the directory containing installed Swift SDKs.]:swift-sdks-path:_files -/'
        '*--toolset[Specify a toolset JSON file to use when building for the target platform. Use the option multiple times to specify more than one toolset. Toolsets will be merged in the order they'\''re specified into a single final toolset for the current build.]:toolset:_files -g '\''*..json'\'''
        '*--pkg-config-path[Specify alternative path to search for pkg-config `.pc` files. Use the option multiple times to
        specify more than one path.]:pkg-config-path:_files -/'
        '--enable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--disable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--enable-build-manifest-caching'
        '--disable-build-manifest-caching'
        '--manifest-cache[Caching mode of Package.swift manifests. Valid values are\: (shared\: shared cache, local\: package'\''s build directory, none\: disabled)]:manifest-cache:'
        '--enable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '--disable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '(--verbose -v)'{--verbose,-v}'[Increase verbosity to include informational output.]'
        '(--very-verbose --vv)'{--very-verbose,--vv}'[Increase verbosity to include debug output.]'
        '(--quiet -q)'{--quiet,-q}'[Decrease verbosity to only include error output.]'
        '--color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--no-color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--disable-sandbox[Disable using the sandbox when executing subprocesses.]'
        '--netrc[Use netrc file even in cases where other credential stores are preferred.]'
        '--enable-netrc[Load credentials from a netrc file.]'
        '--disable-netrc[Load credentials from a netrc file.]'
        '--netrc-file[Specify the netrc file path.]:netrc-file:_files'
        '--enable-keychain[Search credentials in macOS keychain.]'
        '--disable-keychain[Search credentials in macOS keychain.]'
        '--resolver-fingerprint-checking:resolver-fingerprint-checking:'
        '--resolver-signing-entity-checking:resolver-signing-entity-checking:'
        '--enable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--disable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--enable-prefetching'
        '--disable-prefetching'
        '(--force-resolved-versions --disable-automatic-resolution --only-use-versions-from-resolved-file)'{--force-resolved-versions,--disable-automatic-resolution,--only-use-versions-from-resolved-file}'[Only use versions from the Package.resolved file and fail resolution if it is out-of-date.]'
        '--skip-update[Skip updating dependencies from their remote during a resolution.]'
        '--disable-scm-to-registry-transformation[Disable source control to registry transformation.]'
        '--use-registry-identity-for-scm[Look up source control dependencies in the registry and use their registry identity when possible to help deduplicate across the two origins.]'
        '--replace-scm-with-registry[Look up source control dependencies in the registry and use the registry to retrieve them instead of source control when possible.]'
        '--default-registry-url[Default registry URL to use, instead of the registries.json configuration file.]:default-registry-url:'
        '(--configuration -c)'{--configuration,-c}'[Build with configuration]:configuration:{__swift_complete "${___configuration[@]}"}'
        '*-Xcc[Pass flag through to all C compiler invocations.]:Xcc:'
        '*-Xswiftc[Pass flag through to all Swift compiler invocations.]:Xswiftc:'
        '*-Xlinker[Pass flag through to all linker invocations.]:Xlinker:'
        '*-Xcxx[Pass flag through to all C++ compiler invocations.]:Xcxx:'
        '--triple:triple:'
        '--sdk:sdk:_files -/'
        '--toolchain:toolchain:_files -/'
        '--swift-sdk[Filter for selecting a specific Swift SDK to build with.]:swift-sdk:'
        '*--sanitize[Turn on runtime checks for erroneous behavior.]:sanitize:{__swift_complete "${___sanitize[@]}"}'
        '--auto-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-index-store[Enable or disable indexing-while-building feature.]'
        '--disable-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-parseable-module-interfaces'
        '(--jobs -j)'{--jobs,-j}'[The number of jobs to spawn in parallel during the build process.]:jobs:'
        '--use-integrated-swift-driver'
        '--explicit-target-dependency-import-check[A flag that indicates this build should check whether targets only import their explicitly-declared dependencies.]:explicit-target-dependency-import-check:{__swift_complete "${___explicit_target_dependency_import_check[@]}"}'
        '--build-system[Specify the build system to use.]:build-system:{__swift_complete "${___build_system[@]}"}'
        '-debug-info-format[The Debug Information Format to use.]:debug-info-format:{__swift_complete "${__debug_info_format[@]}"}'
        '--enable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-local-rpath[Disable adding $ORIGIN/@loader_path to the rpath by default.]'
        '--traits[Enables the passed traits of the package. Multiple traits can be specified by providing a comma separated list e.g. `--traits Trait1,Trait2`. When enabling specific traits the defaults traits need to explictily enabled as well by passing `defaults` to this command.]:traits:'
        '--enable-all-traits[Enables all traits of the package.]'
        '--disable-default-traits[Disables all default traits of the package.]'
        '--pretty-print[Pretty-print the output JSON.]'
        '--skip-synthesized-members[Skip members inherited through classes or default implementations.]'
        '--minimum-access-level[Include symbols with this access level or more. Possible values\: private | fileprivate | internal | package | public | open.]:minimum-access-level:{__swift_complete "${___minimum_access_level[@]}"}'
        '--skip-inherited-docs[Skip emitting doc comments for members inherited through classes or default implementations.]'
        '--include-spi-symbols[Add symbols with SPI information to the symbol graph.]'
        '--emit-extension-block-symbols[Emit extension block symbols for extensions to external types or directly associate members and conformances with the extended nominal.]'
        '--omit-extension-block-symbols[Emit extension block symbols for extensions to external types or directly associate members and conformances with the extended nominal.]'
        '--version[Show the version.]'
        '(-help -h --help)'{-help,-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_swift_package_dump-package() {
    local -i ret=1
    local -ar ___configuration=('debug' 'release')
    local -ar ___sanitize=('address' 'thread' 'undefined' 'scudo' 'fuzzer')
    local -ar ___explicit_target_dependency_import_check=('none' 'warn' 'error')
    local -ar ___build_system=('native' 'swiftbuild' 'xcode')
    local -ar __debug_info_format=('dwarf' 'codeview' 'none')
    local -ar arg_specs=(
        '--package-path[Specify the package path to operate on (default current directory). This changes the working directory before any other operation.]:package-path:_files -/'
        '--cache-path[Specify the shared cache directory path.]:cache-path:_files -/'
        '--config-path[Specify the shared configuration directory path.]:config-path:_files -/'
        '--security-path[Specify the shared security directory path.]:security-path:_files -/'
        '--scratch-path[Specify a custom scratch directory path. (default .build)]:scratch-path:_files -/'
        '--swift-sdks-path[Path to the directory containing installed Swift SDKs.]:swift-sdks-path:_files -/'
        '*--toolset[Specify a toolset JSON file to use when building for the target platform. Use the option multiple times to specify more than one toolset. Toolsets will be merged in the order they'\''re specified into a single final toolset for the current build.]:toolset:_files -g '\''*..json'\'''
        '*--pkg-config-path[Specify alternative path to search for pkg-config `.pc` files. Use the option multiple times to
        specify more than one path.]:pkg-config-path:_files -/'
        '--enable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--disable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--enable-build-manifest-caching'
        '--disable-build-manifest-caching'
        '--manifest-cache[Caching mode of Package.swift manifests. Valid values are\: (shared\: shared cache, local\: package'\''s build directory, none\: disabled)]:manifest-cache:'
        '--enable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '--disable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '(--verbose -v)'{--verbose,-v}'[Increase verbosity to include informational output.]'
        '(--very-verbose --vv)'{--very-verbose,--vv}'[Increase verbosity to include debug output.]'
        '(--quiet -q)'{--quiet,-q}'[Decrease verbosity to only include error output.]'
        '--color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--no-color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--disable-sandbox[Disable using the sandbox when executing subprocesses.]'
        '--netrc[Use netrc file even in cases where other credential stores are preferred.]'
        '--enable-netrc[Load credentials from a netrc file.]'
        '--disable-netrc[Load credentials from a netrc file.]'
        '--netrc-file[Specify the netrc file path.]:netrc-file:_files'
        '--enable-keychain[Search credentials in macOS keychain.]'
        '--disable-keychain[Search credentials in macOS keychain.]'
        '--resolver-fingerprint-checking:resolver-fingerprint-checking:'
        '--resolver-signing-entity-checking:resolver-signing-entity-checking:'
        '--enable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--disable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--enable-prefetching'
        '--disable-prefetching'
        '(--force-resolved-versions --disable-automatic-resolution --only-use-versions-from-resolved-file)'{--force-resolved-versions,--disable-automatic-resolution,--only-use-versions-from-resolved-file}'[Only use versions from the Package.resolved file and fail resolution if it is out-of-date.]'
        '--skip-update[Skip updating dependencies from their remote during a resolution.]'
        '--disable-scm-to-registry-transformation[Disable source control to registry transformation.]'
        '--use-registry-identity-for-scm[Look up source control dependencies in the registry and use their registry identity when possible to help deduplicate across the two origins.]'
        '--replace-scm-with-registry[Look up source control dependencies in the registry and use the registry to retrieve them instead of source control when possible.]'
        '--default-registry-url[Default registry URL to use, instead of the registries.json configuration file.]:default-registry-url:'
        '(--configuration -c)'{--configuration,-c}'[Build with configuration]:configuration:{__swift_complete "${___configuration[@]}"}'
        '*-Xcc[Pass flag through to all C compiler invocations.]:Xcc:'
        '*-Xswiftc[Pass flag through to all Swift compiler invocations.]:Xswiftc:'
        '*-Xlinker[Pass flag through to all linker invocations.]:Xlinker:'
        '*-Xcxx[Pass flag through to all C++ compiler invocations.]:Xcxx:'
        '--triple:triple:'
        '--sdk:sdk:_files -/'
        '--toolchain:toolchain:_files -/'
        '--swift-sdk[Filter for selecting a specific Swift SDK to build with.]:swift-sdk:'
        '*--sanitize[Turn on runtime checks for erroneous behavior.]:sanitize:{__swift_complete "${___sanitize[@]}"}'
        '--auto-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-index-store[Enable or disable indexing-while-building feature.]'
        '--disable-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-parseable-module-interfaces'
        '(--jobs -j)'{--jobs,-j}'[The number of jobs to spawn in parallel during the build process.]:jobs:'
        '--use-integrated-swift-driver'
        '--explicit-target-dependency-import-check[A flag that indicates this build should check whether targets only import their explicitly-declared dependencies.]:explicit-target-dependency-import-check:{__swift_complete "${___explicit_target_dependency_import_check[@]}"}'
        '--build-system[Specify the build system to use.]:build-system:{__swift_complete "${___build_system[@]}"}'
        '-debug-info-format[The Debug Information Format to use.]:debug-info-format:{__swift_complete "${__debug_info_format[@]}"}'
        '--enable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-local-rpath[Disable adding $ORIGIN/@loader_path to the rpath by default.]'
        '--traits[Enables the passed traits of the package. Multiple traits can be specified by providing a comma separated list e.g. `--traits Trait1,Trait2`. When enabling specific traits the defaults traits need to explictily enabled as well by passing `defaults` to this command.]:traits:'
        '--enable-all-traits[Enables all traits of the package.]'
        '--disable-default-traits[Disables all default traits of the package.]'
        '--version[Show the version.]'
        '(-help -h --help)'{-help,-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_swift_package_edit() {
    local -i ret=1
    local -ar ___configuration=('debug' 'release')
    local -ar ___sanitize=('address' 'thread' 'undefined' 'scudo' 'fuzzer')
    local -ar ___explicit_target_dependency_import_check=('none' 'warn' 'error')
    local -ar ___build_system=('native' 'swiftbuild' 'xcode')
    local -ar __debug_info_format=('dwarf' 'codeview' 'none')
    local -ar arg_specs=(
        '--package-path[Specify the package path to operate on (default current directory). This changes the working directory before any other operation.]:package-path:_files -/'
        '--cache-path[Specify the shared cache directory path.]:cache-path:_files -/'
        '--config-path[Specify the shared configuration directory path.]:config-path:_files -/'
        '--security-path[Specify the shared security directory path.]:security-path:_files -/'
        '--scratch-path[Specify a custom scratch directory path. (default .build)]:scratch-path:_files -/'
        '--swift-sdks-path[Path to the directory containing installed Swift SDKs.]:swift-sdks-path:_files -/'
        '*--toolset[Specify a toolset JSON file to use when building for the target platform. Use the option multiple times to specify more than one toolset. Toolsets will be merged in the order they'\''re specified into a single final toolset for the current build.]:toolset:_files -g '\''*..json'\'''
        '*--pkg-config-path[Specify alternative path to search for pkg-config `.pc` files. Use the option multiple times to
        specify more than one path.]:pkg-config-path:_files -/'
        '--enable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--disable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--enable-build-manifest-caching'
        '--disable-build-manifest-caching'
        '--manifest-cache[Caching mode of Package.swift manifests. Valid values are\: (shared\: shared cache, local\: package'\''s build directory, none\: disabled)]:manifest-cache:'
        '--enable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '--disable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '(--verbose -v)'{--verbose,-v}'[Increase verbosity to include informational output.]'
        '(--very-verbose --vv)'{--very-verbose,--vv}'[Increase verbosity to include debug output.]'
        '(--quiet -q)'{--quiet,-q}'[Decrease verbosity to only include error output.]'
        '--color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--no-color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--disable-sandbox[Disable using the sandbox when executing subprocesses.]'
        '--netrc[Use netrc file even in cases where other credential stores are preferred.]'
        '--enable-netrc[Load credentials from a netrc file.]'
        '--disable-netrc[Load credentials from a netrc file.]'
        '--netrc-file[Specify the netrc file path.]:netrc-file:_files'
        '--enable-keychain[Search credentials in macOS keychain.]'
        '--disable-keychain[Search credentials in macOS keychain.]'
        '--resolver-fingerprint-checking:resolver-fingerprint-checking:'
        '--resolver-signing-entity-checking:resolver-signing-entity-checking:'
        '--enable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--disable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--enable-prefetching'
        '--disable-prefetching'
        '(--force-resolved-versions --disable-automatic-resolution --only-use-versions-from-resolved-file)'{--force-resolved-versions,--disable-automatic-resolution,--only-use-versions-from-resolved-file}'[Only use versions from the Package.resolved file and fail resolution if it is out-of-date.]'
        '--skip-update[Skip updating dependencies from their remote during a resolution.]'
        '--disable-scm-to-registry-transformation[Disable source control to registry transformation.]'
        '--use-registry-identity-for-scm[Look up source control dependencies in the registry and use their registry identity when possible to help deduplicate across the two origins.]'
        '--replace-scm-with-registry[Look up source control dependencies in the registry and use the registry to retrieve them instead of source control when possible.]'
        '--default-registry-url[Default registry URL to use, instead of the registries.json configuration file.]:default-registry-url:'
        '(--configuration -c)'{--configuration,-c}'[Build with configuration]:configuration:{__swift_complete "${___configuration[@]}"}'
        '*-Xcc[Pass flag through to all C compiler invocations.]:Xcc:'
        '*-Xswiftc[Pass flag through to all Swift compiler invocations.]:Xswiftc:'
        '*-Xlinker[Pass flag through to all linker invocations.]:Xlinker:'
        '*-Xcxx[Pass flag through to all C++ compiler invocations.]:Xcxx:'
        '--triple:triple:'
        '--sdk:sdk:_files -/'
        '--toolchain:toolchain:_files -/'
        '--swift-sdk[Filter for selecting a specific Swift SDK to build with.]:swift-sdk:'
        '*--sanitize[Turn on runtime checks for erroneous behavior.]:sanitize:{__swift_complete "${___sanitize[@]}"}'
        '--auto-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-index-store[Enable or disable indexing-while-building feature.]'
        '--disable-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-parseable-module-interfaces'
        '(--jobs -j)'{--jobs,-j}'[The number of jobs to spawn in parallel during the build process.]:jobs:'
        '--use-integrated-swift-driver'
        '--explicit-target-dependency-import-check[A flag that indicates this build should check whether targets only import their explicitly-declared dependencies.]:explicit-target-dependency-import-check:{__swift_complete "${___explicit_target_dependency_import_check[@]}"}'
        '--build-system[Specify the build system to use.]:build-system:{__swift_complete "${___build_system[@]}"}'
        '-debug-info-format[The Debug Information Format to use.]:debug-info-format:{__swift_complete "${__debug_info_format[@]}"}'
        '--enable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-local-rpath[Disable adding $ORIGIN/@loader_path to the rpath by default.]'
        '--traits[Enables the passed traits of the package. Multiple traits can be specified by providing a comma separated list e.g. `--traits Trait1,Trait2`. When enabling specific traits the defaults traits need to explictily enabled as well by passing `defaults` to this command.]:traits:'
        '--enable-all-traits[Enables all traits of the package.]'
        '--disable-default-traits[Disables all default traits of the package.]'
        '--revision[The revision to edit.]:revision:'
        '--branch[The branch to create.]:branch:'
        '--path[Create or use the checkout at this path.]:path:_files -/'
        ':package-identity:'
        '--version[Show the version.]'
        '(-help -h --help)'{-help,-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_swift_package_unedit() {
    local -i ret=1
    local -ar ___configuration=('debug' 'release')
    local -ar ___sanitize=('address' 'thread' 'undefined' 'scudo' 'fuzzer')
    local -ar ___explicit_target_dependency_import_check=('none' 'warn' 'error')
    local -ar ___build_system=('native' 'swiftbuild' 'xcode')
    local -ar __debug_info_format=('dwarf' 'codeview' 'none')
    local -ar arg_specs=(
        '--package-path[Specify the package path to operate on (default current directory). This changes the working directory before any other operation.]:package-path:_files -/'
        '--cache-path[Specify the shared cache directory path.]:cache-path:_files -/'
        '--config-path[Specify the shared configuration directory path.]:config-path:_files -/'
        '--security-path[Specify the shared security directory path.]:security-path:_files -/'
        '--scratch-path[Specify a custom scratch directory path. (default .build)]:scratch-path:_files -/'
        '--swift-sdks-path[Path to the directory containing installed Swift SDKs.]:swift-sdks-path:_files -/'
        '*--toolset[Specify a toolset JSON file to use when building for the target platform. Use the option multiple times to specify more than one toolset. Toolsets will be merged in the order they'\''re specified into a single final toolset for the current build.]:toolset:_files -g '\''*..json'\'''
        '*--pkg-config-path[Specify alternative path to search for pkg-config `.pc` files. Use the option multiple times to
        specify more than one path.]:pkg-config-path:_files -/'
        '--enable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--disable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--enable-build-manifest-caching'
        '--disable-build-manifest-caching'
        '--manifest-cache[Caching mode of Package.swift manifests. Valid values are\: (shared\: shared cache, local\: package'\''s build directory, none\: disabled)]:manifest-cache:'
        '--enable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '--disable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '(--verbose -v)'{--verbose,-v}'[Increase verbosity to include informational output.]'
        '(--very-verbose --vv)'{--very-verbose,--vv}'[Increase verbosity to include debug output.]'
        '(--quiet -q)'{--quiet,-q}'[Decrease verbosity to only include error output.]'
        '--color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--no-color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--disable-sandbox[Disable using the sandbox when executing subprocesses.]'
        '--netrc[Use netrc file even in cases where other credential stores are preferred.]'
        '--enable-netrc[Load credentials from a netrc file.]'
        '--disable-netrc[Load credentials from a netrc file.]'
        '--netrc-file[Specify the netrc file path.]:netrc-file:_files'
        '--enable-keychain[Search credentials in macOS keychain.]'
        '--disable-keychain[Search credentials in macOS keychain.]'
        '--resolver-fingerprint-checking:resolver-fingerprint-checking:'
        '--resolver-signing-entity-checking:resolver-signing-entity-checking:'
        '--enable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--disable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--enable-prefetching'
        '--disable-prefetching'
        '(--force-resolved-versions --disable-automatic-resolution --only-use-versions-from-resolved-file)'{--force-resolved-versions,--disable-automatic-resolution,--only-use-versions-from-resolved-file}'[Only use versions from the Package.resolved file and fail resolution if it is out-of-date.]'
        '--skip-update[Skip updating dependencies from their remote during a resolution.]'
        '--disable-scm-to-registry-transformation[Disable source control to registry transformation.]'
        '--use-registry-identity-for-scm[Look up source control dependencies in the registry and use their registry identity when possible to help deduplicate across the two origins.]'
        '--replace-scm-with-registry[Look up source control dependencies in the registry and use the registry to retrieve them instead of source control when possible.]'
        '--default-registry-url[Default registry URL to use, instead of the registries.json configuration file.]:default-registry-url:'
        '(--configuration -c)'{--configuration,-c}'[Build with configuration]:configuration:{__swift_complete "${___configuration[@]}"}'
        '*-Xcc[Pass flag through to all C compiler invocations.]:Xcc:'
        '*-Xswiftc[Pass flag through to all Swift compiler invocations.]:Xswiftc:'
        '*-Xlinker[Pass flag through to all linker invocations.]:Xlinker:'
        '*-Xcxx[Pass flag through to all C++ compiler invocations.]:Xcxx:'
        '--triple:triple:'
        '--sdk:sdk:_files -/'
        '--toolchain:toolchain:_files -/'
        '--swift-sdk[Filter for selecting a specific Swift SDK to build with.]:swift-sdk:'
        '*--sanitize[Turn on runtime checks for erroneous behavior.]:sanitize:{__swift_complete "${___sanitize[@]}"}'
        '--auto-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-index-store[Enable or disable indexing-while-building feature.]'
        '--disable-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-parseable-module-interfaces'
        '(--jobs -j)'{--jobs,-j}'[The number of jobs to spawn in parallel during the build process.]:jobs:'
        '--use-integrated-swift-driver'
        '--explicit-target-dependency-import-check[A flag that indicates this build should check whether targets only import their explicitly-declared dependencies.]:explicit-target-dependency-import-check:{__swift_complete "${___explicit_target_dependency_import_check[@]}"}'
        '--build-system[Specify the build system to use.]:build-system:{__swift_complete "${___build_system[@]}"}'
        '-debug-info-format[The Debug Information Format to use.]:debug-info-format:{__swift_complete "${__debug_info_format[@]}"}'
        '--enable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-local-rpath[Disable adding $ORIGIN/@loader_path to the rpath by default.]'
        '--traits[Enables the passed traits of the package. Multiple traits can be specified by providing a comma separated list e.g. `--traits Trait1,Trait2`. When enabling specific traits the defaults traits need to explictily enabled as well by passing `defaults` to this command.]:traits:'
        '--enable-all-traits[Enables all traits of the package.]'
        '--disable-default-traits[Disables all default traits of the package.]'
        '--force[Unedit the package even if it has uncommitted and unpushed changes.]'
        ':package-identity:'
        '--version[Show the version.]'
        '(-help -h --help)'{-help,-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_swift_package_config() {
    local -i ret=1
    local -ar arg_specs=(
        '--version[Show the version.]'
        '(-help -h --help)'{-help,-h,--help}'[Show help information.]'
        '(-): :->command'
        '(-)*:: :->arg'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0
    case "${state}" in
    command)
        local -ar subcommands=(
            'set-mirror:Set a mirror for a dependency.'
            'unset-mirror:Remove an existing mirror.'
            'get-mirror:Print mirror configuration for the given package dependency.'
        )
        _describe -V subcommand subcommands
        ;;
    arg)
        case "${words[1]}" in
        set-mirror|unset-mirror|get-mirror)
            "_swift_package_config_${words[1]}"
            ;;
        esac
        ;;
    esac

    return "${ret}"
}

_swift_package_config_set-mirror() {
    local -i ret=1
    local -ar ___configuration=('debug' 'release')
    local -ar ___sanitize=('address' 'thread' 'undefined' 'scudo' 'fuzzer')
    local -ar ___explicit_target_dependency_import_check=('none' 'warn' 'error')
    local -ar ___build_system=('native' 'swiftbuild' 'xcode')
    local -ar __debug_info_format=('dwarf' 'codeview' 'none')
    local -ar arg_specs=(
        '--package-path[Specify the package path to operate on (default current directory). This changes the working directory before any other operation.]:package-path:_files -/'
        '--cache-path[Specify the shared cache directory path.]:cache-path:_files -/'
        '--config-path[Specify the shared configuration directory path.]:config-path:_files -/'
        '--security-path[Specify the shared security directory path.]:security-path:_files -/'
        '--scratch-path[Specify a custom scratch directory path. (default .build)]:scratch-path:_files -/'
        '--swift-sdks-path[Path to the directory containing installed Swift SDKs.]:swift-sdks-path:_files -/'
        '*--toolset[Specify a toolset JSON file to use when building for the target platform. Use the option multiple times to specify more than one toolset. Toolsets will be merged in the order they'\''re specified into a single final toolset for the current build.]:toolset:_files -g '\''*..json'\'''
        '*--pkg-config-path[Specify alternative path to search for pkg-config `.pc` files. Use the option multiple times to
        specify more than one path.]:pkg-config-path:_files -/'
        '--enable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--disable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--enable-build-manifest-caching'
        '--disable-build-manifest-caching'
        '--manifest-cache[Caching mode of Package.swift manifests. Valid values are\: (shared\: shared cache, local\: package'\''s build directory, none\: disabled)]:manifest-cache:'
        '--enable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '--disable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '(--verbose -v)'{--verbose,-v}'[Increase verbosity to include informational output.]'
        '(--very-verbose --vv)'{--very-verbose,--vv}'[Increase verbosity to include debug output.]'
        '(--quiet -q)'{--quiet,-q}'[Decrease verbosity to only include error output.]'
        '--color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--no-color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--disable-sandbox[Disable using the sandbox when executing subprocesses.]'
        '--netrc[Use netrc file even in cases where other credential stores are preferred.]'
        '--enable-netrc[Load credentials from a netrc file.]'
        '--disable-netrc[Load credentials from a netrc file.]'
        '--netrc-file[Specify the netrc file path.]:netrc-file:_files'
        '--enable-keychain[Search credentials in macOS keychain.]'
        '--disable-keychain[Search credentials in macOS keychain.]'
        '--resolver-fingerprint-checking:resolver-fingerprint-checking:'
        '--resolver-signing-entity-checking:resolver-signing-entity-checking:'
        '--enable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--disable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--enable-prefetching'
        '--disable-prefetching'
        '(--force-resolved-versions --disable-automatic-resolution --only-use-versions-from-resolved-file)'{--force-resolved-versions,--disable-automatic-resolution,--only-use-versions-from-resolved-file}'[Only use versions from the Package.resolved file and fail resolution if it is out-of-date.]'
        '--skip-update[Skip updating dependencies from their remote during a resolution.]'
        '--disable-scm-to-registry-transformation[Disable source control to registry transformation.]'
        '--use-registry-identity-for-scm[Look up source control dependencies in the registry and use their registry identity when possible to help deduplicate across the two origins.]'
        '--replace-scm-with-registry[Look up source control dependencies in the registry and use the registry to retrieve them instead of source control when possible.]'
        '--default-registry-url[Default registry URL to use, instead of the registries.json configuration file.]:default-registry-url:'
        '(--configuration -c)'{--configuration,-c}'[Build with configuration]:configuration:{__swift_complete "${___configuration[@]}"}'
        '*-Xcc[Pass flag through to all C compiler invocations.]:Xcc:'
        '*-Xswiftc[Pass flag through to all Swift compiler invocations.]:Xswiftc:'
        '*-Xlinker[Pass flag through to all linker invocations.]:Xlinker:'
        '*-Xcxx[Pass flag through to all C++ compiler invocations.]:Xcxx:'
        '--triple:triple:'
        '--sdk:sdk:_files -/'
        '--toolchain:toolchain:_files -/'
        '--swift-sdk[Filter for selecting a specific Swift SDK to build with.]:swift-sdk:'
        '*--sanitize[Turn on runtime checks for erroneous behavior.]:sanitize:{__swift_complete "${___sanitize[@]}"}'
        '--auto-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-index-store[Enable or disable indexing-while-building feature.]'
        '--disable-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-parseable-module-interfaces'
        '(--jobs -j)'{--jobs,-j}'[The number of jobs to spawn in parallel during the build process.]:jobs:'
        '--use-integrated-swift-driver'
        '--explicit-target-dependency-import-check[A flag that indicates this build should check whether targets only import their explicitly-declared dependencies.]:explicit-target-dependency-import-check:{__swift_complete "${___explicit_target_dependency_import_check[@]}"}'
        '--build-system[Specify the build system to use.]:build-system:{__swift_complete "${___build_system[@]}"}'
        '-debug-info-format[The Debug Information Format to use.]:debug-info-format:{__swift_complete "${__debug_info_format[@]}"}'
        '--enable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-local-rpath[Disable adding $ORIGIN/@loader_path to the rpath by default.]'
        '--traits[Enables the passed traits of the package. Multiple traits can be specified by providing a comma separated list e.g. `--traits Trait1,Trait2`. When enabling specific traits the defaults traits need to explictily enabled as well by passing `defaults` to this command.]:traits:'
        '--enable-all-traits[Enables all traits of the package.]'
        '--disable-default-traits[Disables all default traits of the package.]'
        '--original[The original url or identity.]:original:'
        '--mirror[The mirror url or identity.]:mirror:'
        '--version[Show the version.]'
        '(-help -h --help)'{-help,-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_swift_package_config_unset-mirror() {
    local -i ret=1
    local -ar ___configuration=('debug' 'release')
    local -ar ___sanitize=('address' 'thread' 'undefined' 'scudo' 'fuzzer')
    local -ar ___explicit_target_dependency_import_check=('none' 'warn' 'error')
    local -ar ___build_system=('native' 'swiftbuild' 'xcode')
    local -ar __debug_info_format=('dwarf' 'codeview' 'none')
    local -ar arg_specs=(
        '--package-path[Specify the package path to operate on (default current directory). This changes the working directory before any other operation.]:package-path:_files -/'
        '--cache-path[Specify the shared cache directory path.]:cache-path:_files -/'
        '--config-path[Specify the shared configuration directory path.]:config-path:_files -/'
        '--security-path[Specify the shared security directory path.]:security-path:_files -/'
        '--scratch-path[Specify a custom scratch directory path. (default .build)]:scratch-path:_files -/'
        '--swift-sdks-path[Path to the directory containing installed Swift SDKs.]:swift-sdks-path:_files -/'
        '*--toolset[Specify a toolset JSON file to use when building for the target platform. Use the option multiple times to specify more than one toolset. Toolsets will be merged in the order they'\''re specified into a single final toolset for the current build.]:toolset:_files -g '\''*..json'\'''
        '*--pkg-config-path[Specify alternative path to search for pkg-config `.pc` files. Use the option multiple times to
        specify more than one path.]:pkg-config-path:_files -/'
        '--enable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--disable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--enable-build-manifest-caching'
        '--disable-build-manifest-caching'
        '--manifest-cache[Caching mode of Package.swift manifests. Valid values are\: (shared\: shared cache, local\: package'\''s build directory, none\: disabled)]:manifest-cache:'
        '--enable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '--disable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '(--verbose -v)'{--verbose,-v}'[Increase verbosity to include informational output.]'
        '(--very-verbose --vv)'{--very-verbose,--vv}'[Increase verbosity to include debug output.]'
        '(--quiet -q)'{--quiet,-q}'[Decrease verbosity to only include error output.]'
        '--color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--no-color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--disable-sandbox[Disable using the sandbox when executing subprocesses.]'
        '--netrc[Use netrc file even in cases where other credential stores are preferred.]'
        '--enable-netrc[Load credentials from a netrc file.]'
        '--disable-netrc[Load credentials from a netrc file.]'
        '--netrc-file[Specify the netrc file path.]:netrc-file:_files'
        '--enable-keychain[Search credentials in macOS keychain.]'
        '--disable-keychain[Search credentials in macOS keychain.]'
        '--resolver-fingerprint-checking:resolver-fingerprint-checking:'
        '--resolver-signing-entity-checking:resolver-signing-entity-checking:'
        '--enable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--disable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--enable-prefetching'
        '--disable-prefetching'
        '(--force-resolved-versions --disable-automatic-resolution --only-use-versions-from-resolved-file)'{--force-resolved-versions,--disable-automatic-resolution,--only-use-versions-from-resolved-file}'[Only use versions from the Package.resolved file and fail resolution if it is out-of-date.]'
        '--skip-update[Skip updating dependencies from their remote during a resolution.]'
        '--disable-scm-to-registry-transformation[Disable source control to registry transformation.]'
        '--use-registry-identity-for-scm[Look up source control dependencies in the registry and use their registry identity when possible to help deduplicate across the two origins.]'
        '--replace-scm-with-registry[Look up source control dependencies in the registry and use the registry to retrieve them instead of source control when possible.]'
        '--default-registry-url[Default registry URL to use, instead of the registries.json configuration file.]:default-registry-url:'
        '(--configuration -c)'{--configuration,-c}'[Build with configuration]:configuration:{__swift_complete "${___configuration[@]}"}'
        '*-Xcc[Pass flag through to all C compiler invocations.]:Xcc:'
        '*-Xswiftc[Pass flag through to all Swift compiler invocations.]:Xswiftc:'
        '*-Xlinker[Pass flag through to all linker invocations.]:Xlinker:'
        '*-Xcxx[Pass flag through to all C++ compiler invocations.]:Xcxx:'
        '--triple:triple:'
        '--sdk:sdk:_files -/'
        '--toolchain:toolchain:_files -/'
        '--swift-sdk[Filter for selecting a specific Swift SDK to build with.]:swift-sdk:'
        '*--sanitize[Turn on runtime checks for erroneous behavior.]:sanitize:{__swift_complete "${___sanitize[@]}"}'
        '--auto-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-index-store[Enable or disable indexing-while-building feature.]'
        '--disable-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-parseable-module-interfaces'
        '(--jobs -j)'{--jobs,-j}'[The number of jobs to spawn in parallel during the build process.]:jobs:'
        '--use-integrated-swift-driver'
        '--explicit-target-dependency-import-check[A flag that indicates this build should check whether targets only import their explicitly-declared dependencies.]:explicit-target-dependency-import-check:{__swift_complete "${___explicit_target_dependency_import_check[@]}"}'
        '--build-system[Specify the build system to use.]:build-system:{__swift_complete "${___build_system[@]}"}'
        '-debug-info-format[The Debug Information Format to use.]:debug-info-format:{__swift_complete "${__debug_info_format[@]}"}'
        '--enable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-local-rpath[Disable adding $ORIGIN/@loader_path to the rpath by default.]'
        '--traits[Enables the passed traits of the package. Multiple traits can be specified by providing a comma separated list e.g. `--traits Trait1,Trait2`. When enabling specific traits the defaults traits need to explictily enabled as well by passing `defaults` to this command.]:traits:'
        '--enable-all-traits[Enables all traits of the package.]'
        '--disable-default-traits[Disables all default traits of the package.]'
        '--original[The original url or identity.]:original:'
        '--mirror[The mirror url or identity.]:mirror:'
        '--version[Show the version.]'
        '(-help -h --help)'{-help,-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_swift_package_config_get-mirror() {
    local -i ret=1
    local -ar ___configuration=('debug' 'release')
    local -ar ___sanitize=('address' 'thread' 'undefined' 'scudo' 'fuzzer')
    local -ar ___explicit_target_dependency_import_check=('none' 'warn' 'error')
    local -ar ___build_system=('native' 'swiftbuild' 'xcode')
    local -ar __debug_info_format=('dwarf' 'codeview' 'none')
    local -ar arg_specs=(
        '--package-path[Specify the package path to operate on (default current directory). This changes the working directory before any other operation.]:package-path:_files -/'
        '--cache-path[Specify the shared cache directory path.]:cache-path:_files -/'
        '--config-path[Specify the shared configuration directory path.]:config-path:_files -/'
        '--security-path[Specify the shared security directory path.]:security-path:_files -/'
        '--scratch-path[Specify a custom scratch directory path. (default .build)]:scratch-path:_files -/'
        '--swift-sdks-path[Path to the directory containing installed Swift SDKs.]:swift-sdks-path:_files -/'
        '*--toolset[Specify a toolset JSON file to use when building for the target platform. Use the option multiple times to specify more than one toolset. Toolsets will be merged in the order they'\''re specified into a single final toolset for the current build.]:toolset:_files -g '\''*..json'\'''
        '*--pkg-config-path[Specify alternative path to search for pkg-config `.pc` files. Use the option multiple times to
        specify more than one path.]:pkg-config-path:_files -/'
        '--enable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--disable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--enable-build-manifest-caching'
        '--disable-build-manifest-caching'
        '--manifest-cache[Caching mode of Package.swift manifests. Valid values are\: (shared\: shared cache, local\: package'\''s build directory, none\: disabled)]:manifest-cache:'
        '--enable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '--disable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '(--verbose -v)'{--verbose,-v}'[Increase verbosity to include informational output.]'
        '(--very-verbose --vv)'{--very-verbose,--vv}'[Increase verbosity to include debug output.]'
        '(--quiet -q)'{--quiet,-q}'[Decrease verbosity to only include error output.]'
        '--color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--no-color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--disable-sandbox[Disable using the sandbox when executing subprocesses.]'
        '--netrc[Use netrc file even in cases where other credential stores are preferred.]'
        '--enable-netrc[Load credentials from a netrc file.]'
        '--disable-netrc[Load credentials from a netrc file.]'
        '--netrc-file[Specify the netrc file path.]:netrc-file:_files'
        '--enable-keychain[Search credentials in macOS keychain.]'
        '--disable-keychain[Search credentials in macOS keychain.]'
        '--resolver-fingerprint-checking:resolver-fingerprint-checking:'
        '--resolver-signing-entity-checking:resolver-signing-entity-checking:'
        '--enable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--disable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--enable-prefetching'
        '--disable-prefetching'
        '(--force-resolved-versions --disable-automatic-resolution --only-use-versions-from-resolved-file)'{--force-resolved-versions,--disable-automatic-resolution,--only-use-versions-from-resolved-file}'[Only use versions from the Package.resolved file and fail resolution if it is out-of-date.]'
        '--skip-update[Skip updating dependencies from their remote during a resolution.]'
        '--disable-scm-to-registry-transformation[Disable source control to registry transformation.]'
        '--use-registry-identity-for-scm[Look up source control dependencies in the registry and use their registry identity when possible to help deduplicate across the two origins.]'
        '--replace-scm-with-registry[Look up source control dependencies in the registry and use the registry to retrieve them instead of source control when possible.]'
        '--default-registry-url[Default registry URL to use, instead of the registries.json configuration file.]:default-registry-url:'
        '(--configuration -c)'{--configuration,-c}'[Build with configuration]:configuration:{__swift_complete "${___configuration[@]}"}'
        '*-Xcc[Pass flag through to all C compiler invocations.]:Xcc:'
        '*-Xswiftc[Pass flag through to all Swift compiler invocations.]:Xswiftc:'
        '*-Xlinker[Pass flag through to all linker invocations.]:Xlinker:'
        '*-Xcxx[Pass flag through to all C++ compiler invocations.]:Xcxx:'
        '--triple:triple:'
        '--sdk:sdk:_files -/'
        '--toolchain:toolchain:_files -/'
        '--swift-sdk[Filter for selecting a specific Swift SDK to build with.]:swift-sdk:'
        '*--sanitize[Turn on runtime checks for erroneous behavior.]:sanitize:{__swift_complete "${___sanitize[@]}"}'
        '--auto-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-index-store[Enable or disable indexing-while-building feature.]'
        '--disable-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-parseable-module-interfaces'
        '(--jobs -j)'{--jobs,-j}'[The number of jobs to spawn in parallel during the build process.]:jobs:'
        '--use-integrated-swift-driver'
        '--explicit-target-dependency-import-check[A flag that indicates this build should check whether targets only import their explicitly-declared dependencies.]:explicit-target-dependency-import-check:{__swift_complete "${___explicit_target_dependency_import_check[@]}"}'
        '--build-system[Specify the build system to use.]:build-system:{__swift_complete "${___build_system[@]}"}'
        '-debug-info-format[The Debug Information Format to use.]:debug-info-format:{__swift_complete "${__debug_info_format[@]}"}'
        '--enable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-local-rpath[Disable adding $ORIGIN/@loader_path to the rpath by default.]'
        '--traits[Enables the passed traits of the package. Multiple traits can be specified by providing a comma separated list e.g. `--traits Trait1,Trait2`. When enabling specific traits the defaults traits need to explictily enabled as well by passing `defaults` to this command.]:traits:'
        '--enable-all-traits[Enables all traits of the package.]'
        '--disable-default-traits[Disables all default traits of the package.]'
        '--original[The original url or identity.]:original:'
        '--version[Show the version.]'
        '(-help -h --help)'{-help,-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_swift_package_resolve() {
    local -i ret=1
    local -ar ___configuration=('debug' 'release')
    local -ar ___sanitize=('address' 'thread' 'undefined' 'scudo' 'fuzzer')
    local -ar ___explicit_target_dependency_import_check=('none' 'warn' 'error')
    local -ar ___build_system=('native' 'swiftbuild' 'xcode')
    local -ar __debug_info_format=('dwarf' 'codeview' 'none')
    local -ar arg_specs=(
        '--package-path[Specify the package path to operate on (default current directory). This changes the working directory before any other operation.]:package-path:_files -/'
        '--cache-path[Specify the shared cache directory path.]:cache-path:_files -/'
        '--config-path[Specify the shared configuration directory path.]:config-path:_files -/'
        '--security-path[Specify the shared security directory path.]:security-path:_files -/'
        '--scratch-path[Specify a custom scratch directory path. (default .build)]:scratch-path:_files -/'
        '--swift-sdks-path[Path to the directory containing installed Swift SDKs.]:swift-sdks-path:_files -/'
        '*--toolset[Specify a toolset JSON file to use when building for the target platform. Use the option multiple times to specify more than one toolset. Toolsets will be merged in the order they'\''re specified into a single final toolset for the current build.]:toolset:_files -g '\''*..json'\'''
        '*--pkg-config-path[Specify alternative path to search for pkg-config `.pc` files. Use the option multiple times to
        specify more than one path.]:pkg-config-path:_files -/'
        '--enable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--disable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--enable-build-manifest-caching'
        '--disable-build-manifest-caching'
        '--manifest-cache[Caching mode of Package.swift manifests. Valid values are\: (shared\: shared cache, local\: package'\''s build directory, none\: disabled)]:manifest-cache:'
        '--enable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '--disable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '(--verbose -v)'{--verbose,-v}'[Increase verbosity to include informational output.]'
        '(--very-verbose --vv)'{--very-verbose,--vv}'[Increase verbosity to include debug output.]'
        '(--quiet -q)'{--quiet,-q}'[Decrease verbosity to only include error output.]'
        '--color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--no-color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--disable-sandbox[Disable using the sandbox when executing subprocesses.]'
        '--netrc[Use netrc file even in cases where other credential stores are preferred.]'
        '--enable-netrc[Load credentials from a netrc file.]'
        '--disable-netrc[Load credentials from a netrc file.]'
        '--netrc-file[Specify the netrc file path.]:netrc-file:_files'
        '--enable-keychain[Search credentials in macOS keychain.]'
        '--disable-keychain[Search credentials in macOS keychain.]'
        '--resolver-fingerprint-checking:resolver-fingerprint-checking:'
        '--resolver-signing-entity-checking:resolver-signing-entity-checking:'
        '--enable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--disable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--enable-prefetching'
        '--disable-prefetching'
        '(--force-resolved-versions --disable-automatic-resolution --only-use-versions-from-resolved-file)'{--force-resolved-versions,--disable-automatic-resolution,--only-use-versions-from-resolved-file}'[Only use versions from the Package.resolved file and fail resolution if it is out-of-date.]'
        '--skip-update[Skip updating dependencies from their remote during a resolution.]'
        '--disable-scm-to-registry-transformation[Disable source control to registry transformation.]'
        '--use-registry-identity-for-scm[Look up source control dependencies in the registry and use their registry identity when possible to help deduplicate across the two origins.]'
        '--replace-scm-with-registry[Look up source control dependencies in the registry and use the registry to retrieve them instead of source control when possible.]'
        '--default-registry-url[Default registry URL to use, instead of the registries.json configuration file.]:default-registry-url:'
        '(--configuration -c)'{--configuration,-c}'[Build with configuration]:configuration:{__swift_complete "${___configuration[@]}"}'
        '*-Xcc[Pass flag through to all C compiler invocations.]:Xcc:'
        '*-Xswiftc[Pass flag through to all Swift compiler invocations.]:Xswiftc:'
        '*-Xlinker[Pass flag through to all linker invocations.]:Xlinker:'
        '*-Xcxx[Pass flag through to all C++ compiler invocations.]:Xcxx:'
        '--triple:triple:'
        '--sdk:sdk:_files -/'
        '--toolchain:toolchain:_files -/'
        '--swift-sdk[Filter for selecting a specific Swift SDK to build with.]:swift-sdk:'
        '*--sanitize[Turn on runtime checks for erroneous behavior.]:sanitize:{__swift_complete "${___sanitize[@]}"}'
        '--auto-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-index-store[Enable or disable indexing-while-building feature.]'
        '--disable-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-parseable-module-interfaces'
        '(--jobs -j)'{--jobs,-j}'[The number of jobs to spawn in parallel during the build process.]:jobs:'
        '--use-integrated-swift-driver'
        '--explicit-target-dependency-import-check[A flag that indicates this build should check whether targets only import their explicitly-declared dependencies.]:explicit-target-dependency-import-check:{__swift_complete "${___explicit_target_dependency_import_check[@]}"}'
        '--build-system[Specify the build system to use.]:build-system:{__swift_complete "${___build_system[@]}"}'
        '-debug-info-format[The Debug Information Format to use.]:debug-info-format:{__swift_complete "${__debug_info_format[@]}"}'
        '--enable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-local-rpath[Disable adding $ORIGIN/@loader_path to the rpath by default.]'
        '--traits[Enables the passed traits of the package. Multiple traits can be specified by providing a comma separated list e.g. `--traits Trait1,Trait2`. When enabling specific traits the defaults traits need to explictily enabled as well by passing `defaults` to this command.]:traits:'
        '--enable-all-traits[Enables all traits of the package.]'
        '--disable-default-traits[Disables all default traits of the package.]'
        '--version[The version to resolve at.]:version:'
        '--branch[The branch to resolve at.]:branch:'
        '--revision[The revision to resolve at.]:revision:'
        ':package-name:'
        '--version[Show the version.]'
        '(-help -h --help)'{-help,-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_swift_package_show-dependencies() {
    local -i ret=1
    local -ar ___configuration=('debug' 'release')
    local -ar ___sanitize=('address' 'thread' 'undefined' 'scudo' 'fuzzer')
    local -ar ___explicit_target_dependency_import_check=('none' 'warn' 'error')
    local -ar ___build_system=('native' 'swiftbuild' 'xcode')
    local -ar __debug_info_format=('dwarf' 'codeview' 'none')
    local -ar ___format=('text' 'dot' 'json' 'flatlist')
    local -ar arg_specs=(
        '--package-path[Specify the package path to operate on (default current directory). This changes the working directory before any other operation.]:package-path:_files -/'
        '--cache-path[Specify the shared cache directory path.]:cache-path:_files -/'
        '--config-path[Specify the shared configuration directory path.]:config-path:_files -/'
        '--security-path[Specify the shared security directory path.]:security-path:_files -/'
        '--scratch-path[Specify a custom scratch directory path. (default .build)]:scratch-path:_files -/'
        '--swift-sdks-path[Path to the directory containing installed Swift SDKs.]:swift-sdks-path:_files -/'
        '*--toolset[Specify a toolset JSON file to use when building for the target platform. Use the option multiple times to specify more than one toolset. Toolsets will be merged in the order they'\''re specified into a single final toolset for the current build.]:toolset:_files -g '\''*..json'\'''
        '*--pkg-config-path[Specify alternative path to search for pkg-config `.pc` files. Use the option multiple times to
        specify more than one path.]:pkg-config-path:_files -/'
        '--enable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--disable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--enable-build-manifest-caching'
        '--disable-build-manifest-caching'
        '--manifest-cache[Caching mode of Package.swift manifests. Valid values are\: (shared\: shared cache, local\: package'\''s build directory, none\: disabled)]:manifest-cache:'
        '--enable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '--disable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '(--verbose -v)'{--verbose,-v}'[Increase verbosity to include informational output.]'
        '(--very-verbose --vv)'{--very-verbose,--vv}'[Increase verbosity to include debug output.]'
        '(--quiet -q)'{--quiet,-q}'[Decrease verbosity to only include error output.]'
        '--color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--no-color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--disable-sandbox[Disable using the sandbox when executing subprocesses.]'
        '--netrc[Use netrc file even in cases where other credential stores are preferred.]'
        '--enable-netrc[Load credentials from a netrc file.]'
        '--disable-netrc[Load credentials from a netrc file.]'
        '--netrc-file[Specify the netrc file path.]:netrc-file:_files'
        '--enable-keychain[Search credentials in macOS keychain.]'
        '--disable-keychain[Search credentials in macOS keychain.]'
        '--resolver-fingerprint-checking:resolver-fingerprint-checking:'
        '--resolver-signing-entity-checking:resolver-signing-entity-checking:'
        '--enable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--disable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--enable-prefetching'
        '--disable-prefetching'
        '(--force-resolved-versions --disable-automatic-resolution --only-use-versions-from-resolved-file)'{--force-resolved-versions,--disable-automatic-resolution,--only-use-versions-from-resolved-file}'[Only use versions from the Package.resolved file and fail resolution if it is out-of-date.]'
        '--skip-update[Skip updating dependencies from their remote during a resolution.]'
        '--disable-scm-to-registry-transformation[Disable source control to registry transformation.]'
        '--use-registry-identity-for-scm[Look up source control dependencies in the registry and use their registry identity when possible to help deduplicate across the two origins.]'
        '--replace-scm-with-registry[Look up source control dependencies in the registry and use the registry to retrieve them instead of source control when possible.]'
        '--default-registry-url[Default registry URL to use, instead of the registries.json configuration file.]:default-registry-url:'
        '(--configuration -c)'{--configuration,-c}'[Build with configuration]:configuration:{__swift_complete "${___configuration[@]}"}'
        '*-Xcc[Pass flag through to all C compiler invocations.]:Xcc:'
        '*-Xswiftc[Pass flag through to all Swift compiler invocations.]:Xswiftc:'
        '*-Xlinker[Pass flag through to all linker invocations.]:Xlinker:'
        '*-Xcxx[Pass flag through to all C++ compiler invocations.]:Xcxx:'
        '--triple:triple:'
        '--sdk:sdk:_files -/'
        '--toolchain:toolchain:_files -/'
        '--swift-sdk[Filter for selecting a specific Swift SDK to build with.]:swift-sdk:'
        '*--sanitize[Turn on runtime checks for erroneous behavior.]:sanitize:{__swift_complete "${___sanitize[@]}"}'
        '--auto-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-index-store[Enable or disable indexing-while-building feature.]'
        '--disable-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-parseable-module-interfaces'
        '(--jobs -j)'{--jobs,-j}'[The number of jobs to spawn in parallel during the build process.]:jobs:'
        '--use-integrated-swift-driver'
        '--explicit-target-dependency-import-check[A flag that indicates this build should check whether targets only import their explicitly-declared dependencies.]:explicit-target-dependency-import-check:{__swift_complete "${___explicit_target_dependency_import_check[@]}"}'
        '--build-system[Specify the build system to use.]:build-system:{__swift_complete "${___build_system[@]}"}'
        '-debug-info-format[The Debug Information Format to use.]:debug-info-format:{__swift_complete "${__debug_info_format[@]}"}'
        '--enable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-local-rpath[Disable adding $ORIGIN/@loader_path to the rpath by default.]'
        '--traits[Enables the passed traits of the package. Multiple traits can be specified by providing a comma separated list e.g. `--traits Trait1,Trait2`. When enabling specific traits the defaults traits need to explictily enabled as well by passing `defaults` to this command.]:traits:'
        '--enable-all-traits[Enables all traits of the package.]'
        '--disable-default-traits[Disables all default traits of the package.]'
        '--format[Set the output format.]:format:{__swift_complete "${___format[@]}"}'
        '(--output-path -o)'{--output-path,-o}'[The absolute or relative path to output the resolved dependency graph.]:output-path:_files -/'
        '--version[Show the version.]'
        '(-help -h --help)'{-help,-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_swift_package_show-executables() {
    local -i ret=1
    local -ar ___configuration=('debug' 'release')
    local -ar ___sanitize=('address' 'thread' 'undefined' 'scudo' 'fuzzer')
    local -ar ___explicit_target_dependency_import_check=('none' 'warn' 'error')
    local -ar ___build_system=('native' 'swiftbuild' 'xcode')
    local -ar __debug_info_format=('dwarf' 'codeview' 'none')
    local -ar ___format=('flatlist' 'json')
    local -ar arg_specs=(
        '--package-path[Specify the package path to operate on (default current directory). This changes the working directory before any other operation.]:package-path:_files -/'
        '--cache-path[Specify the shared cache directory path.]:cache-path:_files -/'
        '--config-path[Specify the shared configuration directory path.]:config-path:_files -/'
        '--security-path[Specify the shared security directory path.]:security-path:_files -/'
        '--scratch-path[Specify a custom scratch directory path. (default .build)]:scratch-path:_files -/'
        '--swift-sdks-path[Path to the directory containing installed Swift SDKs.]:swift-sdks-path:_files -/'
        '*--toolset[Specify a toolset JSON file to use when building for the target platform. Use the option multiple times to specify more than one toolset. Toolsets will be merged in the order they'\''re specified into a single final toolset for the current build.]:toolset:_files -g '\''*..json'\'''
        '*--pkg-config-path[Specify alternative path to search for pkg-config `.pc` files. Use the option multiple times to
        specify more than one path.]:pkg-config-path:_files -/'
        '--enable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--disable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--enable-build-manifest-caching'
        '--disable-build-manifest-caching'
        '--manifest-cache[Caching mode of Package.swift manifests. Valid values are\: (shared\: shared cache, local\: package'\''s build directory, none\: disabled)]:manifest-cache:'
        '--enable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '--disable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '(--verbose -v)'{--verbose,-v}'[Increase verbosity to include informational output.]'
        '(--very-verbose --vv)'{--very-verbose,--vv}'[Increase verbosity to include debug output.]'
        '(--quiet -q)'{--quiet,-q}'[Decrease verbosity to only include error output.]'
        '--color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--no-color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--disable-sandbox[Disable using the sandbox when executing subprocesses.]'
        '--netrc[Use netrc file even in cases where other credential stores are preferred.]'
        '--enable-netrc[Load credentials from a netrc file.]'
        '--disable-netrc[Load credentials from a netrc file.]'
        '--netrc-file[Specify the netrc file path.]:netrc-file:_files'
        '--enable-keychain[Search credentials in macOS keychain.]'
        '--disable-keychain[Search credentials in macOS keychain.]'
        '--resolver-fingerprint-checking:resolver-fingerprint-checking:'
        '--resolver-signing-entity-checking:resolver-signing-entity-checking:'
        '--enable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--disable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--enable-prefetching'
        '--disable-prefetching'
        '(--force-resolved-versions --disable-automatic-resolution --only-use-versions-from-resolved-file)'{--force-resolved-versions,--disable-automatic-resolution,--only-use-versions-from-resolved-file}'[Only use versions from the Package.resolved file and fail resolution if it is out-of-date.]'
        '--skip-update[Skip updating dependencies from their remote during a resolution.]'
        '--disable-scm-to-registry-transformation[Disable source control to registry transformation.]'
        '--use-registry-identity-for-scm[Look up source control dependencies in the registry and use their registry identity when possible to help deduplicate across the two origins.]'
        '--replace-scm-with-registry[Look up source control dependencies in the registry and use the registry to retrieve them instead of source control when possible.]'
        '--default-registry-url[Default registry URL to use, instead of the registries.json configuration file.]:default-registry-url:'
        '(--configuration -c)'{--configuration,-c}'[Build with configuration]:configuration:{__swift_complete "${___configuration[@]}"}'
        '*-Xcc[Pass flag through to all C compiler invocations.]:Xcc:'
        '*-Xswiftc[Pass flag through to all Swift compiler invocations.]:Xswiftc:'
        '*-Xlinker[Pass flag through to all linker invocations.]:Xlinker:'
        '*-Xcxx[Pass flag through to all C++ compiler invocations.]:Xcxx:'
        '--triple:triple:'
        '--sdk:sdk:_files -/'
        '--toolchain:toolchain:_files -/'
        '--swift-sdk[Filter for selecting a specific Swift SDK to build with.]:swift-sdk:'
        '*--sanitize[Turn on runtime checks for erroneous behavior.]:sanitize:{__swift_complete "${___sanitize[@]}"}'
        '--auto-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-index-store[Enable or disable indexing-while-building feature.]'
        '--disable-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-parseable-module-interfaces'
        '(--jobs -j)'{--jobs,-j}'[The number of jobs to spawn in parallel during the build process.]:jobs:'
        '--use-integrated-swift-driver'
        '--explicit-target-dependency-import-check[A flag that indicates this build should check whether targets only import their explicitly-declared dependencies.]:explicit-target-dependency-import-check:{__swift_complete "${___explicit_target_dependency_import_check[@]}"}'
        '--build-system[Specify the build system to use.]:build-system:{__swift_complete "${___build_system[@]}"}'
        '-debug-info-format[The Debug Information Format to use.]:debug-info-format:{__swift_complete "${__debug_info_format[@]}"}'
        '--enable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-local-rpath[Disable adding $ORIGIN/@loader_path to the rpath by default.]'
        '--traits[Enables the passed traits of the package. Multiple traits can be specified by providing a comma separated list e.g. `--traits Trait1,Trait2`. When enabling specific traits the defaults traits need to explictily enabled as well by passing `defaults` to this command.]:traits:'
        '--enable-all-traits[Enables all traits of the package.]'
        '--disable-default-traits[Disables all default traits of the package.]'
        '--format[Set the output format.]:format:{__swift_complete "${___format[@]}"}'
        '--version[Show the version.]'
        '(-help -h --help)'{-help,-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_swift_package_show-traits() {
    local -i ret=1
    local -ar ___configuration=('debug' 'release')
    local -ar ___sanitize=('address' 'thread' 'undefined' 'scudo' 'fuzzer')
    local -ar ___explicit_target_dependency_import_check=('none' 'warn' 'error')
    local -ar ___build_system=('native' 'swiftbuild' 'xcode')
    local -ar __debug_info_format=('dwarf' 'codeview' 'none')
    local -ar ___format=('text' 'json')
    local -ar arg_specs=(
        '--package-path[Specify the package path to operate on (default current directory). This changes the working directory before any other operation.]:package-path:_files -/'
        '--cache-path[Specify the shared cache directory path.]:cache-path:_files -/'
        '--config-path[Specify the shared configuration directory path.]:config-path:_files -/'
        '--security-path[Specify the shared security directory path.]:security-path:_files -/'
        '--scratch-path[Specify a custom scratch directory path. (default .build)]:scratch-path:_files -/'
        '--swift-sdks-path[Path to the directory containing installed Swift SDKs.]:swift-sdks-path:_files -/'
        '*--toolset[Specify a toolset JSON file to use when building for the target platform. Use the option multiple times to specify more than one toolset. Toolsets will be merged in the order they'\''re specified into a single final toolset for the current build.]:toolset:_files -g '\''*..json'\'''
        '*--pkg-config-path[Specify alternative path to search for pkg-config `.pc` files. Use the option multiple times to
        specify more than one path.]:pkg-config-path:_files -/'
        '--enable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--disable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--enable-build-manifest-caching'
        '--disable-build-manifest-caching'
        '--manifest-cache[Caching mode of Package.swift manifests. Valid values are\: (shared\: shared cache, local\: package'\''s build directory, none\: disabled)]:manifest-cache:'
        '--enable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '--disable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '(--verbose -v)'{--verbose,-v}'[Increase verbosity to include informational output.]'
        '(--very-verbose --vv)'{--very-verbose,--vv}'[Increase verbosity to include debug output.]'
        '(--quiet -q)'{--quiet,-q}'[Decrease verbosity to only include error output.]'
        '--color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--no-color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--disable-sandbox[Disable using the sandbox when executing subprocesses.]'
        '--netrc[Use netrc file even in cases where other credential stores are preferred.]'
        '--enable-netrc[Load credentials from a netrc file.]'
        '--disable-netrc[Load credentials from a netrc file.]'
        '--netrc-file[Specify the netrc file path.]:netrc-file:_files'
        '--enable-keychain[Search credentials in macOS keychain.]'
        '--disable-keychain[Search credentials in macOS keychain.]'
        '--resolver-fingerprint-checking:resolver-fingerprint-checking:'
        '--resolver-signing-entity-checking:resolver-signing-entity-checking:'
        '--enable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--disable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--enable-prefetching'
        '--disable-prefetching'
        '(--force-resolved-versions --disable-automatic-resolution --only-use-versions-from-resolved-file)'{--force-resolved-versions,--disable-automatic-resolution,--only-use-versions-from-resolved-file}'[Only use versions from the Package.resolved file and fail resolution if it is out-of-date.]'
        '--skip-update[Skip updating dependencies from their remote during a resolution.]'
        '--disable-scm-to-registry-transformation[Disable source control to registry transformation.]'
        '--use-registry-identity-for-scm[Look up source control dependencies in the registry and use their registry identity when possible to help deduplicate across the two origins.]'
        '--replace-scm-with-registry[Look up source control dependencies in the registry and use the registry to retrieve them instead of source control when possible.]'
        '--default-registry-url[Default registry URL to use, instead of the registries.json configuration file.]:default-registry-url:'
        '(--configuration -c)'{--configuration,-c}'[Build with configuration]:configuration:{__swift_complete "${___configuration[@]}"}'
        '*-Xcc[Pass flag through to all C compiler invocations.]:Xcc:'
        '*-Xswiftc[Pass flag through to all Swift compiler invocations.]:Xswiftc:'
        '*-Xlinker[Pass flag through to all linker invocations.]:Xlinker:'
        '*-Xcxx[Pass flag through to all C++ compiler invocations.]:Xcxx:'
        '--triple:triple:'
        '--sdk:sdk:_files -/'
        '--toolchain:toolchain:_files -/'
        '--swift-sdk[Filter for selecting a specific Swift SDK to build with.]:swift-sdk:'
        '*--sanitize[Turn on runtime checks for erroneous behavior.]:sanitize:{__swift_complete "${___sanitize[@]}"}'
        '--auto-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-index-store[Enable or disable indexing-while-building feature.]'
        '--disable-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-parseable-module-interfaces'
        '(--jobs -j)'{--jobs,-j}'[The number of jobs to spawn in parallel during the build process.]:jobs:'
        '--use-integrated-swift-driver'
        '--explicit-target-dependency-import-check[A flag that indicates this build should check whether targets only import their explicitly-declared dependencies.]:explicit-target-dependency-import-check:{__swift_complete "${___explicit_target_dependency_import_check[@]}"}'
        '--build-system[Specify the build system to use.]:build-system:{__swift_complete "${___build_system[@]}"}'
        '-debug-info-format[The Debug Information Format to use.]:debug-info-format:{__swift_complete "${__debug_info_format[@]}"}'
        '--enable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-local-rpath[Disable adding $ORIGIN/@loader_path to the rpath by default.]'
        '--traits[Enables the passed traits of the package. Multiple traits can be specified by providing a comma separated list e.g. `--traits Trait1,Trait2`. When enabling specific traits the defaults traits need to explictily enabled as well by passing `defaults` to this command.]:traits:'
        '--enable-all-traits[Enables all traits of the package.]'
        '--disable-default-traits[Disables all default traits of the package.]'
        '--package-id[Show traits for any package id in the transitive dependencies.]:package-id:'
        '--format[Set the output format.]:format:{__swift_complete "${___format[@]}"}'
        '--version[Show the version.]'
        '(-help -h --help)'{-help,-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_swift_package_tools-version() {
    local -i ret=1
    local -ar ___configuration=('debug' 'release')
    local -ar ___sanitize=('address' 'thread' 'undefined' 'scudo' 'fuzzer')
    local -ar ___explicit_target_dependency_import_check=('none' 'warn' 'error')
    local -ar ___build_system=('native' 'swiftbuild' 'xcode')
    local -ar __debug_info_format=('dwarf' 'codeview' 'none')
    local -ar arg_specs=(
        '--package-path[Specify the package path to operate on (default current directory). This changes the working directory before any other operation.]:package-path:_files -/'
        '--cache-path[Specify the shared cache directory path.]:cache-path:_files -/'
        '--config-path[Specify the shared configuration directory path.]:config-path:_files -/'
        '--security-path[Specify the shared security directory path.]:security-path:_files -/'
        '--scratch-path[Specify a custom scratch directory path. (default .build)]:scratch-path:_files -/'
        '--swift-sdks-path[Path to the directory containing installed Swift SDKs.]:swift-sdks-path:_files -/'
        '*--toolset[Specify a toolset JSON file to use when building for the target platform. Use the option multiple times to specify more than one toolset. Toolsets will be merged in the order they'\''re specified into a single final toolset for the current build.]:toolset:_files -g '\''*..json'\'''
        '*--pkg-config-path[Specify alternative path to search for pkg-config `.pc` files. Use the option multiple times to
        specify more than one path.]:pkg-config-path:_files -/'
        '--enable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--disable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--enable-build-manifest-caching'
        '--disable-build-manifest-caching'
        '--manifest-cache[Caching mode of Package.swift manifests. Valid values are\: (shared\: shared cache, local\: package'\''s build directory, none\: disabled)]:manifest-cache:'
        '--enable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '--disable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '(--verbose -v)'{--verbose,-v}'[Increase verbosity to include informational output.]'
        '(--very-verbose --vv)'{--very-verbose,--vv}'[Increase verbosity to include debug output.]'
        '(--quiet -q)'{--quiet,-q}'[Decrease verbosity to only include error output.]'
        '--color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--no-color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--disable-sandbox[Disable using the sandbox when executing subprocesses.]'
        '--netrc[Use netrc file even in cases where other credential stores are preferred.]'
        '--enable-netrc[Load credentials from a netrc file.]'
        '--disable-netrc[Load credentials from a netrc file.]'
        '--netrc-file[Specify the netrc file path.]:netrc-file:_files'
        '--enable-keychain[Search credentials in macOS keychain.]'
        '--disable-keychain[Search credentials in macOS keychain.]'
        '--resolver-fingerprint-checking:resolver-fingerprint-checking:'
        '--resolver-signing-entity-checking:resolver-signing-entity-checking:'
        '--enable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--disable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--enable-prefetching'
        '--disable-prefetching'
        '(--force-resolved-versions --disable-automatic-resolution --only-use-versions-from-resolved-file)'{--force-resolved-versions,--disable-automatic-resolution,--only-use-versions-from-resolved-file}'[Only use versions from the Package.resolved file and fail resolution if it is out-of-date.]'
        '--skip-update[Skip updating dependencies from their remote during a resolution.]'
        '--disable-scm-to-registry-transformation[Disable source control to registry transformation.]'
        '--use-registry-identity-for-scm[Look up source control dependencies in the registry and use their registry identity when possible to help deduplicate across the two origins.]'
        '--replace-scm-with-registry[Look up source control dependencies in the registry and use the registry to retrieve them instead of source control when possible.]'
        '--default-registry-url[Default registry URL to use, instead of the registries.json configuration file.]:default-registry-url:'
        '(--configuration -c)'{--configuration,-c}'[Build with configuration]:configuration:{__swift_complete "${___configuration[@]}"}'
        '*-Xcc[Pass flag through to all C compiler invocations.]:Xcc:'
        '*-Xswiftc[Pass flag through to all Swift compiler invocations.]:Xswiftc:'
        '*-Xlinker[Pass flag through to all linker invocations.]:Xlinker:'
        '*-Xcxx[Pass flag through to all C++ compiler invocations.]:Xcxx:'
        '--triple:triple:'
        '--sdk:sdk:_files -/'
        '--toolchain:toolchain:_files -/'
        '--swift-sdk[Filter for selecting a specific Swift SDK to build with.]:swift-sdk:'
        '*--sanitize[Turn on runtime checks for erroneous behavior.]:sanitize:{__swift_complete "${___sanitize[@]}"}'
        '--auto-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-index-store[Enable or disable indexing-while-building feature.]'
        '--disable-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-parseable-module-interfaces'
        '(--jobs -j)'{--jobs,-j}'[The number of jobs to spawn in parallel during the build process.]:jobs:'
        '--use-integrated-swift-driver'
        '--explicit-target-dependency-import-check[A flag that indicates this build should check whether targets only import their explicitly-declared dependencies.]:explicit-target-dependency-import-check:{__swift_complete "${___explicit_target_dependency_import_check[@]}"}'
        '--build-system[Specify the build system to use.]:build-system:{__swift_complete "${___build_system[@]}"}'
        '-debug-info-format[The Debug Information Format to use.]:debug-info-format:{__swift_complete "${__debug_info_format[@]}"}'
        '--enable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-local-rpath[Disable adding $ORIGIN/@loader_path to the rpath by default.]'
        '--traits[Enables the passed traits of the package. Multiple traits can be specified by providing a comma separated list e.g. `--traits Trait1,Trait2`. When enabling specific traits the defaults traits need to explictily enabled as well by passing `defaults` to this command.]:traits:'
        '--enable-all-traits[Enables all traits of the package.]'
        '--disable-default-traits[Disables all default traits of the package.]'
        '--set-current[Set tools version of package to the current tools version in use.]'
        '--set[Set tools version of package to the given value.]:set:'
        '--version[Show the version.]'
        '(-help -h --help)'{-help,-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_swift_package_compute-checksum() {
    local -i ret=1
    local -ar ___configuration=('debug' 'release')
    local -ar ___sanitize=('address' 'thread' 'undefined' 'scudo' 'fuzzer')
    local -ar ___explicit_target_dependency_import_check=('none' 'warn' 'error')
    local -ar ___build_system=('native' 'swiftbuild' 'xcode')
    local -ar __debug_info_format=('dwarf' 'codeview' 'none')
    local -ar arg_specs=(
        '--package-path[Specify the package path to operate on (default current directory). This changes the working directory before any other operation.]:package-path:_files -/'
        '--cache-path[Specify the shared cache directory path.]:cache-path:_files -/'
        '--config-path[Specify the shared configuration directory path.]:config-path:_files -/'
        '--security-path[Specify the shared security directory path.]:security-path:_files -/'
        '--scratch-path[Specify a custom scratch directory path. (default .build)]:scratch-path:_files -/'
        '--swift-sdks-path[Path to the directory containing installed Swift SDKs.]:swift-sdks-path:_files -/'
        '*--toolset[Specify a toolset JSON file to use when building for the target platform. Use the option multiple times to specify more than one toolset. Toolsets will be merged in the order they'\''re specified into a single final toolset for the current build.]:toolset:_files -g '\''*..json'\'''
        '*--pkg-config-path[Specify alternative path to search for pkg-config `.pc` files. Use the option multiple times to
        specify more than one path.]:pkg-config-path:_files -/'
        '--enable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--disable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--enable-build-manifest-caching'
        '--disable-build-manifest-caching'
        '--manifest-cache[Caching mode of Package.swift manifests. Valid values are\: (shared\: shared cache, local\: package'\''s build directory, none\: disabled)]:manifest-cache:'
        '--enable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '--disable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '(--verbose -v)'{--verbose,-v}'[Increase verbosity to include informational output.]'
        '(--very-verbose --vv)'{--very-verbose,--vv}'[Increase verbosity to include debug output.]'
        '(--quiet -q)'{--quiet,-q}'[Decrease verbosity to only include error output.]'
        '--color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--no-color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--disable-sandbox[Disable using the sandbox when executing subprocesses.]'
        '--netrc[Use netrc file even in cases where other credential stores are preferred.]'
        '--enable-netrc[Load credentials from a netrc file.]'
        '--disable-netrc[Load credentials from a netrc file.]'
        '--netrc-file[Specify the netrc file path.]:netrc-file:_files'
        '--enable-keychain[Search credentials in macOS keychain.]'
        '--disable-keychain[Search credentials in macOS keychain.]'
        '--resolver-fingerprint-checking:resolver-fingerprint-checking:'
        '--resolver-signing-entity-checking:resolver-signing-entity-checking:'
        '--enable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--disable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--enable-prefetching'
        '--disable-prefetching'
        '(--force-resolved-versions --disable-automatic-resolution --only-use-versions-from-resolved-file)'{--force-resolved-versions,--disable-automatic-resolution,--only-use-versions-from-resolved-file}'[Only use versions from the Package.resolved file and fail resolution if it is out-of-date.]'
        '--skip-update[Skip updating dependencies from their remote during a resolution.]'
        '--disable-scm-to-registry-transformation[Disable source control to registry transformation.]'
        '--use-registry-identity-for-scm[Look up source control dependencies in the registry and use their registry identity when possible to help deduplicate across the two origins.]'
        '--replace-scm-with-registry[Look up source control dependencies in the registry and use the registry to retrieve them instead of source control when possible.]'
        '--default-registry-url[Default registry URL to use, instead of the registries.json configuration file.]:default-registry-url:'
        '(--configuration -c)'{--configuration,-c}'[Build with configuration]:configuration:{__swift_complete "${___configuration[@]}"}'
        '*-Xcc[Pass flag through to all C compiler invocations.]:Xcc:'
        '*-Xswiftc[Pass flag through to all Swift compiler invocations.]:Xswiftc:'
        '*-Xlinker[Pass flag through to all linker invocations.]:Xlinker:'
        '*-Xcxx[Pass flag through to all C++ compiler invocations.]:Xcxx:'
        '--triple:triple:'
        '--sdk:sdk:_files -/'
        '--toolchain:toolchain:_files -/'
        '--swift-sdk[Filter for selecting a specific Swift SDK to build with.]:swift-sdk:'
        '*--sanitize[Turn on runtime checks for erroneous behavior.]:sanitize:{__swift_complete "${___sanitize[@]}"}'
        '--auto-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-index-store[Enable or disable indexing-while-building feature.]'
        '--disable-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-parseable-module-interfaces'
        '(--jobs -j)'{--jobs,-j}'[The number of jobs to spawn in parallel during the build process.]:jobs:'
        '--use-integrated-swift-driver'
        '--explicit-target-dependency-import-check[A flag that indicates this build should check whether targets only import their explicitly-declared dependencies.]:explicit-target-dependency-import-check:{__swift_complete "${___explicit_target_dependency_import_check[@]}"}'
        '--build-system[Specify the build system to use.]:build-system:{__swift_complete "${___build_system[@]}"}'
        '-debug-info-format[The Debug Information Format to use.]:debug-info-format:{__swift_complete "${__debug_info_format[@]}"}'
        '--enable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-local-rpath[Disable adding $ORIGIN/@loader_path to the rpath by default.]'
        '--traits[Enables the passed traits of the package. Multiple traits can be specified by providing a comma separated list e.g. `--traits Trait1,Trait2`. When enabling specific traits the defaults traits need to explictily enabled as well by passing `defaults` to this command.]:traits:'
        '--enable-all-traits[Enables all traits of the package.]'
        '--disable-default-traits[Disables all default traits of the package.]'
        ':path:_files -/'
        '--version[Show the version.]'
        '(-help -h --help)'{-help,-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_swift_package_archive-source() {
    local -i ret=1
    local -ar ___configuration=('debug' 'release')
    local -ar ___sanitize=('address' 'thread' 'undefined' 'scudo' 'fuzzer')
    local -ar ___explicit_target_dependency_import_check=('none' 'warn' 'error')
    local -ar ___build_system=('native' 'swiftbuild' 'xcode')
    local -ar __debug_info_format=('dwarf' 'codeview' 'none')
    local -ar arg_specs=(
        '--package-path[Specify the package path to operate on (default current directory). This changes the working directory before any other operation.]:package-path:_files -/'
        '--cache-path[Specify the shared cache directory path.]:cache-path:_files -/'
        '--config-path[Specify the shared configuration directory path.]:config-path:_files -/'
        '--security-path[Specify the shared security directory path.]:security-path:_files -/'
        '--scratch-path[Specify a custom scratch directory path. (default .build)]:scratch-path:_files -/'
        '--swift-sdks-path[Path to the directory containing installed Swift SDKs.]:swift-sdks-path:_files -/'
        '*--toolset[Specify a toolset JSON file to use when building for the target platform. Use the option multiple times to specify more than one toolset. Toolsets will be merged in the order they'\''re specified into a single final toolset for the current build.]:toolset:_files -g '\''*..json'\'''
        '*--pkg-config-path[Specify alternative path to search for pkg-config `.pc` files. Use the option multiple times to
        specify more than one path.]:pkg-config-path:_files -/'
        '--enable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--disable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--enable-build-manifest-caching'
        '--disable-build-manifest-caching'
        '--manifest-cache[Caching mode of Package.swift manifests. Valid values are\: (shared\: shared cache, local\: package'\''s build directory, none\: disabled)]:manifest-cache:'
        '--enable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '--disable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '(--verbose -v)'{--verbose,-v}'[Increase verbosity to include informational output.]'
        '(--very-verbose --vv)'{--very-verbose,--vv}'[Increase verbosity to include debug output.]'
        '(--quiet -q)'{--quiet,-q}'[Decrease verbosity to only include error output.]'
        '--color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--no-color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--disable-sandbox[Disable using the sandbox when executing subprocesses.]'
        '--netrc[Use netrc file even in cases where other credential stores are preferred.]'
        '--enable-netrc[Load credentials from a netrc file.]'
        '--disable-netrc[Load credentials from a netrc file.]'
        '--netrc-file[Specify the netrc file path.]:netrc-file:_files'
        '--enable-keychain[Search credentials in macOS keychain.]'
        '--disable-keychain[Search credentials in macOS keychain.]'
        '--resolver-fingerprint-checking:resolver-fingerprint-checking:'
        '--resolver-signing-entity-checking:resolver-signing-entity-checking:'
        '--enable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--disable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--enable-prefetching'
        '--disable-prefetching'
        '(--force-resolved-versions --disable-automatic-resolution --only-use-versions-from-resolved-file)'{--force-resolved-versions,--disable-automatic-resolution,--only-use-versions-from-resolved-file}'[Only use versions from the Package.resolved file and fail resolution if it is out-of-date.]'
        '--skip-update[Skip updating dependencies from their remote during a resolution.]'
        '--disable-scm-to-registry-transformation[Disable source control to registry transformation.]'
        '--use-registry-identity-for-scm[Look up source control dependencies in the registry and use their registry identity when possible to help deduplicate across the two origins.]'
        '--replace-scm-with-registry[Look up source control dependencies in the registry and use the registry to retrieve them instead of source control when possible.]'
        '--default-registry-url[Default registry URL to use, instead of the registries.json configuration file.]:default-registry-url:'
        '(--configuration -c)'{--configuration,-c}'[Build with configuration]:configuration:{__swift_complete "${___configuration[@]}"}'
        '*-Xcc[Pass flag through to all C compiler invocations.]:Xcc:'
        '*-Xswiftc[Pass flag through to all Swift compiler invocations.]:Xswiftc:'
        '*-Xlinker[Pass flag through to all linker invocations.]:Xlinker:'
        '*-Xcxx[Pass flag through to all C++ compiler invocations.]:Xcxx:'
        '--triple:triple:'
        '--sdk:sdk:_files -/'
        '--toolchain:toolchain:_files -/'
        '--swift-sdk[Filter for selecting a specific Swift SDK to build with.]:swift-sdk:'
        '*--sanitize[Turn on runtime checks for erroneous behavior.]:sanitize:{__swift_complete "${___sanitize[@]}"}'
        '--auto-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-index-store[Enable or disable indexing-while-building feature.]'
        '--disable-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-parseable-module-interfaces'
        '(--jobs -j)'{--jobs,-j}'[The number of jobs to spawn in parallel during the build process.]:jobs:'
        '--use-integrated-swift-driver'
        '--explicit-target-dependency-import-check[A flag that indicates this build should check whether targets only import their explicitly-declared dependencies.]:explicit-target-dependency-import-check:{__swift_complete "${___explicit_target_dependency_import_check[@]}"}'
        '--build-system[Specify the build system to use.]:build-system:{__swift_complete "${___build_system[@]}"}'
        '-debug-info-format[The Debug Information Format to use.]:debug-info-format:{__swift_complete "${__debug_info_format[@]}"}'
        '--enable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-local-rpath[Disable adding $ORIGIN/@loader_path to the rpath by default.]'
        '--traits[Enables the passed traits of the package. Multiple traits can be specified by providing a comma separated list e.g. `--traits Trait1,Trait2`. When enabling specific traits the defaults traits need to explictily enabled as well by passing `defaults` to this command.]:traits:'
        '--enable-all-traits[Enables all traits of the package.]'
        '--disable-default-traits[Disables all default traits of the package.]'
        '(-o --output)'{-o,--output}'[The absolute or relative path for the generated source archive.]:output:_files -/'
        '--version[Show the version.]'
        '(-help -h --help)'{-help,-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_swift_package_completion-tool() {
    local -i ret=1
    local -ar ___configuration=('debug' 'release')
    local -ar ___sanitize=('address' 'thread' 'undefined' 'scudo' 'fuzzer')
    local -ar ___explicit_target_dependency_import_check=('none' 'warn' 'error')
    local -ar ___build_system=('native' 'swiftbuild' 'xcode')
    local -ar __debug_info_format=('dwarf' 'codeview' 'none')
    local -ar _mode=('generate-bash-script' 'generate-zsh-script' 'generate-fish-script' 'list-dependencies' 'list-executables' 'list-snippets')
    local -ar arg_specs=(
        '--package-path[Specify the package path to operate on (default current directory). This changes the working directory before any other operation.]:package-path:_files -/'
        '--cache-path[Specify the shared cache directory path.]:cache-path:_files -/'
        '--config-path[Specify the shared configuration directory path.]:config-path:_files -/'
        '--security-path[Specify the shared security directory path.]:security-path:_files -/'
        '--scratch-path[Specify a custom scratch directory path. (default .build)]:scratch-path:_files -/'
        '--swift-sdks-path[Path to the directory containing installed Swift SDKs.]:swift-sdks-path:_files -/'
        '*--toolset[Specify a toolset JSON file to use when building for the target platform. Use the option multiple times to specify more than one toolset. Toolsets will be merged in the order they'\''re specified into a single final toolset for the current build.]:toolset:_files -g '\''*..json'\'''
        '*--pkg-config-path[Specify alternative path to search for pkg-config `.pc` files. Use the option multiple times to
        specify more than one path.]:pkg-config-path:_files -/'
        '--enable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--disable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--enable-build-manifest-caching'
        '--disable-build-manifest-caching'
        '--manifest-cache[Caching mode of Package.swift manifests. Valid values are\: (shared\: shared cache, local\: package'\''s build directory, none\: disabled)]:manifest-cache:'
        '--enable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '--disable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '(--verbose -v)'{--verbose,-v}'[Increase verbosity to include informational output.]'
        '(--very-verbose --vv)'{--very-verbose,--vv}'[Increase verbosity to include debug output.]'
        '(--quiet -q)'{--quiet,-q}'[Decrease verbosity to only include error output.]'
        '--color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--no-color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--disable-sandbox[Disable using the sandbox when executing subprocesses.]'
        '--netrc[Use netrc file even in cases where other credential stores are preferred.]'
        '--enable-netrc[Load credentials from a netrc file.]'
        '--disable-netrc[Load credentials from a netrc file.]'
        '--netrc-file[Specify the netrc file path.]:netrc-file:_files'
        '--enable-keychain[Search credentials in macOS keychain.]'
        '--disable-keychain[Search credentials in macOS keychain.]'
        '--resolver-fingerprint-checking:resolver-fingerprint-checking:'
        '--resolver-signing-entity-checking:resolver-signing-entity-checking:'
        '--enable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--disable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--enable-prefetching'
        '--disable-prefetching'
        '(--force-resolved-versions --disable-automatic-resolution --only-use-versions-from-resolved-file)'{--force-resolved-versions,--disable-automatic-resolution,--only-use-versions-from-resolved-file}'[Only use versions from the Package.resolved file and fail resolution if it is out-of-date.]'
        '--skip-update[Skip updating dependencies from their remote during a resolution.]'
        '--disable-scm-to-registry-transformation[Disable source control to registry transformation.]'
        '--use-registry-identity-for-scm[Look up source control dependencies in the registry and use their registry identity when possible to help deduplicate across the two origins.]'
        '--replace-scm-with-registry[Look up source control dependencies in the registry and use the registry to retrieve them instead of source control when possible.]'
        '--default-registry-url[Default registry URL to use, instead of the registries.json configuration file.]:default-registry-url:'
        '(--configuration -c)'{--configuration,-c}'[Build with configuration]:configuration:{__swift_complete "${___configuration[@]}"}'
        '*-Xcc[Pass flag through to all C compiler invocations.]:Xcc:'
        '*-Xswiftc[Pass flag through to all Swift compiler invocations.]:Xswiftc:'
        '*-Xlinker[Pass flag through to all linker invocations.]:Xlinker:'
        '*-Xcxx[Pass flag through to all C++ compiler invocations.]:Xcxx:'
        '--triple:triple:'
        '--sdk:sdk:_files -/'
        '--toolchain:toolchain:_files -/'
        '--swift-sdk[Filter for selecting a specific Swift SDK to build with.]:swift-sdk:'
        '*--sanitize[Turn on runtime checks for erroneous behavior.]:sanitize:{__swift_complete "${___sanitize[@]}"}'
        '--auto-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-index-store[Enable or disable indexing-while-building feature.]'
        '--disable-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-parseable-module-interfaces'
        '(--jobs -j)'{--jobs,-j}'[The number of jobs to spawn in parallel during the build process.]:jobs:'
        '--use-integrated-swift-driver'
        '--explicit-target-dependency-import-check[A flag that indicates this build should check whether targets only import their explicitly-declared dependencies.]:explicit-target-dependency-import-check:{__swift_complete "${___explicit_target_dependency_import_check[@]}"}'
        '--build-system[Specify the build system to use.]:build-system:{__swift_complete "${___build_system[@]}"}'
        '-debug-info-format[The Debug Information Format to use.]:debug-info-format:{__swift_complete "${__debug_info_format[@]}"}'
        '--enable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-local-rpath[Disable adding $ORIGIN/@loader_path to the rpath by default.]'
        '--traits[Enables the passed traits of the package. Multiple traits can be specified by providing a comma separated list e.g. `--traits Trait1,Trait2`. When enabling specific traits the defaults traits need to explictily enabled as well by passing `defaults` to this command.]:traits:'
        '--enable-all-traits[Enables all traits of the package.]'
        '--disable-default-traits[Disables all default traits of the package.]'
        ':mode:{__swift_complete "${_mode[@]}"}'
        '--version[Show the version.]'
        '(-help -h --help)'{-help,-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_swift_package_plugin() {
    local -i ret=1
    local -ar ___configuration=('debug' 'release')
    local -ar ___sanitize=('address' 'thread' 'undefined' 'scudo' 'fuzzer')
    local -ar ___explicit_target_dependency_import_check=('none' 'warn' 'error')
    local -ar ___build_system=('native' 'swiftbuild' 'xcode')
    local -ar __debug_info_format=('dwarf' 'codeview' 'none')
    local -ar ___allow_network_connections=('none' 'local(ports: [])' 'all(ports: [])' 'docker' 'unixDomainSocket')
    local -ar arg_specs=(
        '--package-path[Specify the package path to operate on (default current directory). This changes the working directory before any other operation.]:package-path:_files -/'
        '--cache-path[Specify the shared cache directory path.]:cache-path:_files -/'
        '--config-path[Specify the shared configuration directory path.]:config-path:_files -/'
        '--security-path[Specify the shared security directory path.]:security-path:_files -/'
        '--scratch-path[Specify a custom scratch directory path. (default .build)]:scratch-path:_files -/'
        '--swift-sdks-path[Path to the directory containing installed Swift SDKs.]:swift-sdks-path:_files -/'
        '*--toolset[Specify a toolset JSON file to use when building for the target platform. Use the option multiple times to specify more than one toolset. Toolsets will be merged in the order they'\''re specified into a single final toolset for the current build.]:toolset:_files -g '\''*..json'\'''
        '*--pkg-config-path[Specify alternative path to search for pkg-config `.pc` files. Use the option multiple times to
        specify more than one path.]:pkg-config-path:_files -/'
        '--enable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--disable-dependency-cache[Use a shared cache when fetching dependencies.]'
        '--enable-build-manifest-caching'
        '--disable-build-manifest-caching'
        '--manifest-cache[Caching mode of Package.swift manifests. Valid values are\: (shared\: shared cache, local\: package'\''s build directory, none\: disabled)]:manifest-cache:'
        '--enable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '--disable-experimental-prebuilts[Whether to use prebuilt swift-syntax libraries for macros.]'
        '(--verbose -v)'{--verbose,-v}'[Increase verbosity to include informational output.]'
        '(--very-verbose --vv)'{--very-verbose,--vv}'[Increase verbosity to include debug output.]'
        '(--quiet -q)'{--quiet,-q}'[Decrease verbosity to only include error output.]'
        '--color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--no-color-diagnostics[Enables or disables color diagnostics when printing to a TTY.
        By default, color diagnostics are enabled when connected to a TTY and disabled otherwise.]'
        '--disable-sandbox[Disable using the sandbox when executing subprocesses.]'
        '--netrc[Use netrc file even in cases where other credential stores are preferred.]'
        '--enable-netrc[Load credentials from a netrc file.]'
        '--disable-netrc[Load credentials from a netrc file.]'
        '--netrc-file[Specify the netrc file path.]:netrc-file:_files'
        '--enable-keychain[Search credentials in macOS keychain.]'
        '--disable-keychain[Search credentials in macOS keychain.]'
        '--resolver-fingerprint-checking:resolver-fingerprint-checking:'
        '--resolver-signing-entity-checking:resolver-signing-entity-checking:'
        '--enable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--disable-signature-validation[Validate signature of a signed package release downloaded from registry.]'
        '--enable-prefetching'
        '--disable-prefetching'
        '(--force-resolved-versions --disable-automatic-resolution --only-use-versions-from-resolved-file)'{--force-resolved-versions,--disable-automatic-resolution,--only-use-versions-from-resolved-file}'[Only use versions from the Package.resolved file and fail resolution if it is out-of-date.]'
        '--skip-update[Skip updating dependencies from their remote during a resolution.]'
        '--disable-scm-to-registry-transformation[Disable source control to registry transformation.]'
        '--use-registry-identity-for-scm[Look up source control dependencies in the registry and use their registry identity when possible to help deduplicate across the two origins.]'
        '--replace-scm-with-registry[Look up source control dependencies in the registry and use the registry to retrieve them instead of source control when possible.]'
        '--default-registry-url[Default registry URL to use, instead of the registries.json configuration file.]:default-registry-url:'
        '(--configuration -c)'{--configuration,-c}'[Build with configuration]:configuration:{__swift_complete "${___configuration[@]}"}'
        '*-Xcc[Pass flag through to all C compiler invocations.]:Xcc:'
        '*-Xswiftc[Pass flag through to all Swift compiler invocations.]:Xswiftc:'
        '*-Xlinker[Pass flag through to all linker invocations.]:Xlinker:'
        '*-Xcxx[Pass flag through to all C++ compiler invocations.]:Xcxx:'
        '--triple:triple:'
        '--sdk:sdk:_files -/'
        '--toolchain:toolchain:_files -/'
        '--swift-sdk[Filter for selecting a specific Swift SDK to build with.]:swift-sdk:'
        '*--sanitize[Turn on runtime checks for erroneous behavior.]:sanitize:{__swift_complete "${___sanitize[@]}"}'
        '--auto-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-index-store[Enable or disable indexing-while-building feature.]'
        '--disable-index-store[Enable or disable indexing-while-building feature.]'
        '--enable-parseable-module-interfaces'
        '(--jobs -j)'{--jobs,-j}'[The number of jobs to spawn in parallel during the build process.]:jobs:'
        '--use-integrated-swift-driver'
        '--explicit-target-dependency-import-check[A flag that indicates this build should check whether targets only import their explicitly-declared dependencies.]:explicit-target-dependency-import-check:{__swift_complete "${___explicit_target_dependency_import_check[@]}"}'
        '--build-system[Specify the build system to use.]:build-system:{__swift_complete "${___build_system[@]}"}'
        '-debug-info-format[The Debug Information Format to use.]:debug-info-format:{__swift_complete "${__debug_info_format[@]}"}'
        '--enable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-dead-strip[Disable/enable dead code stripping by the linker.]'
        '--disable-local-rpath[Disable adding $ORIGIN/@loader_path to the rpath by default.]'
        '--traits[Enables the passed traits of the package. Multiple traits can be specified by providing a comma separated list e.g. `--traits Trait1,Trait2`. When enabling specific traits the defaults traits need to explictily enabled as well by passing `defaults` to this command.]:traits:'
        '--enable-all-traits[Enables all traits of the package.]'
        '--disable-default-traits[Disables all default traits of the package.]'
        '--list[List the available command plugins.]'
        '--allow-writing-to-package-directory[Allow the plugin to write to the package directory.]'
        '*--allow-writing-to-directory[Allow the plugin to write to an additional directory.]:allow-writing-to-directory:'
        '--allow-network-connections:allow-network-connections:{__swift_complete "${___allow_network_connections[@]}"}'
        '--package[Limit available plugins to a single package with the given identity.]:package:'
        ':command:'
        '*:arguments:'
        '--version[Show the version.]'
        '(-help -h --help)'{-help,-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_swift_help() {
    local -i ret=1
    local -ar arg_specs=(
        '*:subcommands:'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_swift
