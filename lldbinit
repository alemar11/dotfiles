# One word aliases.
command alias alias command alias
alias unalias command unalias
alias import command script import
alias shell platform shell

# Set breakpoint in a file at a given line
# ie: bfl foo.c 12
#alias bfl breakpoint set -f %1 -l %2 